# Checkpoint C handoff

- **State:** complete and reviewer-accepted at the mandatory pause. Checkpoint D remains paused.
- **Outcome:** latest v5 Workloads with a validated `SetupPlan` execute the twelve source-derived setup actions once and in order, retain non-void objects for the attempt, settle setup-created pending events, resolve participant arguments from those retained objects, and only then start the target Sagas. Setup/reset/isolation failure is pre-target failure; execution-side failure leaves ImpactV1 `NOT_EVALUATED`. Explicit valid v4 Workloads still use the prerequisite-provider path.
- **Fresh-state remediation:** source-setup **preflight** no longer loops through candidates in one runtime context. `ScenarioExecutorCli` reads and validates the package before starting Spring. If source-setup candidates exist, `ScenarioSetupPreflightProcessOrchestrator` launches one fresh JVM per source-setup WorkloadPlan; candidates without source setup retain the legacy one-process batch. Every worker has an explicit five-minute wall timeout. Timeout, parent interruption, wait failure, or other exceptional lifecycle exit invokes `destroy()`, waits at most five seconds, invokes `destroyForcibly()` if still alive, and waits at most another five seconds; consumed interrupt status is restored. A nonzero exit is rejected before report ingestion. A zero-exit fragment is merged only after strict raw-envelope and semantic validation of report schema, terminal `SUCCESS`, candidate selection/count and exact IDs, normalized package path, runtime metadata, participant counts/states, blockers, and source-setup evidence. Direct in-process batch preflight still rejects a package containing source setup before dispatcher or target invocation. Any lifecycle, exit, fragment, validation, or cleanup failure becomes `FRESH_STATE_ISOLATION_FAILED`. No database reset API or same-process reset was invented.
- **Collection-typing remediation:** `SetupPlanValidator` now carries the exact closed generic expectations for `QuestionDto.topicDto` (`Set<TopicDto>`) and `QuestionDto.optionDtos` (`List<OptionDto>`), derives element types from expected collection signatures, recognizes the approved list/set concrete shapes including `HashSet`, and validates every element recursively. `ScenarioSetupRunner` independently derives generic parameter types from exact dispatcher arguments and DTO setters through `Type`/`ParameterizedType`, fully validates all nested values before action 1, materializes each element against that expected type, and uses `HashSet` when the exact target requires it. Action 12's `List<Integer>` is therefore checked element-by-element before dispatch. The closed B value/type language remains unchanged; there is no generic evaluator.
- **Dispatch boundary:** `ScenarioSetupActionDispatcher` remains the application-owned closed method map. `ScenarioSetupRunner` resolves every method key, validates the complete runtime value/property/type contract, and rejects active fault state before setup. Reflection remains limited to validator-approved DTO no-argument constructors, exact setters, and exact `getAggregateId`/`getCourseAggregateId` properties. `QuizzesSourceSetupActionDispatcher` still contains seven Java-confirmed facade keys and typed lambdas for the twelve calls.
- **Remediation production files:** updated `verifiers/.../executor/ScenarioExecutor.java`, `ScenarioExecutorCli.java`, and `ScenarioSetupRunner.java`; added `ScenarioSetupPreflightProcessOrchestrator.java`; updated `verifiers/.../scenario/SetupPlanValidator.java`. Existing C report schemas remain execution-report v5 and preflight-report v1. Package identity, scheduling, fault, recovery, event-consequence, impact, and provider semantics did not change.
- **Remediation tests:** updated `ScenarioExecutorSpec.groovy` and `ScenarioModelSpec.groovy`; added `ScenarioSetupPreflightProcessOrchestratorSpec.groovy`. Stateful setup tests prove in-process source preflight fails before persistent dispatcher mutation. Six process-lifecycle tests use real subprocesses to prove separate source workers plus one legacy batch, normalized relative package/output/working paths, timeout termination, parent-interrupt termination with interrupt restoration, rejection of a nonzero exit despite a success-looking fragment, rejection of an invalid envelope, launch-failure reporting, and recursive fragment cleanup on success and failure. Collection tests prove exact `List<Integer>` acceptance, wrong nested action-12 elements fail with dispatcher `invocationCount == 0` and no target constructor/body, and `QuestionDto` `Set<TopicDto>`/`List<OptionDto>` reject wrong element types while valid exact DTO setup remains accepted.
- **Focused proof:** after lifecycle remediation, `cd verifiers && mvn -q -Dtest=ScenarioExecutorSpec,ScenarioExecutorReadinessEvaluatorSpec,ScenarioExecutorWrapperSpec,ScenarioExecutorOrchestratorSpec,ScenarioSetupPreflightProcessOrchestratorSpec,SourceDerivedSharedSagaWorkloadAnalysisSpec,ScenarioModelSpec test` passed 244 tests with zero failures/errors/skips. `cd applications/quizzes && mvn -q -Ptest-sagas -Dtest=QuizzesStaleReadPrerequisiteProviderTest test` passed 1 test. Final `cd verifiers && mvn -q test` passed 682 tests with zero failures/errors/skips.
- **Existing readable isolated trace:** the existing 388 KiB focused package and `setup-preflight-report-remediated.json` were preserved; no catalog was regenerated and no new Docker setup run was needed for this process-lifecycle-only correction. That earlier trace still shows one isolated Quizzes Spring/H2 worker, twelve successful setup actions, and shared action-12 Tournament reuse, but it predates the timeout/envelope/cleanup correction and is not cited as proof of those lifecycle semantics. This remains preflight-only evidence; no Checkpoint D target action ran.
- **Package immutability:** the reused package's five hashes remain: manifest `304ef04a2a7ad877a964fa77f8452b41eccf604688eb32690ae1ad7be1bfba78`; workloads `58e879a94ae761f0704437103961c6d7c31fc0f615aae05fe9db69f1a57c5f12`; faults `471fa6112649830bf46a49202cef59f2401f5f4451beeb4c18fdaae0a0ff1f4a`; accounting `3c524de95d39224d332efaca3ad7e5dba0dbb9c3033f6bff8beccd96382df1af`; rejected-inputs empty SHA-256 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`. Runtime IDs remain outside the five package files.
- **Compatibility impact:** supported CLI/wrapper source-setup preflight now incurs one JVM/Spring/H2 startup per source-setup candidate. Legacy/no-setup preflight keeps its prior batch behavior. Direct callers of `ScenarioExecutor.preflight(...)` must use the supported CLI/wrapper for source-setup packages; the method now fails before mutation instead of making an unsafe in-process freshness claim. Normal one-scenario execution is already one process per attempt and is unchanged.
- **Artifacts/disk:** no broad catalog was generated. Worker fragments are now deleted explicitly after every attempt and their parent directory is recursively removed. The obsolete root-owned `.setup-preflight-workers-6085593156637926026` directory was removed through the existing Docker image/mount while final readable package/report evidence was preserved. After validation, `verifiers/target` remained about 1.4 GiB, `applications/quizzes/target` about 19 MiB, and free disk was about 6.4 GiB (89% used). No image/cache or unrelated artifact cleanup was performed.
- **Scope checks:** `git diff --check` passed after the remediation. The ordinary recovery-window test, current-state handbook, scheduler/order/caps/reservation, final-impact issue, fault/recovery/event-consequence/impact code, and Checkpoint D harmful/control benchmark were not changed. No material architecture change outside approved C orchestration was required.
- **Reviewer decision:** the final read-only medium-tier review found no blocking issues and accepted Checkpoint C. It confirmed bounded worker termination, interrupt restoration, fail-closed exit/report validation, recursive cleanup, candidate isolation, legacy batching, recursive collection typing, package immutability, and scope control.
- **Remaining C evidence:** none known. Checkpoint D remains unstarted and requires separate approval.
