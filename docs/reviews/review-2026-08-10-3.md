# Artifacts Review — 2026-08-10

**Date:** 2026-08-10 (third run of the day; `review-2026-08-10.md` and `review-2026-08-10-2.md` precede it)
**Skill files reviewed:** 11 (`_shared/conventions.md`, `_shared/session-completion.md`, `boot-strap/SKILL.md`, `boot-strap/templates/README.md`, `classify-and-plan/SKILL.md`, `harness-retrospective/SKILL.md`, `implement-aggregate/SKILL.md`, `implement-aggregate/session-a..d.md`, `implement-aggregate-full/SKILL.md`, `review-artifacts/SKILL.md`)
**Scaffold templates reviewed:** 9
**Agent contract files reviewed:** 1 (`.claude/agents/aggregate-slice.md`)
**Doc files reviewed:** 11 (`AGENTS.md`, `docs/architecture.md`, `docs/workflow.md`, 6 × `docs/concepts/`, 2 × `docs/templates/`)
**Verdict:** Issues requiring action

> **Clean** = all alignment checks pass; only improvement opportunities found.
> **Minor issues** = inconsistencies present but none cause incorrect agent output.
> **Issues requiring action** = any inconsistency that would cause an agent to produce
>   incorrect output.

---

## Executive Summary

One Major finding produces incorrect output: `classify-and-plan/SKILL.md` Step 7 lists the per-read-op
command file as `Get{Query}Command.java` while its own substitution rule defines `{Query}` as the
read operation name already carrying `Get` (`GetOpenShipments`), so every generated plan.md names
`GetGetOpenShipmentsCommand.java` — and `session-b.md` instructs the agent to "match plan.md file list
exactly". A second Major finding: `AGENTS.md` § Module Map places `reviews/` under
`applications/{app-name}/`, while every skill that writes a review writes to `docs/reviews/` and no
such per-application directory exists. Everything else is clean: all path references resolve, P1–P4
and R1–R8 are consistent across all six locations each, and the neutral-domain scan is clean over
1156 added lines with zero hits.

---

## Check 1 — Path Validity

Every literal `docs/…`, `.claude/…` and `AGENTS.md` reference across the review set was extracted
mechanically (`python3`, not `rg`) and existence-checked. No broken reference found.

| Source file | Referenced path | Status | Notes |
|-------------|----------------|--------|-------|
| all skills, all docs | every `docs/**/*.md` reference | Exists | 0 broken |
| all skills, all docs | every `.claude/skills/**` and `.claude/agents/**` reference | Exists | 0 broken |
| `implement-aggregate/SKILL.md`, `implement-aggregate-full/SKILL.md`, `aggregate-slice.md` | `AGENTS.md` § "Harness evolution" | Exists | section present |
| `docs/architecture.md`, `docs/concepts/{aggregate,sagas,testing}.md`, `_shared/session-completion.md` | `simulator/.../ms/...` elided paths (14) | Exists | written with `.../` elision; each resolves to a real file under `simulator/src/main/java/pt/ulisboa/tecnico/socialsoftware/ms/` — verified by simple-name lookup |
| `boot-strap/SKILL.md` | `applications/*/.mvn`, `simulator/.mvn/maven.config` | Template / untracked | glob + machine config, correctly not existence-checked |
| `implement-aggregate/SKILL.md` | `applications/*/plan.md` | Template | glob, correct |
| `classify-and-plan/SKILL.md` | `applications/my-app/my-app-domain-model.md`, `…-aggregate-grouping.md` | Template-adjacent | usage examples under § Input, paired on the next line with the `{app-name}` form; `my-app` is the docs' neutral placeholder app, not the app under generation — not a leaked literal |
| `review-artifacts/SKILL.md` | `applications/{APP}/plan.md` | Template | inside the Check 4 python script, where `{app-name}` is bound to `APP` — correct |

All `applications/…` prose references outside the rows above use `applications/{app-name}/…` and match
the layout in `docs/workflow.md` § Conventions → Package layout. No literal application name is
hard-coded anywhere in the review set.

### Scaffold template contents

| Template | Symbol claimed | Claimed by | Present? | Notes |
|----------|---------------|------------|----------|-------|
| all 9 | the nine-template list | `boot-strap/SKILL.md:47-57` | Yes | directory holds exactly these 9 plus `README.md` |
| `AppSimulator.java.template` | `@SpringBootApplication`, `@EnableJpaRepositories`/`@EntityScan` over `…{pkg}` and `…ms`, package `…{{PKG}}` | `boot-strap/SKILL.md:220` | Yes | — |
| `SpockTest.groovy.template` | package `pt.ulisboa.tecnico.socialsoftware` (parent, not `{pkg}`) | `boot-strap/SKILL.md:104-105`, `docs/workflow.md:53`, `testing.md:137` | Yes | — |
| `BeanConfigurationSagas.groovy.template` | `@PropertySource("classpath:application-test.properties")` | `boot-strap/SKILL.md:222` | Yes | — |
| `BeanConfigurationSagas.groovy.template` | marker comments "added here as aggregates are implemented in Phase 2" | `boot-strap/SKILL.md:88-90` | Yes | two markers: domain imports (l.28-29), domain beans (l.127) |
| `BeanConfigurationSagas.groovy.template` | `ImpairmentService` bean | `testing.md:515-516` | Yes | l.64-67 |
| `BeanConfigurationSagas.groovy.template` | more than one `CommandGateway` implementation registered | `testing.md:446-450` | Yes | `LocalCommandGateway` (l.85) + mocked `StreamCommandGateway` (l.102) — the ambiguity the doc warns about is real |
| `AppSpockTest.groovy.template` | markers `// Domain constants…`, `// Domain @Autowired fields…`, `// Domain imports`, `// Domain create* helpers…` | `session-a.md:285-297` | Yes | l.12, 16, 25, 53 |
| `AppSpockTest.groovy.template` | `impairmentService` field + `loadBehaviorScripts()` | `testing.md:515-519` | Yes | l.18-19, 27-31 |
| `AppSpockTest.groovy.template` | `sagaStateOf(aggregateId)` | `testing.md` § T4, § Compensation Test | Yes | l.33-37 |
| `AppSpockTest.groovy.template` | `loadForCheck(aggregateId, type)` | `testing.md:318,325` | Yes | l.39-42 |
| `AppSpockTest.groovy.template` | `flushAndClear()` = `EntityManager.flush()` then `clear()` | `testing.md:205-206` | Yes | l.48-51 |
| `AppSpockTest.groovy.template` | does **not** declare `commandGateway` | `testing.md:440-441` | Yes (absent, as claimed) | — |
| `AppSpockTest.groovy.template` | does **not** declare `aggregateIdGeneratorService` | `session-b.md:220-221` | Yes (absent, as claimed) | — |
| `AppErrorMessage.java.template` | `PLACEHOLDER = "placeholder"` | `templates/README.md:26-32` | Yes | l.29 |
| `AppException.java.template` | sole user of `{{appClass}}` | `templates/README.md:21-22` | Yes | 38 occurrences here, 0 in the other 8 templates |
| `pom.xml.template` | surefire include `**/{pkg}/sagas/**` | `boot-strap/SKILL.md:190` | Yes | l.78 |
| `pom.xml.template` | `test-sagas` profile | `AGENTS.md` § Build Commands, `conventions.md` § Run the test suite | Yes | l.52-84 |
| `application-test.yaml.template` | `local.messaging.serialize: true` | `commands.md:32-36`, `testing.md:562` | Yes | l.32-34 |
| `application.yaml.template` | `{{APP_NAME}}` substitution | `templates/README.md` token table | Yes | l.3 |

No undocumented helper found: every method the scaffold base test class ships is described by
`docs/concepts/testing.md` or a session sub-file.

---

## Check 2 — Pattern Alignment (P1–P4 and R1–R8)

### P1–P4 Consistency

Authoritative one-liners from `docs/concepts/rule-enforcement-patterns.md` § Quick Reference:

- **P1** — single-aggregate state rule (incl. cached snapshot fields) → `verifyInvariants()` in the aggregate; strong.
- **P2** — cache state from another aggregate, never blocking → `getEventSubscriptions()` + event handler chain; eventual.
- **P3** — synchronous service-level check (own-table uniqueness **or** saga-assembled DTO validation) → guard in `*Service.java` inside `@Transactional(SERIALIZABLE)`, before any mutation; strong.
- **P4a** — precondition implicit in a saga fetch query (the query fails when unmet); no enforcement code.
- **P4b** — invariant holds because the saga passes the same value to two aggregates; no enforcement code.

| Location | Pattern | Claim | Authoritative definition | Verdict |
|----------|---------|-------|--------------------------|---------|
| `docs/architecture.md:247-253` — pattern selection table | P1/P3/P2/P4a/P4b | "Always true within one aggregate; derivable from its own fields → P1 `verifyInvariants()`"; "Synchronous service-level check (own-table uniqueness OR saga-assembled DTO field validation) → P3 service guard"; "Cross-aggregate; eventual consistency acceptable → P2"; "Precondition implicit in saga fetch / same value to two aggregates → P4a/P4b" | as above | Consistent |
| `docs/architecture.md:130-147` — Request Lifecycle | P1/P2/P3 | `[P3]` service-layer guard inside `@Transactional(SERIALIZABLE)` before mutation; `[P1]` `verifyInvariants()` at UoW commit; `[P2]` async EventProcessing caches publisher state | matches § Enforcement Lifecycle of the owning doc | Consistent |
| `classify-and-plan/SKILL.md:211-217` — Step 4 | P1–P4a/b | explicitly defers: "Apply the flowchart in `docs/concepts/rule-enforcement-patterns.md` § Decision Guide … That section is authoritative — do not re-derive the classification logic here" | n/a (pointer, not restatement) | Consistent |
| `session-a.md:62` | P1 | `verifyInvariants()` enforces all P1 rules from plan.md; reads only fields the aggregate holds; never a repository (R6) | P1 + R6 | Consistent |
| `session-c.md:67-68` | P3 | own-table uniqueness guards query the repository before creating; DTO field checks receive the saga-assembled DTO and validate before mutation | P3 both sub-cases | Consistent |
| `session-d.md:138` | P2 | "The invariant check happens inside the Functionalities update method, which loads the aggregate, applies the cached-field change and registers the new version changed" — caching only, no blocking | P2 (cache, never block) | Consistent |

No drift found. Each site either restates the definition verbatim in the owning vocabulary or points
at the owning section.

### R1–R8 Coverage

Authoritative one-liners from `docs/architecture.md` § Architectural Restrictions.

| Restriction | Expected location | Mentioned? | Consistent? | Notes |
|-------------|------------------|------------|-------------|-------|
| R1 — service loads only its own aggregate type | `service.md`, session-b, session-c | Yes | Yes | `service.md:48`; `session-b.md:60`; `session-c.md:64` ("loads no foreign aggregate") |
| R2 — service injects only its own components | `service.md`, session-b, session-c | Yes | Yes | `service.md:46` + the closed five-item list at 42-44; `session-b.md:171-177` (closed list repeated as a pointer to `service.md` § Injected Dependencies); `session-c.md:300-304` |
| R3 — cross-aggregate flow via DTOs only | `service.md`, session-b, session-c | Yes | Yes | `service.md:52`; `session-b.md:60`; `session-c.md:64` |
| R4 — saga steps declare lock intent | `sagas.md`, session-c | Yes | Yes | `sagas.md:110-118` § R4 Decision Table (3 cases: primary → `setSemanticLock`, foreign → `setForbiddenStates`, newly created → neither); `session-c.md:35,120` defers to it as authoritative |
| R5 — `getEventSubscriptions()` in consumer only | `events.md`, session-d | Yes | Yes | `events.md:61`; `session-d.md:48-49`; also `aggregate.md:31-32` and `rule-enforcement-patterns.md:229` |
| R6 — `verifyInvariants()` must not read from DB | `aggregate.md`, session-a | Yes | Yes | `aggregate.md:26-29`; `session-a.md:62` |
| R7 — DTOs are immutable value objects | `service.md`, session-b, session-c | Yes | Yes | `service.md:219-222` § DTO Immutability (R7); `session-b.md:101-102`; `session-c.md:21` |
| R8 — functionalities only send commands upstream | `commands.md`, session-b, session-c | Yes | Yes | `commands.md:143`; `sagas.md:433`; `session-b.md:99-101`; `session-c.md:120` |

All eight restrictions are stated in their owning concept doc and echoed (as pointers, not
paraphrases) in every session sub-file that can violate them.

---

## Check 3 — Improvement Opportunities

### Missing Examples

| Doc | Section | Why an example would help |
|-----|---------|---------------------------|
| `docs/concepts/rule-enforcement-patterns.md` | § Decision Guide, Step 4, case 1 ("A saga fetch already targets it → P4a … Add the § P4 comment at the saga step") | The section ships a Java block for case 2 (owned-entity P3 guard) but none for case 1. The obligation is a *comment* whose shape is defined elsewhere (§ P4a), so an agent has to cross-jump to learn what "the § P4 comment" looks like. Four lines of the `// Fetching this DTO enforces the precondition implicitly …` form, inline, would close it. |
| `docs/architecture.md` | R8 — Functionalities may only send commands to upstream aggregates | R7 immediately above it carries a WRONG/RIGHT Java pair; R8 is prose only. R8 is the restriction most easily violated by accident (a saga reaching "just one field" from a downstream aggregate), and the wrong shape is not self-evidently wrong on the page. |
| `docs/concepts/commands.md` | § What a Command Is — the two `null` `rootAggregateId` cases (create command, bulk read filtered by non-PK field) | Both are stated in blockquote prose against a worked example whose `super(...)` passes a real id. An agent copying the example and reading the blockquote must decide *which* constructor argument becomes `null`; showing the create-command constructor with `super(unitOfWork, serviceName, null)` removes the inference. |
| `docs/concepts/events.md` | § Cascade Invalidation Pattern, step 3 ("Downstream aggregates … subscribe to `Invalidate{Consumer}Event` and process it the same way") | Steps 1 and 2 carry code; step 3 — the consumer side, which is where the pattern actually closes — is one prose sentence. The reader has to reassemble the subscription + handler + ByEvent chain from § Canonical Wiring Snippet without being told which of its two deletion branches applies. |

### Ambiguous Guidance

| File | Step | Ambiguous phrase | Missing decision criterion |
|------|------|------------------|---------------------------|
| `.claude/skills/classify-and-plan/SKILL.md` | Step 4, parser heuristic | `rule_is_implicitly_enforced_by_fetch(rule)` / `rule_holds_by_shared_value_in_same_saga(rule)` | Every other branch of that pseudo-code block is mechanical (same-aggregate check, keyword lists). These two are undefined predicates, so the P4a and P4b branches of an otherwise unattended parse have no test at all. The block is labelled "mechanical only" and the doc's § Decision Guide is authoritative for semantics, which bounds the damage — but a one-line proxy per predicate (e.g. "the rule's predicate names a compound-key lookup the §4 description says the saga performs") would make the heuristic runnable rather than aspirational. |
| `.claude/skills/implement-aggregate-full/SKILL.md` | § Slice brief, `VERIFY` block | `-Dtest=<this slice's T4 test>,{Aggregate}ServiceTest` | The placeholder assumes a T4 test exists for the slice. Session `a` produces only `{Aggregate}IntraInvariantTest` (T1) and session `d` only `{Aggregate}InterInvariantTest` (T3) — neither has a T4 test, so the manager must silently reinterpret the placeholder for two of the four session types. State the per-session-type test class instead, or say "this slice's own test class(es)". |
| `docs/concepts/service.md` | § Mutate method with optional sub-collection parameter | "disproportionate when the update intent covers only scalar fields" | No threshold for *disproportionate*. The § When to use / § When not to use pair beneath it does carry a real criterion ("is the sub-collection update the primary intent?"), so the word is decorative — but it reads as if it were the test. |
| `.claude/skills/classify-and-plan/SKILL.md` | Step 7, session `2.N.d` file list | (omission, not a phrase) | The `d` row lists only the four new files plus the T3 test. Every `d` session must also modify `{Aggregate}Functionalities.java` (one `{operation}ByEvent` per event), `{Aggregate}Service.java` (one mutate helper per event) and `{Aggregate}.java` (`getEventSubscriptions()`) — all three named as shared files by `implement-aggregate-full/SKILL.md` § Shared files per session type. plan.md is explicitly a blueprint, so this is legal, but it guarantees a plan.md amendment on every `d` session of every run. Listing them as "(appended)" entries, as the `c` row already does for its shared files, would remove recurring friction. |
| `.claude/skills/implement-aggregate/SKILL.md` | Steps 5-8 table | rows ordered 5, 5b, 6, 7, 7.d | The harness-log rows step is numbered `7.d` and listed *after* step 7, while `session-completion.md` § "Retro assembly" opens with "Run § 'Harness-log rows' first". The parenthetical "(run before writing the retro file)" rescues it, but the row order still shows the reverse of the required order. |

---

## Check 4 — Neutral Domain

**Run in progress:** quizzes-full-2
**Base commit:** `0f22736c45d001f314442385ef4007d96ec8afee` (derived by `git merge-base HEAD master`; the
`harness:`-commit fallback was not needed, since HEAD is on branch `quizzes-full-2` and the merge-base
is a real ancestor)
**Forbidden nouns:** `Course`, `Execution`, `Question`, `Quiz`, `QuizAnswer`, `Topic`, `Tournament`, `User`
**Added lines scanned:** 1156 (over `docs/`, `.claude/skills/`, `.claude/agents/`, `AGENTS.md`, excluding `docs/reviews/`)

| File | Added line | Noun | Verdict |
|------|-----------|------|---------|
| — | — | — | nothing to report |

**Counts:** Violation 0, Moved 0, False positive 0. Zero raw hits over 1156 scanned added lines — the
harness delta of this run introduces no domain noun of the application under generation, not even one
that the move-test would have had to clear.

---

## Action Items

| Priority | Category | File | Finding | Suggested Fix |
|----------|---------|------|---------|---------------|
| Critical | — | — | nothing to report | — |
| Major | Naming contradiction | `.claude/skills/classify-and-plan/SKILL.md:512` | The session `2.N.b` file-list template reads ``` `{src}commands/{aggregate}/Get{Query}Command.java` (one per read op) ```, while the substitution rule at line 573 reads ``` `{Query}` → read operation name (PascalCase, e.g., "GetOpenShipments") ```. Substituting gives `GetGetOpenShipmentsCommand.java`. Every other use of the token is bare — `coordination/sagas/{Query}FunctionalitySagas.java` (l.512), `sagas/coordination/{aggregate}/{Query}Test.groovy` (l.512) — and `session-b.md:76` says "Name convention: `Get{Aggregate}By{Field}Command` or similar (**match plan.md file list exactly**)", so the session agent is instructed to reproduce the doubled prefix rather than correct it. | Change the Step 7 entry to `{src}commands/{aggregate}/{Query}Command.java`, matching `session-b.md` § "One `{Query}Command.java` per read functionality" and the bare-token form used for the saga and test files on the same line. |
| Major | Path contradiction | `AGENTS.md:75` | The Module Map row reads ``` | `applications/{app-name}/` | A generated application; its spec pair, `plan.md`, `retros/`, `reviews/` and `harness-log.md` live here | ```. No skill writes there: `review-artifacts/SKILL.md:57` sets `{report-file} = docs/reviews/review-{review-date}.md` and `harness-retrospective/SKILL.md:55` sets `docs/reviews/harness-retro-{app-name}-{retro-date}.md`; both run `mkdir -p docs/reviews`. `applications/quizzes-full-2/` contains `retros/`, `harness-log.md`, `plan.md`, the spec pair and `logs/` — and no `reviews/`. An agent asked to find or file a review is pointed at a directory that has never existed. | Drop `reviews/` from that row and add a sentence naming `docs/reviews/` as the home of both report kinds. Optionally add `logs/` to the row, which the directory does contain and the map omits. |
| Minor | Missing example | `docs/concepts/rule-enforcement-patterns.md` § Decision Guide Step 4 | Case 1 (P4a default) has no code block while case 2 does. | Inline the `// Fetching this DTO enforces the precondition implicitly …` comment shape from § P4a. |
| Minor | Missing example | `docs/architecture.md` § R8 | Prose only, unlike the adjacent R7 which carries a WRONG/RIGHT pair. | Add a two-block WRONG/RIGHT showing a downstream `commandGateway.send` versus the event-driven alternative. |
| Minor | Missing example | `docs/concepts/commands.md` § What a Command Is | The two `null`-`rootAggregateId` cases are prose against an example that passes a real id. | Add a create-command constructor showing `super(unitOfWork, serviceName, null)`. |
| Minor | Missing example | `docs/concepts/events.md` § Cascade Invalidation Pattern | Step 3 (the consumer side) is one prose sentence between two code steps. | Add the downstream subscription + ByEvent branch, or point at the specific § Canonical Wiring Snippet subsection that applies. |
| Minor | Ambiguous guidance | `.claude/skills/classify-and-plan/SKILL.md` Step 4 | `rule_is_implicitly_enforced_by_fetch` and `rule_holds_by_shared_value_in_same_saga` are undefined predicates inside an otherwise mechanical heuristic. | Give each a one-line proxy test, or state explicitly that the two P4 branches always require human confirmation. |
| Minor | Ambiguous guidance | `.claude/skills/implement-aggregate-full/SKILL.md` § Slice brief | The `VERIFY` placeholder `<this slice's T4 test>` has no referent for sessions `a` (T1 only) and `d` (T3 only). | Replace with the per-session-type test class, or "this slice's own test class(es) plus `{Aggregate}ServiceTest`". |
| Minor | Blueprint omission | `.claude/skills/classify-and-plan/SKILL.md` Step 7, `2.N.d` row | The row omits the three shared files every `d` session must modify, guaranteeing a plan.md amendment each time. | Add `{Aggregate}Functionalities.java` (ByEvent methods appended), `{Aggregate}Service.java` (mutate helpers appended) and `{Aggregate}.java` (`getEventSubscriptions()` updated) as "(appended)" entries, matching the style of the `2.N.c` row. |
| Minor | Ordering | `.claude/skills/implement-aggregate/SKILL.md` Steps 5-8 table | Row `7.d` (harness-log rows) is listed after row 7 (retro assembly), the reverse of the order `session-completion.md` requires. | Renumber the harness-log row to `6b` / list it before the retro row. |
| Minor | Decorative wording | `docs/concepts/service.md` § Mutate method with optional sub-collection parameter | "disproportionate" reads as the test but carries no threshold; the real criterion is in the § When to use / § When not to use pair below. | Delete the word, or fold it into the criterion beneath. |

**Critical** = causes an agent to produce incorrect output (wrong code, missing file, wrong beans,
  wrong exception type, broken skill delegation).
**Major** = leaves an agent to infer or guess something the harness should have stated.
**Minor** = cosmetic inconsistency, missing example, ambiguous wording with low impact.
