# Artifacts Review — 2026-08-02

**Date:** 2026-08-02
**Skill files reviewed:** 11
**Doc files reviewed:** 11 (8 files under `docs/reviews/` excluded as generated outputs)
**Verdict:** Issues requiring action

> **Clean** = all alignment checks pass; only improvement opportunities found.
> **Minor issues** = inconsistencies present but none cause incorrect agent output.
> **Issues requiring action** = any inconsistency that would cause an agent to produce
>   incorrect output.

---

## Executive Summary

Two harness files prescribe opposite things about the per-aggregate `SagaState` enum:
`session-a.md` forbids a `NOT_IN_SAGA` member, `docs/concepts/sagas.md` § SagaState shows one in its
worked enum — an agent that follows the doc emits a constant that breaks the `GenericSagaState.NOT_IN_SAGA`
equality assertion `testing.md` mandates. A second alignment defect: `architecture.md` R4 states that
*every* step mutating a pre-existing aggregate must declare `forbiddenStates`, which `sagas.md`
§ Lock-Acquisition Step Pattern contradicts for the primary aggregate (plain unwrapped command, no
forbidden states). Path validity is clean, and the neutral-domain check found zero violations —
all 10 noun hits are relocations from the session-letter swap. Fix the two contradictions before
the next run starts.

---

## Check 1 — Path Validity

Every literal `docs/...` and `.claude/skills/...` path mentioned in the review set was resolved
against disk (57 literal references, plus all relative markdown links). All resolve.

| Source file | Referenced path | Status | Notes |
|-------------|----------------|--------|-------|
| all skills (`boot-strap`, `classify-and-plan`, `implement-aggregate`, `harness-retrospective`, `review-artifacts`) | `.claude/skills/_shared/conventions.md` | Exists | 6 referring sites |
| `docs/workflow.md:112,127,184-187,146` | `.claude/skills/{classify-and-plan,boot-strap}/SKILL.md`, `session-a..d.md`, `_shared/conventions.md` | Exists | all 7 resolve |
| `.claude/skills/harness-retrospective/SKILL.md:273` | `.claude/skills/review-artifacts/SKILL.md` | Exists | runtime delegation target — would be Critical if broken |
| `.claude/skills/implement-aggregate/session-c.md:198` | `.claude/skills/implement-aggregate/SKILL.md` | Exists | — |
| `.claude/skills/review-artifacts/SKILL.md:114-143` | 9 doc paths | Exists | — |
| session-a/b/c/d `## Reads` lists | `docs/concepts/{aggregate,service,commands,sagas,events,testing}.md`, `docs/architecture.md` | Exists | — |
| `.claude/skills/boot-strap/SKILL.md:47-58` | 9 files under `templates/` | Exists | all 9 `.template` files present on disk |
| `docs/templates/aggregate-grouping-template.md:61` | `../../docs/concepts/events.md` | Exists | relative link resolves |
| `.claude/skills/harness-retrospective/SKILL.md:3,55`; `review-artifacts/SKILL.md:3,56` | `docs/reviews/…-{date}.md` | Template | output paths, correctly templated |
| `.claude/skills/classify-and-plan/SKILL.md:21` | `applications/my-app/my-app-domain-model.md` | Template-equivalent | `my-app` is the harness-wide neutral example name used in `conventions.md` § "Resolve app context"; not a leaked application name |

No literal application name is hard-coded anywhere in the review set. Every other
`applications/...` reference is written `applications/{app-name}/...`, and the directory shapes
match `docs/workflow.md` § Package layout with one exception recorded under Check 2 / Action Items.

---

## Check 2 — Pattern Alignment (P1–P4 and R1–R8)

### P1–P4 Consistency

Authoritative one-liners, `docs/concepts/rule-enforcement-patterns.md` § Quick Reference:
**P1** single-aggregate state rule → `verifyInvariants()`, strong. **P2** cache state from another
aggregate, no blocking → `getEventSubscriptions()` + handler chain, eventual. **P3** synchronous
service-level check (own-table uniqueness OR saga-assembled DTO validation) → guard in `*Service.java`,
strong. **P4a** precondition implicit in a saga fetch query, no code. **P4b** invariant holds because
the saga passes the same value to two aggregates, no code.

| Location | Pattern | Claim | Authoritative definition | Verdict |
|----------|---------|-------|--------------------------|---------|
| `architecture.md:228-234` selection table | P1/P3/P2/P4a/b | "Always true within one aggregate… P1"; "Synchronous service-level check (own-table uniqueness OR saga-assembled DTO field validation) — P3"; "Cross-aggregate; eventual consistency acceptable — P2"; "Precondition implicit in saga fetch / same value to two aggregates — P4a/P4b" | as above | Consistent |
| `architecture.md:113-144` Request Lifecycle | P1/P2/P3 | "[P3] service-layer guard … runs inside `@Transactional(SERIALIZABLE)`"; "[P1] `verifyInvariants()` on each changed aggregate"; "[P2] EventProcessing → Update Functionality, consumer aggregate caches publisher state" | as above | Consistent |
| `classify-and-plan/SKILL.md:211-261` Step 4 | P1–P4b | delegates: "Apply the flowchart in `rule-enforcement-patterns.md` § Decision Guide … That section is authoritative"; heuristic order P1 → P4a → P4b → P3 → P2 | flowchart order same-aggregate → P4a → P4b → P3 → P2 | Consistent |
| `session-a.md:56` | P1 | "`verifyInvariants()`: enforces all **P1 rules** for this aggregate listed in plan.md. Throws `{AppClass}Exception`" | P1 = `verifyInvariants()` in aggregate | Consistent |
| `session-c.md:56-57` | P3 | "**P3 own-table uniqueness guards** … query the repository for duplicates"; "**P3 DTO field checks** … receive the saga-assembled DTO as a parameter; validate the field" | both P3 sub-cases | Consistent |
| `session-d.md:126` | P2 | "**P2 rule enforcement:** The invariant check happens inside the Functionalities update method, which loads the aggregate, applies the cached-field change, and calls `verifyInvariants()`" | P2 caches state; no operation blocked on cached state | Consistent — the throw is the consumer's own P1 firing on its own commit, not a blocked caller operation |

### R1–R8 Coverage

| Restriction | Expected location | Mentioned? | Consistent? | Notes |
|-------------|------------------|------------|-------------|-------|
| R1 — service loads only own aggregate | `service.md`, session-b, session-c | `service.md:35` only | Yes | `service.md` § Injected Dependencies + § Method Patterns encode it; sessions reach it by reading `service.md`. Not restated in the sessions — acceptable delegation. |
| R2 — service injects only own components | `service.md`, session-b, session-c | `service.md:35` + § Injected Dependencies code example | Yes | Reinforced by the `AGENTS.md` architecture principle (inject interfaces, not `Sagas*`) at `service.md:37` |
| R3 — cross-aggregate flow via DTOs only | `service.md`, session-b, session-c | Not by code in any of the three | Partial | `service.md` § DTO Immutability covers R7 but never states R3's rule ("must never hold a reference to another aggregate's concrete class"). Only `architecture.md:176-181` carries it. Minor. |
| R4 — `setForbiddenStates` on mutating saga steps | `sagas.md`, session-c | `sagas.md:93-101` (R4 Decision Table), `session-c.md:107` | **No** | `architecture.md:184-190` R4 says *any* step mutating a pre-existing aggregate must declare `forbiddenStates`; `sagas.md` § Lock-Acquisition Step Pattern says the primary-aggregate mutate step is "a plain (unwrapped) command", with the lock taken by `setSemanticLock` on the preceding step. Direct contradiction — see Action Items. |
| R5 — `getEventSubscriptions()` in consumer only | `events.md`, session-d | Not in either | Partial | The rule text lives in `architecture.md:194-198` and `rule-enforcement-patterns.md:153` (§ P2 Upstream/Downstream). `session-d.md` reads `events.md` and `testing.md`, neither of which states the direction rule. |
| R6 — `verifyInvariants()` must not read from DB | `aggregate.md`, session-a | **Not in either** | **No** | `aggregate.md` § Base Class describes `verifyInvariants()` with no DB-read prohibition; `session-a.md` § `{Aggregate}.java` says it "enforces all P1 rules" and never forbids repository calls. `session-a`'s reads are `aggregate.md` + `testing.md` only — it never sees `architecture.md:202-206`. See Action Items. |
| R7 — DTOs are immutable value objects | `service.md`, session-b, session-c | `service.md:202-204` (§ DTO Immutability (R7)), `session-c.md:17` | Yes | — |
| R8 — functionalities only send commands upstream | `commands.md`, session-b, session-c | `sagas.md:370`, `session-c.md:107` | Yes, but misplaced | `commands.md` — the doc the check expects and the one both sessions read for command construction — never mentions R8. It is stated in `sagas.md` § Step Ordering, which `session-c` does read. Minor. |

---

## Check 3 — Improvement Opportunities

### Missing Examples

| Doc | Section | Why an example would help |
|-----|---------|---------------------------|
| `docs/concepts/aggregate.md` | § Repositories | The three repository artifacts are described in a table and prose only. `{Aggregate}CustomRepositorySagas` is produced for **every** aggregate in every session `a`; the class shape (`@Service @Profile("sagas")` + `@Autowired {Aggregate}Repository`) is currently reconstructed from two sentences. A 6-line code block removes the inference. |
| `docs/concepts/aggregate.md` | § Repositories, `AggregateRepository` paragraph | States JPQL "is written against the concrete aggregate class (`select a from {Aggregate} a where ...`)" but the only real JPQL example in the harness is in `service.md` § Custom Repository — Latest-Active-Version Query, with no pointer from here. |
| `docs/concepts/events.md` | § Publishing Events | Two lines of prose plus a one-line call. The anchor-choice rule ("for a child entity event, this is the parent aggregate's ID") is the single most consequential decision in this doc and has no publisher-side example showing the wrong and right anchor side by side. |
| `docs/concepts/service.md` | § Injected Dependencies | The `UnitOfWorkService` raw-type block is 15 lines of rationale with the rejected alternatives spelled out, but no example of the failure mode (what a `UnitOfWorkService<UnitOfWork>` declaration does). Low impact; the rule itself is unambiguous. |

### Ambiguous Guidance

| File | Step | Ambiguous phrase | Missing decision criterion |
|------|------|------------------|---------------------------|
| `.claude/skills/implement-aggregate/session-b.md:159` | § Update BeanConfigurationSagas | `{Aggregate}Service {aggregate}Service(...) { // inject repos and other services as needed` | Which collaborators the constructor takes. `service.md` § Injected Dependencies fixes the exact list (own repository, own custom repository, own factory, `UnitOfWorkService`, `AggregateIdGeneratorService`); the bean template should name it rather than say "as needed". |
| `.claude/skills/implement-aggregate/session-a.md:162` | § `{Aggregate}CustomRepositorySagas.java` | "Add custom JPQL query method implementations **only as needed**" | Session `a` has no service methods yet, so "needed" is unknowable at that point. The honest instruction is: leave it empty in session `a`; session `b`/`c` add methods when a service method requires one. |
| `.claude/skills/implement-aggregate/session-d.md:132` | § ByEvent methods | "Add the corresponding service helper **if needed**" | No criterion for when a helper is needed vs. reusing an existing mutate method. `events.md` § ByEvent sagaState guard implies a dedicated method; say so. |
| `.claude/skills/implement-aggregate/session-c.md:135-148`, `session-d.md:171-177`, `session-a.md:190` | T2/T3/T1 test bullets | (not ambiguity — duplication) | `testing.md:8-13` declares itself the single source of truth and says "If you find a skill file paraphrasing a rule instead of pointing here, that's drift — fix it by deleting the copy, not by editing both." These three sites paraphrase the fresh-UnitOfWork rule, the kill-mutation check, the boundary on/off-point rule and the capture-before-firing rule instead of pointing. Drift by the harness's own definition. |
| `docs/concepts/events.md`, `session-d.md:146-163`, `session-b.md:30`, `session-c.md:176` | worked examples | domain nouns from the completed run (`Course`, `Execution`, `Question`, `Quiz`, `Tournament`, `User`) | These predate the neutral-domain rule (Check 4 confirms none were newly introduced), but `conventions.md` § "Neutral domain" would forbid every one of them if written today. `session-d.md` § "UpdateQuestionEvent for consumers that cache only `questionVersion`" is an entire subsection keyed to one application's aggregate. Rewriting them in `{Aggregate}`/`{Event}` placeholders is the largest remaining neutrality debt in the harness. |

---

## Check 4 — Neutral Domain

**Run in progress:** `quizzes-full` (36/36 checkboxes ticked — the run is complete, but `plan.md`
exists, so the check runs)
**Base commit:** `ad0f3256` — derived by the fallback rule, not `merge-base`. `git merge-base HEAD master`
returned `HEAD` itself (`f306e46c`), since all harness work sits on master; the fallback took the
parent of the oldest `harness:` commit touching `docs`/`.claude/skills` (`908d1218^`). Ten `harness:`
commits fall in that range.
**Added lines scanned:** 1500 (`git diff ad0f3256 -- docs .claude/skills -M`, `+` lines, excluding
`+++` headers and the generated `docs/reviews/` outputs)

Forbidden nouns (from `applications/quizzes-full/plan.md` `### {N}. {Aggregate}` headers): Course,
Execution, Question, Quiz, QuizAnswer, Topic, Tournament, User. Matched whole-word, then re-scanned
case-insensitively with plural/lowercase variants to catch what `-w` misses.

| File | Added line | Noun | Verdict |
|------|-----------|------|---------|
| `session-b.md` | `7. ***(Conditional)*** If any read functionality joins data from an upstream aggregate (e.g., a "get with details" that includes Course name alongside Execution): …` | Course, Execution | Moved — verbatim at base in `session-c.md:30` |
| `session-b.md` | `> - **One step** — when every filter criterion is stored directly on the aggregate (e.g., \`executionAggregateId\` is a field on \`Tournament\`). …` | Tournament, Execution | Moved — verbatim at base in `session-c.md:78` |
| `session-b.md` | `> - **Two steps** — when the filter parameter is a foreign aggregate's ID … (e.g., \`executionId → courseAggregateId\`)` | Course, Execution | Moved — at base in `session-c.md` |
| `session-b.md` | `> **Two-step read saga:** If the read's filter parameter is a foreign aggregate's ID …` | Course, Execution | Moved — at base in `session-c.md` |
| `session-b.md` | `- **List-return reads**: If the read returns a collection (e.g., all open tournaments for an execution) …` | Tournament, Execution | Moved — at base in `session-c.md` |
| `session-c.md` | `// Spec: plan.md §3.5 Question / functionalities — UpdateQuestionContent; rule QUESTION_CONTENT_REQUIRED` | Question | Moved — verbatim at base in `session-b.md:181` |
| `session-c.md` | `def "updateQuestionContent: QUESTION_CONTENT_REQUIRED violation"() {` | Question | Moved — at base in `session-b.md` |
| `session-c.md` | `- Name convention: operation in PascalCase + aggregate name + \`Command\` (e.g., \`CreateTournamentCommand\`)` | Tournament | Moved — at base in `session-b.md` |
| `session-c.md` | `- **P4a prerequisite tests**: test what happens when the upstream fetch fails (e.g., creator not enrolled in execution)` | Execution | Moved — at base in `session-b.md` |
| `docs/concepts/commands.md` | `        return "Tournament";` | Tournament | Moved — the noun is present on the same line at base (`commands.md:125`, `return "Tournament";   // must match ServiceMapping value`); the edit replaced the trailing comment, not the noun |

**Counts: 0 Violation / 10 Moved / 0 False positive.** A clean run. Every hit is a consequence of the
session-letter swap (`session-b.md` ↔ `session-c.md` content exchange) recorded in commit
`2fc456d6`, plus one comment rewrite in `commands.md`. No domain noun of `quizzes-full` was newly
introduced into the harness by this run's ten `harness:` commits.

---

## Action Items

| Priority | Category | File | Finding | Suggested Fix |
|----------|---------|------|---------|---------------|
| Critical | Contradiction | `docs/concepts/sagas.md:22-33` vs `.claude/skills/implement-aggregate/session-a.md:122` | `sagas.md` § SagaState shows the worked enum as `public enum CourseExecutionSagaState implements SagaAggregate.SagaState { NOT_IN_SAGA("NOT_IN_SAGA"), IN_ADD_PARTICIPANT(…), … }`. `session-a.md` says the opposite, verbatim: "**Do not** include `NOT_IN_SAGA` — the initial state is set to `GenericSagaState.NOT_IN_SAGA` (from the framework) in the `Saga{Aggregate}` constructor. This enum only holds operation-specific locked states." An agent that follows the doc emits a second `NOT_IN_SAGA` constant that is not identity-equal to `GenericSagaState.NOT_IN_SAGA`, breaking every `sagaStateOf(id) == GenericSagaState.NOT_IN_SAGA` assertion `testing.md` § T4 and § Compensation Test require. | Delete the `NOT_IN_SAGA("NOT_IN_SAGA"),` line from the `sagas.md` § SagaState example and add one line under it: "The quiescent state is `GenericSagaState.NOT_IN_SAGA` from the framework — never redeclare it in the per-aggregate enum." `session-a.md` is already correct; do not touch it. |
| Major | Contradiction | `docs/architecture.md:184-190` (R4) vs `docs/concepts/sagas.md:36-61,93-101,360-368` | R4's headline and body say **any** step mutating a pre-existing aggregate "must list the `SagaState` values of concurrent operations that would conflict". `sagas.md` § Lock-Acquisition Step Pattern step 2 and § Step Ordering step 4 both prescribe the opposite for the primary aggregate: "send a plain (unwrapped) command that performs the mutation", with the lock taken by `setSemanticLock` on the preceding read step; § R4 Decision Table reserves `setForbiddenStates` for **foreign** aggregates. R4's only carve-out is for newly-created aggregates. | Restate R4 as: "a saga step that touches a pre-existing aggregate must declare its lock intent — `SagaCommand` + `setSemanticLock` when the aggregate is the saga's primary, `setForbiddenStates` when it is foreign. A step that creates the aggregate declares neither. See `concepts/sagas.md` § R4 Decision Table for which applies." |
| Major | Rule not reachable | `docs/concepts/aggregate.md` § Base Class; `.claude/skills/implement-aggregate/session-a.md` § `{Aggregate}.java` | R6 ("`verifyInvariants()` must not perform DB reads") appears only in `architecture.md:202-206`. `session-a.md` — the only session that writes `verifyInvariants()` — reads `aggregate.md` and `testing.md`, neither of which states it. An agent implementing a cross-entity P1 rule over cached snapshot fields has nothing telling it not to reach for the repository. | Add one line to `aggregate.md` § Base Class under `verifyInvariants()`: "Must not perform repository reads — it runs inside the UoW commit path (R6, `architecture.md`). Intra-invariants check only fields on the aggregate instance; anything requiring a DB read is a P3 service guard." |
| Major | Layout inconsistency | `docs/architecture.md:82-90` vs `docs/workflow.md:29-48` and `.claude/skills/implement-aggregate/session-d.md` | `architecture.md` § Package Structure lists `notification/api/{Xxx}EventController.java` as part of the canonical per-service layout. No session produces it, `classify-and-plan/SKILL.md` Step 7 never lists it in a 2.N.d file row, and `workflow.md` § Package layout omits the `api/` directory entirely. `workflow.md` also omits `coordination/webapi/`, which `session-c.md` does produce. `session-b.md:15` sends agents to `architecture.md` "§ package layout for this aggregate", so the discrepancy is reachable. | Delete `notification/api/{Xxx}EventController.java` from `architecture.md` § Package Structure (no skill emits it), and add `coordination/webapi/` and `{Aggregate}ServiceApplication.java` to the `workflow.md` § Package layout tree so the two trees agree. |
| Major | Self-declared drift | `.claude/skills/implement-aggregate/session-c.md:135-148`, `session-d.md:171-177`, `session-a.md:190` | `testing.md:8-13` declares itself the sole owner of tier definitions, required scenarios, assertion-ownership rules and templates, and names paraphrase-instead-of-pointer as drift to be fixed "by deleting the copy, not by editing both". These sites paraphrase the fresh-UnitOfWork rule, the kill-mutation check, the on-point/off-point boundary rule and the capture-`originalValue`-before-firing rule. | Replace each paraphrase with a section pointer (`testing.md § T2 — Service Test`, `§ Choosing Input Values`, `§ Fake / Wrong / Weak Detection Checklist`), keeping only the session-specific instruction (which file, which tier, appended vs. created). |
| Minor | Coverage | `docs/concepts/events.md`, `.claude/skills/implement-aggregate/session-d.md` | R5 (subscriptions live in the consumer only) is stated in `architecture.md` and `rule-enforcement-patterns.md` § P2, but not in `events.md` or `session-d.md` — the doc and skill that actually wire subscriptions. | Add one sentence to `events.md` § EventSubscription: "Subscriptions live in the downstream (consumer) aggregate only; a publisher never subscribes to its own events (R5)." |
| Minor | Coverage | `docs/concepts/commands.md` | R8 (commands go upstream only) is absent from `commands.md`, the doc both session-b and session-c read for command construction; it is stated only in `sagas.md` § Step Ordering. | Add a one-line note under `commands.md` § Sending Commands pointing at R8 and `sagas.md` § Step Ordering. |
| Minor | Coverage | `docs/concepts/service.md` | R3 (never hold a foreign aggregate's concrete class; cross-aggregate state flows as DTOs) is never stated in `service.md`; only `architecture.md:176-181` carries it. | Add it to `service.md` § Injected Dependencies alongside the existing R1/R2 pointer. |
| Minor | Ambiguity | `.claude/skills/implement-aggregate/session-b.md:159` | Bean template says "inject repos and other services **as needed**". | Name the closed list from `service.md` § Injected Dependencies. |
| Minor | Ambiguity | `.claude/skills/implement-aggregate/session-a.md:162` | "Add custom JPQL query method implementations **only as needed**" in a session with no service methods yet. | Say: leave empty in session `a`; methods are added by `b`/`c` when a service method needs one. |
| Minor | Ambiguity | `.claude/skills/implement-aggregate/session-d.md:132` | "Add the corresponding service helper **if needed**." | State the criterion: a dedicated helper per ByEvent method, per `events.md` § ByEvent sagaState guard. |
| Minor | Missing example | `docs/concepts/aggregate.md` § Repositories | No code block for `{Aggregate}CustomRepositorySagas`, produced in every session `a`. | Add a 6-line example; cross-link the JPQL pattern in `service.md` § Custom Repository. |
| Minor | Missing example | `docs/concepts/events.md` § Publishing Events | Anchor-ID choice has no publisher-side worked example. | Add a child-entity event example contrasting the wrong anchor (child id) with the right one (parent aggregate id). |
| Minor | Neutrality debt | `docs/concepts/events.md`, `docs/concepts/sagas.md`, `docs/concepts/commands.md`, `session-b.md`, `session-c.md`, `session-d.md` | Pre-existing worked examples name `Course`, `Execution`, `Question`, `Quiz`, `Tournament`, `User` — the exact aggregate set of the completed `quizzes-full` run. Check 4 confirms none was introduced by this run, but `conventions.md` § "Neutral domain" would forbid all of them today. `session-d.md` § "UpdateQuestionEvent for consumers that cache only `questionVersion`" is a whole subsection keyed to one application's aggregate. | Rewrite in placeholder vocabulary (`{Aggregate}`, `{Event}`, `{Entity}`), or in an invented neutral domain, before the harness is pointed at the next application. |
