# Generated compensated-update read qualification

Approved direct follow-up: discover or create the missing path from the existing
controlled update/read witness to a generated package and ordinary ScenarioExecutor.
Work locally on the current branch, preserving inherited detector and documentation changes.

Use existing scheduling, prerequisite binding, fault generation and package APIs. Qualify
all six placements of FindTournament among UpdateTournament's five ordered steps, with
no-fault controls and failures immediately before updateQuizStep. Reuse the existing
experiment prerequisite provider through normal application operations. Do not claim
its concrete inputs were automatically extracted, or change application behavior,
anomaly rules, impact weights, GA objectives or the global scheduling default.

Bounded discovered runtime fix: declared generic prerequisite types such as Set<Integer>
passed descriptor validation but failed Java class lookup, first in provider validation
and then in input materialization. Share the materializer's runtime class resolution
and erase generic arguments at this boundary. Preserve the full declared type in package
and report evidence. This is raw runtime assignability, not recursive element validation.

Proof: an ordinary current package, generated intra-Saga-order-preserving schedules,
all no-fault controls executed before selected fault variants, exact fault/action/recovery
conformance, current production diagnostic sidecars, generic positive/wrong-container
executor tests, and preserved build/source/package hashes. Report failures and remaining
input limitations; do not treat failed preparation as zero impact.
