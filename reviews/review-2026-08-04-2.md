# Artifacts Review — 2026-08-04-2

**Date:** 2026-08-04
**Skill files reviewed:** 13
**Doc files reviewed:** 11
**Verdict:** Minor issues

> **Clean** = all alignment checks pass; only improvement opportunities found.
> **Minor issues** = inconsistencies present but none cause incorrect agent output.
> **Issues requiring action** = any inconsistency that would cause an agent to produce
>   incorrect output.

---

## Executive Summary

Path validity is clean: every literal `docs/...`, `.claude/skills/...` and `.claude/agents/...`
reference in the review set resolves to a real file, and no literal application name leaked into
the harness proper (the one hit was inside a generated `docs/reviews/` report, which is out of
scope by the skill's own rule). P1–P4 definitions are consistent everywhere they are claimed. The
only real gaps are in R1–R8 coverage: `session-a.md` never cites R6 by name even though it is the
session that writes `verifyInvariants()`, and `session-b.md`/`session-c.md` reference R1–R3/R7/R8
inconsistently across the two files. None of these rise to "causes incorrect output" because the
governing concept docs are still in each session's required reading list — they are Minor,
not Critical.

---

## Check 1 — Path Validity

| Source file | Referenced path | Status | Notes |
|-------------|----------------|--------|-------|
| `docs/architecture.md` | `concepts/aggregate.md`, `concepts/sagas.md`, `concepts/service.md`, `concepts/commands.md`, `concepts/rule-enforcement-patterns.md`, `concepts/events.md` | Yes | All resolve under `docs/concepts/` |
| `docs/concepts/aggregate.md` | `../architecture.md`, `rule-enforcement-patterns.md`, `events.md` | Yes | — |
| `docs/concepts/commands.md` | `sagas.md` | Yes | — |
| `docs/concepts/events.md` | `../architecture.md` | Yes | — |
| `docs/concepts/rule-enforcement-patterns.md` | `.claude/skills/implement-aggregate/session-a.md` | Yes | — |
| `docs/concepts/sagas.md` | `testing.md` | Yes | — |
| `docs/concepts/service.md` | `../architecture.md`, `rule-enforcement-patterns.md` | Yes | — |
| `docs/concepts/testing.md` | `docs/concepts/sagas.md` | Yes | — |
| `docs/templates/aggregate-grouping-template.md` | `../../docs/concepts/events.md` | Yes | — |
| `docs/workflow.md` | `concepts/*.md` (5), `docs/concepts/testing.md`, `docs/concepts/rule-enforcement-patterns.md`, `.claude/skills/classify-and-plan/SKILL.md`, `.claude/skills/boot-strap/SKILL.md`, `.claude/skills/boot-strap/templates/`, `.claude/skills/implement-aggregate/session-{a,b,c,d}.md`, `.claude/skills/_shared/session-completion.md`, `.claude/agents/aggregate-slice.md` | Yes | — |
| `.claude/skills/_shared/conventions.md` | `.claude/agents/aggregate-slice.md` | Yes | Real file on disk (outside the two enumerated trees, checked per Hard Rule "or as a real file on disk") |
| `.claude/skills/_shared/session-completion.md` | `docs/concepts/aggregate.md`, `.claude/skills/implement-aggregate/SKILL.md`, `.claude/skills/implement-aggregate-full/SKILL.md`, `.claude/skills/_shared/conventions.md` | Yes | — |
| `.claude/skills/boot-strap/SKILL.md` | `.claude/skills/_shared/conventions.md`, `templates/README.md` | Yes | — |
| `.claude/skills/boot-strap/templates/README.md` | `.claude/skills/_shared/conventions.md` | Yes | — |
| `.claude/skills/classify-and-plan/SKILL.md` | `.claude/skills/_shared/conventions.md`, `docs/concepts/rule-enforcement-patterns.md`, `docs/concepts/sagas.md`, `docs/workflow.md` | Yes | — |
| `.claude/skills/harness-retrospective/SKILL.md` | `.claude/skills/_shared/conventions.md`, `.claude/skills/review-artifacts/SKILL.md` | Yes | — |
| `.claude/skills/implement-aggregate-full/SKILL.md` | `.claude/skills/_shared/conventions.md`, `.claude/skills/_shared/session-completion.md`, `.claude/skills/classify-and-plan/SKILL.md`, `docs/workflow.md`, `.claude/skills/implement-aggregate/session-{type}.md` (template), `.claude/agents/aggregate-slice.md` | Yes | — |
| `.claude/skills/implement-aggregate/session-a.md` | `docs/concepts/aggregate.md`, `docs/concepts/testing.md`, `docs/concepts/service.md` | Yes | — |
| `.claude/skills/implement-aggregate/session-b.md` | `docs/architecture.md`, `docs/concepts/service.md`, `docs/concepts/commands.md`, `docs/concepts/sagas.md`, `docs/concepts/testing.md` | Yes | — |
| `.claude/skills/implement-aggregate/session-c.md` | `docs/concepts/service.md`, `docs/concepts/commands.md`, `docs/concepts/sagas.md`, `docs/concepts/testing.md`, `.claude/skills/_shared/session-completion.md` | Yes | — |
| `.claude/skills/implement-aggregate/session-d.md` | `docs/concepts/events.md`, `docs/concepts/testing.md`, `docs/concepts/aggregate.md` | Yes | — |
| `.claude/skills/implement-aggregate/SKILL.md` | `.claude/skills/_shared/conventions.md`, `docs/concepts/testing.md`, `.claude/skills/_shared/session-completion.md`, `.claude/skills/implement-aggregate-full/SKILL.md`, `.claude/skills/classify-and-plan/SKILL.md` | Yes | — |
| `.claude/skills/review-artifacts/SKILL.md` | `.claude/skills/_shared/conventions.md`, `docs/concepts/rule-enforcement-patterns.md`, `docs/architecture.md`, `docs/concepts/service.md`, `docs/concepts/sagas.md`, `docs/concepts/events.md`, `docs/concepts/aggregate.md`, `docs/concepts/commands.md`, `docs/concepts/testing.md`, `docs/workflow.md`, `.claude/skills/classify-and-plan/SKILL.md`, `.claude/skills/implement-aggregate/session-{a,c,d}.md` | Yes | — |

Literal application names: none found in the review set. The only `quizzes`/`quizzes-full` hits are
inside `reviews/agnosticism-review-2026-07-19.md`, which is a generated-output file excluded
from the input artifact enumeration by the skill's own "Generated outputs excluded" rule.

`applications/{app-name}/...` template patterns throughout are correctly written with the
placeholder, not a literal directory name.

---

## Check 2 — Pattern Alignment (P1–P4 and R1–R8)

### P1–P4 Consistency

| Location | Pattern | Claim | Authoritative definition | Verdict |
|----------|---------|-------|--------------------------|---------|
| `docs/architecture.md` — pattern selection table | P1/P2/P3/P4 | "Always true within one aggregate...", "Synchronous service-level check (own-table uniqueness OR saga-assembled DTO field validation)...", "Cross-aggregate; eventual consistency...", "Precondition implicit in saga fetch / same value..." | `rule-enforcement-patterns.md` § Quick Reference — same wording, including the P3 phrase verbatim | Consistent |
| `docs/architecture.md` — Request Lifecycle | P1/P2/P3 | `[P3]` own-table/DTO guard before mutation, `[P1]` verifyInvariants at commit, `[P2]` EventProcessing caches publisher state | `rule-enforcement-patterns.md` § Enforcement Lifecycle — identical ordering and content | Consistent |
| `.claude/skills/classify-and-plan/SKILL.md` — Step 4 | P1–P4a/b | Delegates to `rule-enforcement-patterns.md` § Decision Guide "in the order that doc specifies"; does not restate one-liners | N/A — pointer, not a restatement | Consistent (by design; no drift surface) |
| `.claude/skills/implement-aggregate/session-a.md` | P1 | "enforces all P1 rules... Throws `{AppClass}Exception`... on violation" | Matches P1 recipe (predicate methods + throw in `verifyInvariants()`) | Consistent |
| `.claude/skills/implement-aggregate/session-c.md` | P3 | "P3 own-table uniqueness guards... P3 DTO field checks... validate the field; throw... on violation" | Matches P3's two sub-cases (own-table uniqueness / DTO field validation) | Consistent |
| `.claude/skills/implement-aggregate/session-d.md` | P2 | "invariant check happens inside the Functionalities update method... calls verifyInvariants() before committing" — caches only, never blocks | Matches P2 ("may only cache state... no operation blocked") | Consistent |

### R1–R8 Coverage

| Restriction | Expected location | Mentioned? | Consistent? | Notes |
|-------------|------------------|------------|-------------|-------|
| R1 — service loads only own aggregate | `docs/concepts/service.md`, session-b.md, session-c.md | service.md yes (pointer to architecture.md); session-b.md yes (line 169, § Update BeanConfigurationSagas); session-c.md **no** | N/A | session-c.md's constructor-widening guidance never cites R1, though its method-pattern text follows it implicitly |
| R2 — service injects only own components | same | service.md yes; session-b.md yes; session-c.md **no** | N/A | Same gap as R1 — session-c.md's "BeanConfigurationSagas — No New Bean" section is the natural place and doesn't cite it |
| R3 — cross-aggregate flow via DTOs only | same | service.md yes; session-b.md yes; session-c.md **no** | N/A | session-c.md § DTO Immutability (R7) cites R7 by name in the same reads list but not R3 |
| R4 — saga steps declare lock intent | `docs/concepts/sagas.md`, session-c.md | Both yes | Consistent | sagas.md § R4 Decision Table and session-c.md § Reads item 5 both cite R4 explicitly |
| R5 — getEventSubscriptions in consumer only | `docs/concepts/events.md`, session-d.md | events.md yes; session-d.md **no** | N/A | session-d.md's entire content structurally enforces R5 (consumer-only subscription classes) but never cites "R5" by name; `docs/concepts/aggregate.md` (session-a.md's required reading) does cite it |
| R6 — verifyInvariants must not read from DB | `docs/concepts/aggregate.md`, session-a.md | aggregate.md yes (line 26); session-a.md **no** | N/A | session-a.md's own `verifyInvariants()` production instructions never restate the no-DB-read constraint; mitigated because session-a.md's Reads list includes "docs/concepts/aggregate.md — all sections", which covers R6 |
| R7 — DTOs are immutable value objects | `docs/concepts/service.md`, session-b.md, session-c.md | service.md yes (line 215); session-b.md **no**; session-c.md yes (line 21, cited explicitly in Reads) | Consistent where present | session-b.md is read-only (no DTO mutation risk), so the omission is lower-impact than R1–R3's |
| R8 — functionalities only send commands upstream | `docs/concepts/commands.md`, session-b.md, session-c.md | commands.md yes; session-b.md **no**; session-c.md yes (lines 35, 119) | Consistent where present | session-b.md's read sagas are single-direction fetches, so the risk surface is smaller, but the two-step foreign-fetch variant could still benefit from the pointer |

No finding here rises to Critical: every session that omits an explicit R-citation still has the
governing concept doc in its required-reading list, so the constraint is reachable, just not
pointed to by name at the point of highest relevance. Recorded as Major in Action Items below
for R1/R2/R3 (session-c.md, where constructor/injection code is actually written) and Minor for
R5/R6/R7/R8 gaps.

---

## Check 3 — Improvement Opportunities

### Missing Examples

Nothing to report. Every concept doc pairs its algorithm/decision descriptions with a worked code
example (P1–P4 recipes in `rule-enforcement-patterns.md`, the canonical wiring snippet in
`events.md`, the T1–T4 templates in `testing.md`, the saga shapes in `sagas.md`).

### Ambiguous Guidance

Nothing to report. A scan of all skill files for "depends on" / "may be" / "if applicable" /
"as appropriate" / "as needed" outside a concrete decision criterion found no hits: every
occurrence either uses "may" in its ordinary possibility sense (not a hedge on the harness's own
instructions) or is immediately followed by the deciding rule (e.g. classify-and-plan's snapshot
class rule immediately states "iff it subscribes to events").

---

## Check 4 — Neutral Domain

**Run in progress:** quizzes-full-2
**Base commit:** `0f22736c45d001f314442385ef4007d96ec8afee` (`git merge-base HEAD master`) — the
primary rule; it did not degenerate to HEAD, so the `harness:`-commit fallback was not needed.
**Added lines scanned:** 419 (whole-run harness delta over `docs/` and `.claude/skills`, excluding
`docs/reviews/`)

| File | Added line | Noun | Verdict |
|------|-----------|------|---------|
| `reviews/review-2026-08-04.md` | `**Forbidden nouns:** Course, Execution, Question, Quiz, QuizAnswer, Topic, Tournament, User` | Course, Execution, Question, Quiz, QuizAnswer, Topic, Tournament, User | False positive — the line is inside a prior `/review-artifacts` report's own Check 4 output, not a harness instruction; `docs/reviews/` is excluded from the neutral-domain scope by the skill's own "Generated outputs excluded" rule |

Violations: 0. Moved: 0. False positive: 1 (the report-listing-its-own-nouns case above). With
`docs/reviews/` excluded from the scan entirely, the harness delta (419 lines across
`_shared/conventions.md`, `boot-strap/templates/AppSpockTest.groovy.template`,
`classify-and-plan/SKILL.md`, `session-a.md`, `session-b.md`, `session-c.md`, `architecture.md`,
`commands.md`, `rule-enforcement-patterns.md`, `service.md`, `testing.md`, `workflow.md`) contains
zero matches against the run's forbidden nouns (Course, Execution, Question, Quiz, QuizAnswer,
Topic, Tournament, User). Clean run.

---

## Action Items

| Priority | Category | File | Finding | Suggested Fix |
|----------|---------|------|---------|---------------|
| Major | R1-R8 coverage | `.claude/skills/implement-aggregate/session-c.md` | R1/R2/R3 are never cited by name, though this session is where the service constructor is widened and where a foreign injection/reference would first appear in application code | In § "BeanConfigurationSagas — No New Bean, But Check the Existing Ones" and the write-method section of § "Produce", add a one-line pointer: "see `docs/concepts/service.md` § Injected Dependencies (R1/R2/R3)" |
| Minor | R1-R8 coverage | `.claude/skills/implement-aggregate/session-a.md` | R6 (verifyInvariants must not read from DB) is never cited by name in the `verifyInvariants()` production instructions | Add "(R6 — see `docs/concepts/aggregate.md`)" to the `verifyInvariants()` bullet in § Produce → `{Aggregate}.java` |
| Minor | R1-R8 coverage | `.claude/skills/implement-aggregate/session-d.md` | R5 (getEventSubscriptions in consumer only) is never cited by name despite the whole session existing to enforce it | Add "(R5)" next to the `{Aggregate}Subscribes{Event}.java` production instructions |
| Minor | R1-R8 coverage | `.claude/skills/implement-aggregate/session-b.md` | R7/R8 not cited; lower impact since session-b is read-only, but the two-step foreign-fetch read variant is exactly where R8's upstream-only rule matters | Add a one-line pointer to R7/R8 near § "Two-step read saga variant" |

**Critical** = causes an agent to produce incorrect output (wrong code, missing file, wrong beans,
  wrong exception type, broken skill delegation).
**Major** = leaves an agent to infer or guess something the harness should have stated.
**Minor** = cosmetic inconsistency, missing example, ambiguous wording with low impact.
