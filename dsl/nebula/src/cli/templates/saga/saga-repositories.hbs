package {{packageName}};

{{#each imports}}
{{this}}
{{/each}}

@Repository
public interface {{aggregateName}}CustomRepositorySagas extends JpaRepository<Saga{{aggregateName}}, Integer> {
    // Saga-specific repository methods can be added here
    }