# Checkpoint B handoff

- **State:** complete; paused before Checkpoint C.
- **Outcome:** latest WorkloadPlan/manifest v5 packages can carry one optional validated `SetupPlan`. The ordinary unmodified Quizzes recovery-window setup is flattened into exactly 12 source-ordered, Java-confirmed facade actions, including two activations and two enrollments. Backward action-result/property references feed later setup and both participants. Normal deterministic generation can emit the RemoveTournament–AddParticipant serial workload with no prerequisite baseline and eager `00100` immediate Remove recovery.
- **Actual production files:** `ScenarioGeneratorApplication.java`; `WorkflowFunctionalityCreationSite.java`; `WorkflowFunctionalityCreationSiteVisitor.java`; `GroovyConstructorInputTraceVisitor.java`; `GroovyValueRecipe.java`; new `GroovyFacadeSetupActionTrace.java`; setup model records under `scenario/model/`; new `SetupPlanMapper.java` and `SetupPlanValidator.java`; `ApplicationAnalysisScenarioModelAdapter.java`; `ScenarioModelAdapterResult.java`; `WorkloadPlan.java`; `ScenarioCatalogManifest.java`; `ScenarioGenerator.java`; `ScenarioIdGenerator.java`; `WorkloadPlanValidator.java`; `EagerFaultScenarioGenerator.java`; `ScenarioExecutorReadinessEvaluator.java`; `ScenarioCatalogJsonlWriter.java`; and `ScenarioCatalogPackageReader.java`. Checkpoint A analysis files remain part of the combined dirty diff.
- **Actual test files:** new `SourceDerivedSharedSagaWorkloadAnalysisSpec.groovy`; updated `ScenarioModelSpec.groovy`, `ScenarioCatalogJsonlWriterSpec.groovy`, `ScenarioGeneratorApplicationSpec.groovy`, `DummyappEventConsequencePackageSpec.groovy`, `ScenarioExecutorSpec.groovy`, and `DummyappAccountingFixtureFoundationSpec.groovy`, plus the accepted Checkpoint A visitor/state specs.
- **Partial-work review:** replaced the broad `InputRecipeNode` conversion/rewrite in setup mapping with direct mapping from the narrow Groovy value forms; restored `InputRecipeMapper` to its prior scope; tightened setup validation so inactive fields cannot smuggle runtime-shaped values; changed package checksum calculation from `Files.readAllBytes` to bounded streaming after a 50,000-workload acceptance package exceeded the Java array-size limit; removed only five stale compiled test classes left by killed partial sessions. No ScenarioExecutor setup execution, dispatch, orchestration, harmful/control run, scheduler, tuple ordering, reservation, or cap semantics were added.
- **Discoveries/classification:** direct narrow mapping, strict value-shape validation, streaming checksum calculation, and two affected regression expectation updates were autonomous B details. The successful whole-Quizzes acceptance package is large (about 4.6 GB) because setup semantics are repeated in workload records; output compaction remains later roadmap work and was not absorbed. No material scope delta was found.
- **Validation:** initial `cd verifiers && mvn -q -DskipTests test` passed after correcting the module directory. Focused extraction/model/package/readiness suites passed. Final `cd verifiers && mvn -q test` passed 649 tests with zero failures/errors/skips. `git diff --check` passed.
- **Normal generation:** Docker Compose package `verifiers/target/quizzes-20260828-141451-109/`; all prerequisite descriptors were mounted as an empty list, so the Remove/Add descriptor/provider path was absent. Explicit caps/config: `INTERACTION_PRUNED`, singles false, Saga-set size 2, catalog 50,000, input variants per Saga 10, schedules per tuple 10, type-only fallback true, `ALLOW_UNRESOLVED`, `ORDER_PRESERVING_INTERLEAVING`, recovery cap 20, seed 1234, dynamic enrichment false. Counts: 50,000 workloads, 268 static setup candidates, 1,641 FaultScenarios, 363 source setup bindings, zero prerequisite workloads.
- **Selected evidence:** WorkloadPlan `359f3c2d05e286094aec1a2d1c81f3fbc507cfe87de0ac79b2729b328cb73ec0`; serial order `getTournamentStep -> removeQuizStep -> removeTournamentStep -> getUserStep -> addParticipantStep`; `prerequisiteBaseline=null`; setup actions 1–12 match the planned list; both Tournament argument-1 bindings reference `setup-action-12.aggregateId`; AddParticipant course and participant bindings reference actions 1 and 4. Immediate `00100` FaultScenario `1b93b0a5eadb884ba22383f5e907a0bb64f5f3781aa9ffca8b29032d238749d3` places Remove compensation immediately after its third forward and before Add forwards.
- **Five hashes:** manifest `ff077ceef28c5d3c9b5c009cf82b2b2db23c9fbf6d1e9f185688ac911c87c2a5`; workload `31440c637c772227e995135cbefade8640851588602fb019756f6dcaa488785a`; fault `4ee8d64b065f3ca97e8c098b9fe9c6c58b1ac5352e1de78c4e77ad59d360265e`; accounting `959be1f22bfa5c179b7dab6d92dcb0c85856c402058310e6517d73440e3c27c2`; rejected inputs `2bb2f6592b13d9ea3312e1c1935eea4649caff7d818e23e38c2038801dd5c44b`.
- **Known exclusions:** runtime setup execution and independent closed dispatch are Checkpoint C. Harmful/control execution is Checkpoint D. The ordinary Quizzes test, scheduler, ordering/cap policy, final-impact issue, current-state handbook, and pre-existing roadmap work were not changed for B.
