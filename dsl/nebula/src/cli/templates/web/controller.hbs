package {{packageName}};

{{#each imports}}
{{{this}}}
{{/each}}

@RestController
public class {{aggregateName}}Controller {
    @Autowired
    private {{aggregateName}}Functionalities {{functionalitiesName}};
{{#each endpoints}}

    @{{method}}Mapping("{{path}}")
    public {{#if returnType}}{{returnType}}{{else}}void{{/if}} {{methodName}}({{#each parameters}}{{annotation}} {{type}} {{name}}{{#unless @last}}, {{/unless}}{{/each}}){{#if throwsException}} throws Exception{{/if}} {
{{#if returnType}}        return {{../functionalitiesName}}.{{methodName}}({{#each parameters}}{{name}}{{#unless @last}}, {{/unless}}{{/each}});
{{else}}        {{../functionalitiesName}}.{{methodName}}({{#each parameters}}{{name}}{{#unless @last}}, {{/unless}}{{/each}});
{{/if}}    }
{{/each}}
}
