# Artifacts Review — 2026-08-02-2

**Date:** 2026-08-02
**Skill files reviewed:** 11
**Doc files reviewed:** 18 (excludes `docs/reviews/*.md` generated outputs)
**Verdict:** Clean

> **Clean** = all alignment checks pass; only improvement opportunities found.
> **Minor issues** = inconsistencies present but none cause incorrect agent output.
> **Issues requiring action** = any inconsistency that would cause an agent to produce
>   incorrect output.

---

## Executive Summary

No broken paths, no P1–P4 or R1–R8 contradictions, and no ambiguous "as needed"-style guidance
without a decision criterion. The Check 4 neutral-domain sweep found 5 literal hits in current
`.claude/skills/implement-aggregate/session-{b,c,d}.md` and `docs/concepts/commands.md` content
plus 3 inside a generated review report; every one of the 5 is a verbatim relocation from before
the scanned base commit (session-letter swap, S2), not a newly introduced domain noun, and the 3
report hits are quotes of that same prior finding, not new content. No action required this cycle.

---

## Check 1 — Path Validity

Scanned every literal `docs/...` and `.claude/skills/...` path in the current (non-`docs/reviews/`)
artifact set.

| Source file | Referenced path | Status | Notes |
|-------------|----------------|--------|-------|
| `.claude/skills/classify-and-plan/SKILL.md` | `docs/concepts/rule-enforcement-patterns.md`, `docs/workflow.md`, `.claude/skills/_shared/conventions.md` | Exists | — |
| `.claude/skills/harness-retrospective/SKILL.md` | `.claude/skills/_shared/conventions.md`, `.claude/skills/review-artifacts/SKILL.md` | Exists | `docs/reviews/harness-retro-{app-name}-{YYYY-MM-DD}.md` is a template pattern (its own output), not existence-checked |
| `.claude/skills/implement-aggregate/session-a.md` | `docs/concepts/aggregate.md`, `docs/concepts/testing.md`, `docs/concepts/service.md` (§ Exception-Throw Convention) | Exists | — |
| `.claude/skills/implement-aggregate/session-b.md` | `docs/architecture.md`, `docs/concepts/commands.md`, `docs/concepts/sagas.md`, `docs/concepts/service.md`, `docs/concepts/testing.md` | Exists | — |
| `.claude/skills/implement-aggregate/session-c.md` | `docs/concepts/commands.md`, `docs/concepts/sagas.md`, `docs/concepts/service.md`, `docs/concepts/testing.md`, `.claude/skills/implement-aggregate/SKILL.md` | Exists | — |
| `.claude/skills/implement-aggregate/session-d.md` | `docs/concepts/aggregate.md`, `docs/concepts/events.md`, `docs/concepts/testing.md` | Exists | — |
| `.claude/skills/implement-aggregate/SKILL.md` | `docs/concepts/aggregate.md`, `docs/concepts/testing.md`, `.claude/skills/_shared/conventions.md` | Exists | — |
| `.claude/skills/review-artifacts/SKILL.md` | all 8 concept docs + `docs/architecture.md`, `docs/workflow.md`, `.claude/skills/_shared/conventions.md`, `.claude/skills/classify-and-plan/SKILL.md`, `.claude/skills/implement-aggregate/session-{a,c,d}.md` | Exists | — |
| `docs/workflow.md` | `docs/concepts/rule-enforcement-patterns.md`, `docs/concepts/testing.md`, `.claude/skills/boot-strap/SKILL.md`, `.claude/skills/classify-and-plan/SKILL.md`, `.claude/skills/implement-aggregate/session-{a,b,c,d}.md`, `.claude/skills/_shared/conventions.md` | Exists | — |
| `docs/concepts/testing.md` | `docs/concepts/sagas.md` | Exists | — |
| `docs/templates/aggregate-grouping-template.md` | `docs/concepts/events.md` | Exists | — |
| `docs/templates/domain-model-template.md` | `docs/concepts/rule-enforcement-patterns.md` | Exists | — |

**Literal application-name check:** searched all current docs/skill files for hardcoded
`applications/{name}/` paths. No hits outside `docs/reviews/*.md` (generated, excluded). All
`applications/` references in the active harness use the `{app-name}` template form.

**Package-layout tree cross-check:** `docs/architecture.md` § Package Structure Convention and
`docs/workflow.md` § Package layout describe the same subdirectory shape
(`aggregate/sagas/{factories,repositories,states}/`, `coordination/{webapi,functionalities,sagas,eventProcessing}/`,
`notification/{handling/handlers,subscribe}/`, `service/`, `messaging/`) — consistent with the
reconciliation done in `6b59353b`.

**Result:** no broken references.

---

## Check 2 — Pattern Alignment (P1–P4 and R1–R8)

### P1–P4 Consistency

| Location | Pattern | Claim | Authoritative definition | Verdict |
|----------|---------|-------|--------------------------|---------|
| `docs/architecture.md` — pattern selection table | P1/P2/P3/P4 | "Always true within one aggregate" → P1; "Synchronous service-level check (own-table uniqueness OR saga-assembled DTO field validation)" → P3; "Cross-aggregate; eventual consistency acceptable" → P2; "Precondition implicit in fetch / same value to two aggregates" → P4a/P4b | Matches `rule-enforcement-patterns.md` § Quick Reference verbatim | Consistent |
| `docs/architecture.md` — Request Lifecycle | P1/P2/P3 | `[P3]` own-table/uniqueness/DTO validation inside `@Transactional(SERIALIZABLE)`; `[P1]` `verifyInvariants()` at commit; `[P2]` EventProcessing caches publisher state | Matches | Consistent |
| `.claude/skills/classify-and-plan/SKILL.md` — Step 4 | P1–P4a/b | Defers to `rule-enforcement-patterns.md` § Decision Guide as authoritative; parser heuristic is explicitly marked non-normative | Matches (delegates rather than restates) | Consistent |
| `.claude/skills/implement-aggregate/session-a.md` | P1 | T1 tests one violation per non-`final` P1 rule via direct construction + `verifyInvariants()` | Matches | Consistent |
| `.claude/skills/implement-aggregate/session-c.md` | P3 | Own-table uniqueness guards, DTO field checks, P3 guard placement at top of service method | Matches | Consistent |
| `.claude/skills/implement-aggregate/session-d.md` | P2 | `getEventSubscriptions()` + `EventHandler`/`EventProcessing` chain caches publisher state; content matches P2 without needing to cite the letter | Matches | Consistent |

No duplicated-across-patterns cases found (e.g., no P1 rule also re-implemented as a P3 guard).

### R1–R8 Coverage

| Restriction | Expected location | Mentioned? | Consistent? | Notes |
|-------------|------------------|------------|-------------|-------|
| R1 — service loads only own aggregate | `docs/concepts/service.md`, session-b.md, session-c.md | `service.md` cites "[R1, R2 in architecture.md]" explicitly | Yes | session-b.md/session-c.md follow the rule in the read-method / mutate-method recipes but don't cite "R1" by number — Minor, not a contradiction |
| R2 — service injects only own components | same | `service.md` explicit; session-b.md § Update BeanConfigurationSagas.groovy states "a foreign service or foreign repository violates R1/R2" | Yes | Consistent |
| R3 — cross-aggregate via DTOs only | `docs/concepts/service.md`, session-b.md, session-c.md | `service.md` explicit ("(R3)"); session-c.md follows via DTO-only method signatures | Yes | Consistent |
| R4 — saga steps declare lock intent | `docs/concepts/sagas.md`, session-c.md | `sagas.md` § R4 Decision Table explicit; session-c.md cites "R4 foreign-vs-primary distinction" | Yes | Consistent; the `NOT_IN_SAGA` enum contradiction fixed in `7a8bd3e9` still holds — session-a.md explicitly says "Do not include `NOT_IN_SAGA`" |
| R5 — `getEventSubscriptions()` in consumer only | `docs/concepts/events.md`, session-d.md | `events.md` explicit ("(R5, ../architecture.md)"); session-d.md implements it via the ACTIVE-guard + subscription-construction recipe | Yes | Consistent |
| R6 — `verifyInvariants()` must not read DB | `docs/concepts/aggregate.md`, session-a.md | `aggregate.md` explicit ("(R6, ../architecture.md)"); session-a.md's `verifyInvariants()` recipe doesn't restate the no-DB-read restriction inline | Partial | Minor — session-a.md relies on the agent having read `aggregate.md` (which it is instructed to read in full); not a contradiction |
| R7 — DTOs immutable | `docs/concepts/service.md`, session-b.md, session-c.md | `service.md` § DTO Immutability (R7) explicit | Yes | Consistent |
| R8 — functionalities send commands upstream only | `docs/concepts/commands.md`, session-b.md, session-c.md | `commands.md` explicit ("Commands travel upstream only (R8)"); `sagas.md` § Step Ordering explicit; session-c.md cites "R8 upstream-only rule" | Yes | Consistent; all reachable from the docs sessions actually read, per `bda1a6c0` |

**Result:** no P1–P4 or R1–R8 contradictions. Two Minor observations noted above (R1, R6 not
cited by number in the session sub-files that implement them) — cosmetic, since the sessions
instruct reading the concept doc that does cite them.

---

## Check 3 — Improvement Opportunities

### Missing Examples

| Doc | Section | Why an example would help |
|-----|---------|---------------------------|

Nothing to report. `docs/architecture.md` § Architectural Restrictions (R1–R8) is prose-plus-table
by design and points to `service.md`/`sagas.md`/`commands.md` for the worked examples, which all
carry code blocks. Every other concept doc already has a code example alongside each pattern or
rule it states.

### Ambiguous Guidance

| File | Step | Ambiguous phrase | Missing decision criterion |
|------|------|------------------|------------------------------------|

Nothing to report. Searched all skill files for "depends on", "may be", "if applicable", "as
appropriate", "as needed" — every hit found is either a descriptive use ("may be comma-separated",
"may be `null`") or is immediately followed by the concrete criterion (e.g., classify-and-plan's
"depends on one thing only" is answered in the next sentence). No hedge phrase was found standing
alone without a resolvable rule.

---

## Check 4 — Neutral Domain

**Run in progress:** `quizzes-full` (plan.md present, 0 unchecked boxes — run complete but the
plan.md file itself still exists, so the check applies)
**Base commit:** `908d121883a2512ab368c10a16213d9057bd5f80^` — derived via the fallback path:
`git merge-base HEAD master` resolved to `HEAD` itself (harness work is committed directly on
`master` between runs), so the base is the parent of the oldest `harness:` commit reachable from
HEAD touching `docs`/`.claude/skills`.
**Added lines scanned:** 1576 (14 `harness:` commits in range, spanning the freeze-removal and
subsequent harness-fix commits through `8889b274`)

| File | Added line | Noun | Verdict |
|------|-----------|------|---------|
| `session-b.md` (§ conditional read step) | `...that includes Course name alongside Execution...` | Course, Execution | Moved — verbatim at base in `session-c.md` (pre-S2-swap) |
| `session-b.md` (§ one-step vs two-step) | `...(e.g., \`executionAggregateId\` is a field on \`Tournament\`)...` | Tournament | Moved — verbatim at base in `session-c.md` |
| `session-c.md` (T4 test template) | `// Spec: plan.md §3.5 Question / functionalities — UpdateQuestionContent; rule QUESTION_CONTENT_REQUIRED` | Question | Moved — verbatim at base in `session-b.md` |
| `session-d.md` (§ deletion events) | `...(e.g., \`DeleteQuestionEvent\` / \`DeleteCourseExecutionEvent\` received by \`Quiz\`)...` | Question, Quiz | Moved — same line at base in `session-d.md`, only the trailing clause after the domain-noun example was reworded |
| `docs/concepts/commands.md` (CommandHandler example) | `return "Tournament";` | Tournament | Moved — identical line at base, same file/location |
| `reviews/review-2026-08-01-2.md` (×3 table rows) | quotes of the `session-b.md`/`session-c.md` Course/Execution/Tournament/Question hits above, with verdict "False positive — present verbatim... relocated" | Course, Execution, Tournament, Question | Not a harness fix — this is a generated report quoting a prior review's own analysis of the same relocation, not new guidance text |

**Counts:** Violation 0 / Moved 5 / False positive (not applicable — report quotes) 3.

All hits trace to the session-letter swap (`4af2e7c4` — "swap session letters so reads (b) precede
writes (c)"), which relocated existing worked examples between `session-b.md` and `session-c.md`
without altering their content. No new domain noun from the `quizzes-full` run was introduced into
the harness in this diff window.

---

## Action Items

| Priority | Category | File | Finding | Suggested Fix |
|----------|---------|------|---------|---------------|
| Minor | R-coverage cross-reference | `.claude/skills/implement-aggregate/session-b.md`, `session-c.md` | R1/R2 aren't cited by number where the service-injection recipe is described | Optional: add "(R1/R2)" parenthetical next to the injection guidance, matching how R3/R7/R8 are already cited elsewhere |
| Minor | R-coverage cross-reference | `.claude/skills/implement-aggregate/session-a.md` | `verifyInvariants()` recipe doesn't restate the no-DB-read restriction (R6) inline, relying on the referenced read of `aggregate.md` | Optional: add one clause noting `verifyInvariants()` must not perform repository reads, citing R6 |

**Critical** = causes an agent to produce incorrect output (wrong code, missing file, wrong beans,
  wrong exception type, broken skill delegation).
**Major** = leaves an agent to infer or guess something the harness should have stated.
**Minor** = cosmetic inconsistency, missing example, ambiguous wording with low impact.
