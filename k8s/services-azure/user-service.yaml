apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: microservices-simulator
spec:
  selector:
    app: user-service
  ports:
    - port: 8088
      targetPort: 8088
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: microservices-simulator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      serviceAccountName: microservices-sa
      containers:
        - name: user-service
          image: simulatorcr.azurecr.io/quizzes:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8088
          resources:
            requests:
              memory: "256Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          env:
            - name: START_CLASS
              value: "pt.ulisboa.tecnico.socialsoftware.quizzes.microservices.user.UserServiceApplication"
            - name: SPRING_PROFILES_ACTIVE
              value: "kubernetes,user-service,sagas,stream,distributed-version"
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgres:5432/userdb"
            - name: SPRING_RABBITMQ_HOST
              value: "rabbitmq"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://jaeger:4317"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8088
            initialDelaySeconds: 60
            periodSeconds: 10
