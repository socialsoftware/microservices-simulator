# Harness Log - quizzes-full-2

Append-only. Schema and rules: `.claude/skills/_shared/conventions.md` § "Harness log".

| # | Session | Type | Artifact | Problem | Outcome | Ref |
|---|---------|------|----------|---------|---------|-----|
| 1 | 2.1.a | 2 | `.claude/skills/implement-aggregate/session-a.md` | Needed the sanctioned JPA realization of a P1 rule classified as "Java `final` field" (COURSE_NAME_FINAL, COURSE_TYPE_FINAL); the skill names the `non-final P1 rule` category but prescribes no field/constructor shape, so the no-arg-constructor and Hibernate-load-path handling was inferred. T1 never loads a row, so the inference is unproven until 2.1.b reads a Course back. | deferred | |
| 2 | 2.1.a | 2 | `.claude/skills/classify-and-plan/SKILL.md` | session-a.md requires `READ_{AGGREGATE}` in the saga-state enum when a later aggregate uses this one as a cross-aggregate prerequisite, but plan.md emits no per-aggregate saga-state set, so the slice had to scan later aggregates' plan.md sections and guess the enum; a mismatch would only surface in a later session's `c`. | deferred | |
| 3 | 2.1.b | 1 | `.claude/skills/classify-and-plan/SKILL.md`, `.claude/skills/implement-aggregate/session-b.md`, `.claude/skills/implement-aggregate/session-c.md` | The 2.N.b/2.N.c file-table templates and both session sub-files write command classes as `commands/{aggregate}/...`, which under the tables' stated root resolves inside `microservices/{aggregate}/`, contradicting `docs/concepts/commands.md` § "File Location" (app source root, so commands are shareable across services). Two harness files prescribed two packages for the same class. | fixed | 700600ac |
| 4 | 2.1.b | 1 | `docs/concepts/commands.md` | Closes row 1's sibling risk for commands. The § "What a Command Is" example declares only a multi-arg constructor and getters, but the test profile sets `local.messaging.serialize: true`, so commands round-trip through Jackson; the example as written fails to deserialize on the first command sent (`CreateCourse`/`GetCourseById`). | fixed | 90e5fe00 |
| 5 | 2.1.b | 1 | `docs/concepts/commands.md` | The § "Routing Commands" CommandHandler snippet calls `logger.warning(...)` in its `default ->` branch, but `ms.messaging.CommandHandler` declares no `logger` field, so the snippet does not compile as written. | fixed | 90e5fe00 |
| 6 | 2.1.b | 1 | `.claude/skills/implement-aggregate/session-b.md` | The § "Update `{AppClass}SpockTest.groovy`" fixture-helper template calls `aggregateIdGeneratorService`, but the section only instructs adding the functionalities field and the scaffolded base class declares no such field - silent compile error at first use. | fixed | 90e5fe00 |
| 7 | 2.1.b | 2 | `docs/concepts/testing.md` | Closes row 1. The final-field inference from 2.1.a is confirmed: a persisted Course loads back with correct `name`/`type`. But the mandated T2 shape does not prove it - under `@DataJpaTest` a fresh `UnitOfWork` still shares one persistence context, so the read-back returns the managed instance rather than a Hibernate-instantiated one. The slice proved it with a throwaway flush/clear probe, since deleted; no permanent test guards the load path. | deferred | |
| 8 | 2.1.c | 1 | `.claude/skills/implement-aggregate/session-c.md` | § Produce declares its `###` subheadings "the authority on what this session must emit", but the mandatory `create{Aggregate}()` helper replacement is a top-level `##` section outside it; a session treating § Produce as its manifest ships a stale placeholder helper and leaves session `b`'s read tests off the create path. Self-contradiction within one file. | fixed | 688cb5f7 |
| 9 | 2.1.c | 2 | `.claude/skills/implement-aggregate/session-b.md`, `.claude/skills/implement-aggregate/session-c.md` | session-c.md prescribes deriving `functionalityName` via `new Throwable().getStackTrace()[0].getMethodName()`; session-b.md is silent, so 2.1.b's coordinators used string literals. Both evaluate identically, so neither is demonstrably wrong, but every session `c` that appends to a session-`b` file faces the same choice. 2.1.c matched the existing file rather than introduce a second idiom mid-append. | deferred | |
| 10 | 2.1.c | 2 | `docs/concepts/service.md` | § "Injected Dependencies" shows `AggregateIdGeneratorService` as an `@Autowired` field while the same example takes repositories via the constructor, without saying whether the mix is deliberate. A session appending to the all-constructor-injection service that session `b` produces must decide whether to widen the constructor and the `@Bean` method instead. Following the example verbatim is what makes session-c.md § "BeanConfigurationSagas - No Change Needed" true. | deferred | |
