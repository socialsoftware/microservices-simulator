# Artifacts Review — 2026-08-01

**Date:** 2026-08-01
**Skill files reviewed:** 14 (excluding the retired `review-aggregate-vs-quizzes/SKILL.md`)
**Doc files reviewed:** 11 (excluding generated outputs under `docs/reviews/`)
**Verdict:** Issues requiring action

> **Clean** = all alignment checks pass; only improvement opportunities found.
> **Minor issues** = inconsistencies present but none cause incorrect agent output.
> **Issues requiring action** = any inconsistency that would cause an agent to produce
>   incorrect output.

---

## Executive Summary

The most critical finding is that S3 added a third row to `docs/concepts/sagas.md` § "R4 Decision
Table" (the newly-created-aggregate case) and re-scoped `docs/architecture.md` R4 to match, but the
single place R1-R8 are checked against code — `adversarial-review-aggregate/SKILL.md` Step 6,
Family D — still instructs the reviewer to classify every mutating step as "primary or foreign".
A create step fits neither, and both available verdicts flag correct code as a violation.

Overall the harness is internally consistent after Phase A: the freeze vocabulary is gone, the
session letters agree everywhere, and every `§` pointer resolves to a real heading. The two
remaining structural weaknesses are in this skill's own new Check 4, which is inert in the
configuration Phase A actually ran in and cannot distinguish a moved line from an introduced one.

Recommended next action: fix the Family D R4 check and the `session-c.md` reading list before any
write session runs, then decide whether Check 4's base-commit rule needs to be branch-aware.

---

## Check 1 — Path Validity

31 distinct literal `docs/**` and `.claude/skills/**` paths referenced; 29 resolve.

| Source file | Referenced path | Status | Notes |
|-------------|----------------|--------|-------|
| `.claude/skills/classify-and-plan/SKILL.md:22` | `docs/examples/example-domain-model.md` | **Broken** | `docs/examples/` does not exist. Usage example in the Input section. |
| `.claude/skills/classify-and-plan/SKILL.md:22` | `docs/examples/example-aggregate-grouping.md` | **Broken** | Same line, same cause. |
| all others (29) | — | OK | Every `docs/concepts/*`, `docs/templates/*`, `.claude/skills/**` and `session-*.md` reference resolves. |

Both broken references were already reported as Major in `reviews/review-2026-07-23.md:197`
and never fixed. No literal application name is hard-coded in any skill or doc; every
`applications/` path is written as `applications/{app-name}/...`.

---

## Check 2 — Pattern Alignment (P1–P4 and R1–R8)

### P1–P4 Consistency

Authoritative definitions from `docs/concepts/rule-enforcement-patterns.md`: P1 Intra-Invariant
(l.91), P2 Inter-Invariant / Event Subscription (l.149), P3 Service Guard (l.217), P4 By
Construction (l.261) with P4a Construction Prerequisite (l.267) and P4b Construction Invariant
(l.282).

| Location | Pattern | Claim | Authoritative definition | Verdict |
|----------|---------|-------|--------------------------|---------|
| `docs/architecture.md` — pattern selection table | P1–P4 | delegates; P4a/P4b named as "by construction" | same | Consistent |
| `docs/architecture.md` — Request Lifecycle | P1/P2/P3 | same | same | Consistent |
| `classify-and-plan/SKILL.md` — Step 4 | P1–P4a/b | delegates to § Decision Guide; keeps a mechanical parser heuristic, labelled as such | same | Consistent |
| `classify-and-plan/SKILL.md:667` — summary template | `P4a/b/c` | names a pattern **P4c** | taxonomy has only P4a and P4b | **Inconsistent (Minor)** |
| `session-a.md` | P1 | verifyInvariants on the aggregate | same | Consistent |
| `session-c.md` | P3 | service guard in the write session | same | Consistent (correct after the S2 letter swap) |
| `session-d.md` | P2 | subscription handling | same | Consistent |
| `review-aggregate/SKILL.md` — Step 6 (l.290, 302-303) | P1–P4 | P4a = saga fetches upstream via correct command; P4b = same value passed to both aggregates | same | Consistent |
| `adversarial-review-aggregate/SKILL.md` — Steps 3, 8 | P1, Family F re-derivation | independent re-derivation from the Decision Guide | same | Consistent |

### R1–R8 Coverage

Authoritative one-liners from `docs/architecture.md` l.154-216. Code-conformance owner is
`adversarial-review-aggregate/SKILL.md` Step 6, Family D (l.204-219).

| Restriction | Expected location | In Family D table? | Mentioned? | Consistent? | Notes |
|-------------|------------------|--------------------|------------|-------------|-------|
| R1 — service loads only own aggregate | `service.md`, session-b/c | Yes (l.212) | Yes | Yes | — |
| R2 — service injects only own components | `service.md`, session-b/c | Yes (l.213) | Yes | Yes | — |
| R3 — cross-aggregate flow via DTOs only | `service.md`, session-b/c | Yes (l.214) | Yes | Yes | — |
| R4 — forbiddenStates on steps mutating a **pre-existing** aggregate | `sagas.md`, session-c | Yes (l.215) | Yes | **No** | Family D names only "primary or foreign"; the Decision Table has three rows since S3. See Critical below. |
| R5 — getEventSubscriptions in consumer only | `events.md`, session-d | Yes (l.216) | Yes | Yes | Also forbids self-subscription; matches architecture.md. |
| R6 — verifyInvariants must not read from DB | `aggregate.md`, session-a | Yes (l.217) | Yes | Yes | — |
| R7 — DTOs are immutable | `service.md`, session-b/c | Yes (l.218) | Yes | Yes | — |
| R8 — commands upstream only | `commands.md`, session-b/c | Yes (l.219) | Yes | Yes | — |

All eight restrictions are present in the Family D table, so none is documented-but-unenforced.
R4's *check* is stale rather than absent.

---

## Check 3 — Improvement Opportunities

### Missing Examples

| Doc | Section | Why an example would help |
|-----|---------|---------------------------|
| `docs/concepts/sagas.md` | § "Create Functionality Sagas" | The section has both worked shapes, but no example of the compensation step for a **multi-step** create that also mutates a foreign aggregate — the combination R4 now splits across two of its three rows. |
| `docs/concepts/rule-enforcement-patterns.md` | § P4b | P4b is defined and cross-referenced but has no code shape, so `review-aggregate` Step 6's check ("saga passes the same value to both aggregates") is the only concrete rendering of it in the harness. |

### Ambiguous Guidance

| File | Step | Ambiguous phrase | Missing decision criterion |
|------|------|------------------|---------------------------|
| `session-c.md` | Reads item 7 | *"(Conditional) If the plan.md aggregate section lists cross-aggregate prerequisites"* | plan.md is now a blueprint rather than a manifest, so "lists" is no longer a sufficient trigger; the session should also derive prerequisites it finds required but unlisted, per `SKILL.md` § "Step 5b". |
| `review-artifacts/SKILL.md` | Step 6.a | *"Derive `{app-name}` per § Resolve app context"* | § "Resolve app context" takes the **first** `find applications -name plan.md` result. With more than one application on disk that is arbitrary, and Check 4's noun list silently comes from the wrong run. |

---

## Check 4 — Neutral Domain

**Run in progress:** `quizzes-full` (the only `plan.md` on disk; the `quizzes-full-2` run has not
been re-cut yet — Phase B is pending)
**Base commit:** `2fc456d6` (`git merge-base HEAD master`, which equals `HEAD` on master)
**Added lines scanned:** 0

As specified, the check found nothing because it had nothing to scan. This is the first exercise of
Check 4 and it exposes two defects in the check itself; both are reported as Major below.

Re-running against the base this plan actually names (`ad0f3256`, the parent of the first Phase A
commit) scans 1429 added lines across the eight `harness:` commits and yields three hits:

| File | Added line | Noun | Verdict |
|------|-----------|------|---------|
| `session-b.md:30` | ``7. ***(Conditional)*** If any read functionality joins data from an upstream aggregate (e.g., a "get with details" that includes Course name alongside Execution): …`` | Course, Execution | **False positive** — present verbatim at `ad0f3256` in `session-c.md:30`; relocated by the S2 letter swap, not introduced. |
| `session-b.md:84` | ``> - **One step** — when every filter criterion is stored directly on the aggregate (e.g., `executionAggregateId` is a field on `Tournament`). …`` | Tournament | **False positive** — present verbatim at `ad0f3256` in `session-c.md:78`; relocated by the S2 swap. |
| `session-c.md:175` | ``    // Spec: plan.md §3.5 Question / functionalities — UpdateQuestionContent; rule QUESTION_CONTENT_REQUIRED`` | Question | **False positive** — present verbatim at `ad0f3256` in `session-b.md:181`; relocated by the S2 swap. |

**Violations: 0. False positives: 3 of 3 hits.** No domain noun was newly introduced into the
harness by Phase A. The pre-existing stock in `docs/concepts/**` (Recorded Limitation 2) is
untouched by this check by design, since it predates any base commit.

---

## Action Items

All Critical and Major items were fixed in the S8 follow-up commit; the two Minors marked `Fixed`
were cheap and unambiguous and went in with them. The `Resolution` column records what happened.
This departs from the skill's own Hard Rule 7 ("Report, do not repair") because S8 of the run plan
explicitly directs the invoking session to fix Critical and Major findings — the repair is the
caller's, made after the report was written, not the skill's.

| Priority | Category | File | Finding | Suggested Fix | Resolution |
|----------|---------|------|---------|---------------|------------|
| Critical | Stale enforcement check | `.claude/skills/adversarial-review-aggregate/SKILL.md:215` | Family D's R4 row says *"Classify each step that mutates an aggregate as primary or foreign per `docs/concepts/sagas.md` § R4 Decision Table"*. That table has had **three** rows since S3 added *"**Newly created aggregate** (the step creates it) \| Plain command, no lock and no forbidden states; register a compensation that removes it"* (`sagas.md:99`), and `architecture.md:184` re-scoped R4 to a **pre-existing** aggregate. A create step matches neither offered category: classified "primary" it is flagged for a missing `SagaCommand` + `setSemanticLock` that must not exist; classified "foreign" it is flagged for a missing `setForbiddenStates` that must not exist. The reviewer emits a false violation against correct code, and the repair makes the code wrong. | Add the third category to the R4 row: a step that brings its aggregate into existence declares neither a lock nor `forbiddenStates`, and is a violation only if it registers **no** compensation while a later step follows it. Cross-reference `sagas.md` § "Create Functionality Sagas". | **Fixed** — Family D R4 now names all three categories and states that demanding a lock or `forbiddenStates` of a create step is a false violation. |
| Major | Broken reference | `.claude/skills/classify-and-plan/SKILL.md:22` | Usage example cites `docs/examples/example-domain-model.md` and `docs/examples/example-aggregate-grouping.md`; `docs/examples/` does not exist. Reported as Major on 2026-07-23 and still open. | Replace with a template-shaped example (`applications/{app-name}/{app-name}-domain-model.md`), which needs no directory to exist and obeys § "Neutral domain". | **Fixed** — replaced with a template-shaped path; `docs/examples/` is no longer referenced anywhere. |
| Major | Incomplete reading list | `.claude/skills/implement-aggregate/session-c.md:28-32` | The writes sub-file's `sagas.md` reading list names § Step Ordering, § Lock-Acquisition Step Pattern, § Semantic Locks in Practice, § R4 Decision Table and § Write Workflow Structure — but **not** § "Create Functionality Sagas", the section S3 added specifically for the create case. Create functionalities are written in session `c`, so the session that needs the pattern is the one not told to read it. | Add `- § Create Functionality Sagas (only if any write functionality for this aggregate creates it)` to item 5. | **Fixed** — § "Create Functionality Sagas" added to item 5, conditional on the aggregate having a create functionality. |
| Major | Check inert in practice | `.claude/skills/review-artifacts/SKILL.md` Step 6.b | `BASE=$(git merge-base HEAD master)` resolves to `HEAD` whenever harness work is committed on master, so the diff is empty and the check scans 0 lines. That is exactly the configuration Phase A ran in: eight `harness:` commits, all invisible to Check 4. The check only functions on a run branch. | Fall back to a run base when `merge-base` degenerates: prefer the first commit of the current run (e.g. the parent of the oldest `harness:` commit not on the upstream branch), or accept an explicit base argument, and state in the report which base was used. | **Fixed** — Step 6.b falls back to the parent of the oldest `harness:` commit when `merge-base` degenerates, and must state the base used. Verified: now derives `ad0f3256` and scans 1454 lines instead of 0. |
| Major | False-positive rate | `.claude/skills/review-artifacts/SKILL.md` Step 6.c | `git diff` renders a moved line as an addition, so any refactor that relocates content floods Check 4. On this run all 3 hits were S2's letter swap moving pre-existing lines between `session-b.md` and `session-c.md` — a 100% false-positive rate on its first exercise. Left unaddressed, a real violation will be dismissed along with the noise. | Before reporting a hit, check whether the identical line exists anywhere at `BASE` (`git grep -F "<line>" $BASE -- docs .claude/skills`); if it does, it is a move, not an introduction. Add `-M` to the diff as a first-pass filter. | **Fixed** — Step 6.c adds a `git grep -F` check against the base and a `Moved` verdict, and `-M` on the diff. Verified against one of the three hits. |
| Minor | Nonexistent pattern name | `.claude/skills/classify-and-plan/SKILL.md:667` | Summary template reads `P4a/b/c: R`. There is no **P4c** anywhere in `docs/concepts/rule-enforcement-patterns.md` or the harness; the taxonomy is P4a and P4b only. | Change to `P4a/b: R`. | **Fixed** — now `P4a/b: R`. |
| Minor | Ambiguous trigger | `.claude/skills/implement-aggregate/session-c.md` Reads item 7 | *"If the plan.md aggregate section lists cross-aggregate prerequisites"* still treats plan.md as complete, which decision 5 and `SKILL.md` § "Step 5b" reversed. | Add that a prerequisite the session finds required but unlisted is handled the same way, and the plan row amended. | Open — Minor; folded into the broader plan-is-a-blueprint wording already carried by `SKILL.md` § "Step 5b". |
| Minor | Arbitrary selection | `.claude/skills/review-artifacts/SKILL.md` Step 6.a | § "Resolve app context" returns the first `find` result; with several applications on disk Check 4's noun list is drawn from an arbitrary one. Today `quizzes-full` was selected while the run of record is `quizzes-full-2`. | Have Check 4 state which `plan.md` it used, and halt if more than one exists without a way to disambiguate. | Open — Minor; needs a decision on multi-application disambiguation, which Phase B makes moot by leaving one application on disk. |
