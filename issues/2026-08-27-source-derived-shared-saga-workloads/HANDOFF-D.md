# Checkpoint D handoff

- **State:** complete. Checkpoints A–D and the documented feature are implemented and evidenced; no Checkpoint D work remains.
- **Outcome:** the ordinary unmodified Quizzes recovery-window test now drives one automatic v5 RemoveTournament–AddParticipant WorkloadPlan with a twelve-action SetupPlan, no prerequisite baseline, one shared action-12 Tournament result, and persisted harmful/control FaultScenarios. Two separate Docker/JVM/H2 attempts proved the bounded final-state distinction without invoking the historical Remove/Add provider.
- **Generated package:** `verifiers/target/checkpoint-d-automatic-20260828-1825/quizzes-20260828-172723-706/` (about 4.6 GiB). Schema was manifest/workload v5 with fault scenario v4. Effective generation bounds were `INTERACTION_PRUNED`, pairs only, `maxCatalogScenarios=50000`, `maxInputVariantsPerSaga=10`, `maxSchedulesPerInputTuple=10`, order-preserving interleaving, type-only fallback enabled, seed `1234`, recovery cap `20`. The package exported 50,000 Workloads, 268 static setup candidates, 1,641 FaultScenarios, and 86 rejected-input diagnostics. No regeneration occurred during remediation or final execution. After medium review accepted Checkpoint D, the reproducible package catalog was deleted to reclaim disk; its identities, configuration, counts, and hashes remain recorded here.
- **Selection:** WorkloadPlan `359f3c2d05e286094aec1a2d1c81f3fbc507cfe87de0ac79b2729b328cb73ec0`; forward/fault-slot order `getTournamentStep -> removeQuizStep -> removeTournamentStep -> getUserStep -> addParticipantStep`; `prerequisiteBaseline=null`; setup actions `setup-action-1` through `setup-action-12`; both participant argument-1 bindings reference `setup-action-12.aggregateId`.
- **Harmful proof:** FaultScenario `1b93b0a5eadb884ba22383f5e907a0bb64f5f3781aa9ffca8b29032d238749d3`, vector `00100`, attempt `dc9c8b1f-4d2f-4bb2-afb4-6e0e2d424bf2`, `PARTIAL_COMPENSATED / EXACT`, ImpactV1 `EVALUATED` with zero findings, observer `VALID / HARMFUL_FOR_RULE`. Tournament `10` was `ACTIVE`, referred to Quiz `9`, and Quiz `9` was `DELETED`; the latest raw Tournament Saga state decoded through `SagaStateConverter` to exact `GenericSagaState.NOT_IN_SAGA`.
- **Control proof:** FaultScenario `4618c12787ee1a23b9c61e72708103ed6d79624a889fa026afffc40bbf85541d`, vector `00000`, attempt `b986dd16-0847-424c-88db-db3c149facb4`, `PARTIAL_COMPENSATED / DEVIATED`, ImpactV1 `EVALUATED` with zero findings, observer `VALID / NO_BROKEN_REFERENCE`. The supported zero-bit fallback left Tournament `10` and Quiz `9` deleted, with the same exact decoded outside-Saga proof. This is only a nearby bounded control, not a global safety claim.
- **Shared setup/provider absence:** each attempt ran all twelve actions once in order with status `SUCCEEDED`, cleared two pending setup events, and proved an empty baseline. Action 12 retained one attempt-local result with aggregate id `10`; both participant bindings used its exact retained-result id and resolved value `10`. Each execution's prerequisite evidence was the exact no-provider sentinel: status `NOT_REQUIRED`; null provider id/version and failure fields; zero duration and pending events; true empty baseline; empty bindings/evidence. The historical descriptor/provider remains unchanged and retained for old v4 evidence.
- **Evidence files:** `harmful-final-execution.json`, `harmful-final-impact.json`, `harmful-final-observation.json`, `control-final-execution.json`, `control-final-impact.json`, and `control-final-observation.json` under `verifiers/target/checkpoint-d-automatic-20260828-1825/`.
- **Package hashes before/after:** manifest `48b92f0b33fc7c8c7e10394fde0b819978da462a5c7619d877f4cbb4030b2966`; workloads `31440c637c772227e995135cbefade8640851588602fb019756f6dcaa488785a`; faults `4ee8d64b065f3ca97e8c098b9fe9c6c58b1ac5352e1de78c4e77ad59d360265e`; accounting `959be1f22bfa5c179b7dab6d92dcb0c85856c402058310e6517d73440e3c27c2`; rejected inputs `2bb2f6592b13d9ea3312e1c1935eea4649caff7d818e23e38c2038801dd5c44b`. All were unchanged after both attempts.
- **D remediation:** selected package reading now opens each linked artifact once and hashes the exact strict-UTF-8 parse stream through EOF; selected execution retains linked paths and missing-id reporting without a whole-package fallback; the benchmark observer accepts only the exact non-provider prerequisite sentinel. No schema, cap, scheduling, fault, recovery, event, impact, provider, or application semantics changed in this remediation.
- **Validation:** focused `ScenarioCatalogJsonlWriterSpec,ScenarioExecutorSpec` passed 176 tests; full verifier suite passed 700 tests; focused `QuizzesRemoveAddBenchmarkRunnerSpec` passed 49 tests; final affected checks and `git diff --check` passed. The Docker executions used the supported benchmark script in separate `scenario-executor` containers with 4 GiB limit/3 GiB heap and `/reports` mounted.
- **Documentation:** updated `docs/verifiers-impl/current-state.md`, child and parent roadmaps, decision index, and added `docs/verifiers-impl/decisions/2026-08-28-source-derived-ordered-setup.md`.
- **Disk/package retention:** free disk was about 5.7 GiB after both runs. Following medium review, the 4.6 GiB package catalog was deleted while the six authoritative `*-final-*` reports were preserved under `verifiers/target/checkpoint-d-automatic-20260828-1825/`, restoring about 11 GiB free.
- **Exclusions:** generic final-state impact, 19/15 parity through that future generic authority, GA/reward, output cleanup, cross-workload allocation, broader setup-language coverage, TCC, and remote/distributed execution remain later work. No staging, commit, reset, clean, or package regeneration was performed.
- **What to inspect:** compare the two `*-final-observation.json` files, then inspect each execution's `prerequisiteSetup`, `sourceSetup.actions`, and `sourceSetup.participantBindings`. The expected distinction is active-Tournament/deleted-Quiz only for `00100`; setup and provider-absence evidence should otherwise have the same shape.
