# Bounded structural space map

Approved by the user's delegated overnight exploration request. Discover additional executable Quizzes spaces for later search, without implementing GA or changing production Java, domain behavior, ImpactV2, or anomaly policy.

Select eight distinct source-derived pair/triple combinations before observing their new scores. Fix each setup, input tuple, normal schedule, and event deliveries. Retain setup/execution/assessment failures and zeros without replacing cases. Positive scenario counts are not defect counts; no causal or synergy claims without matching controls.

Use the current supported generator/request/executor package contracts and a compatible frozen build. At most two fresh Docker/JVM/H2 executions concurrently; 120 seconds per attempt. No global duration cap. Produce a finite sample, condition/score distributions, representative repeatability, costs, limitations, and a concrete recommendation for later GA work.

## Approved setup-qualification correction

After the frozen campaign, diagnose the forty INVALID executions without changing their
evidence. Correct the generic source-setup adapter only where a selected participant is
the same exact facade call already replayed by `setup()`: use a strict prefix before that
target and share it only with compatible inputs from the same source class. Do not delete
or reorder source actions, synthesize receivers, change event replay, ImpactV2, application
rules, or historical packages.

Qualify the change first with the no-fault structural equivalent of `w01`, then with at
least one source-backed receiver control, and only then with a relevant fault vector.
Classify the six event-bearing groups by setup, unassigned application failure, and
receiver eligibility instead of treating all forty INVALID reports as one defect class.
Keep raw output under `verifiers/target`, commit compact evidence and English technical
documentation, and preserve the original selection and meeting note.
