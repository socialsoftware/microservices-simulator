# Context Map

This repository does not use a root `CONTEXT.md` glossary.

Canonical domain glossaries:

- Verifier / fault-analysis scenario generation:
  `docs/verifiers-impl/glossary.md`

Agents and `sp-*` skills should update the mapped glossary above instead of creating or editing root `CONTEXT.md`.
