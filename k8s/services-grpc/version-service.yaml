apiVersion: v1
kind: Service
metadata:
  name: version-service
  namespace: microservices-simulator
  annotations:
    grpcPort: "9091"
spec:
  selector:
    app: version-service
  ports:
    - name: http
      port: 8081
      targetPort: 8081
    - name: grpc
      port: 9091
      targetPort: 9091
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: version-service
  namespace: microservices-simulator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: version-service
  template:
    metadata:
      labels:
        app: version-service
    spec:
      serviceAccountName: microservices-sa
      containers:
        - name: version-service
          image: simulator:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8081
              name: http
            - containerPort: 9091
              name: grpc
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          env:
            - name: START_CLASS
              value: "pt.ulisboa.tecnico.socialsoftware.ms.domain.version.VersionServiceApplication"
            - name: SPRING_PROFILES_ACTIVE
              value: "kubernetes,version-service,grpc"
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://localhost:5432/versiondb"
            - name: SPRING_RABBITMQ_HOST
              value: "rabbitmq"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://jaeger:4317"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 60
            periodSeconds: 10

        - name: postgres
          image: postgres:14-alpine
          ports:
            - containerPort: 5432
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          env:
            - name: POSTGRES_DB
              value: "versiondb"
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              value: "postgres"
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - postgres
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: postgres-data
          emptyDir: {}
