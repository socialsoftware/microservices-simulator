# Artifacts Review — 2026-08-08-2

**Date:** 2026-08-08
**Skill files reviewed:** 13 (`.claude/skills/**/*.md`) + `boot-strap/templates/README.md`
**Scaffold templates reviewed:** 9 (`.claude/skills/boot-strap/templates/*.template`)
**Doc files reviewed:** 11 (`docs/**/*.md`, excluding the generated `docs/reviews/`)
**Verdict:** Issues requiring action

> **Clean** = all alignment checks pass; only improvement opportunities found.
> **Minor issues** = inconsistencies present but none cause incorrect agent output.
> **Issues requiring action** = any inconsistency that would cause an agent to produce
>   incorrect output.

---

## Executive Summary

Every finding of `review-2026-08-08.md` is closed — all three Criticals, both Majors and all thirteen
Minors, across `harness:` commits `5ae093665`, `546a966d6` and `76ab595e6`. The C1 question that
report could not settle statically was resolved against `simulator/`: `EventHandler`'s constructor
takes `JpaRepository<? extends Aggregate, Integer>`, so `events.md` and `session-d.md` needed no
change and no framework edit was made.

One **new Critical** surfaced, of exactly the shape C2 had: `docs/architecture.md` § Package
Structure Convention is a third, stale statement of the event-handler layout, showing
`handlers/{Event}EventHandler.java` — one handler class per event type — which
`docs/concepts/events.md` (sole owner of that wiring), `docs/workflow.md` § Naming conventions and
`session-d.md` all contradict with one dispatcher per consumer aggregate. It survived the previous
review because that review checked § P2 against `events.md` but never checked the package-layout
block. One Minor remains: `classify-and-plan/SKILL.md` still carries the "typically" hedge on the
compensation-test gate that was just dropped from `session-c.md`.

Path validity, the scaffold-template claims, P1–P4 and R1–R8 are otherwise clean, and the
neutral-domain check is clean at 1656 scanned lines. Fix the one Critical before starting aggregate 9.

---

## Check 1 — Path Validity

Every literal `docs/...` and `.claude/...` path across `docs/**` and `.claude/skills/**` was extracted
and existence-checked: **30 distinct concrete references, all resolve. No broken references.**
Template-pattern paths (`applications/{app-name}/...`, `session-{type}.md`, `session-*.md`) were
checked for shape, not existence.

| Source file | Referenced path | Status | Notes |
|-------------|----------------|--------|-------|
| `docs/workflow.md:227`, `_shared/conventions.md:179`, `implement-aggregate-full/SKILL.md:222` | `.claude/agents/aggregate-slice.md` | Exists | Outside the Step 1.b `find` (which scans `.claude/skills`), present on disk |
| all skills | `.claude/skills/_shared/conventions.md` | Exists | — |
| all skills | `.claude/skills/_shared/session-completion.md` | Exists | — |
| `docs/workflow.md`, `implement-aggregate/SKILL.md`, `review-artifacts/SKILL.md` | `.claude/skills/implement-aggregate/session-{a,b,c,d}.md` | Exist | — |
| `implement-aggregate-full/SKILL.md:28,200` | `session-*.md`, `session-{type}.md` | Template | Resolve to the four sub-files above |
| `review-artifacts/SKILL.md:116` | `.claude/skills/boot-strap/templates/README.md` | Exists | — |
| `harness-retrospective/SKILL.md:273` | `.claude/skills/review-artifacts/SKILL.md` | Exists | — |
| every concept doc cross-link | `docs/concepts/*.md` (7 files) | Exist | — |
| `docs/concepts/aggregate.md:20` | `simulator/.../ms/aggregate/Aggregate.java` | Exists | Only fully-spelled `simulator/` path in the artifact set; the rest use `.../` elision |
| `_shared/session-completion.md:175` | `simulator/path/to/ChangedFile.java` | Template | Retro-template placeholder, not a reference |
| `classify-and-plan/SKILL.md:21` | `applications/my-app/…` | Literal app name | Illustrative invocation; `my-app` is the neutral placeholder used across `conventions.md` and `boot-strap/SKILL.md`, and line 22 gives the `{app-name}` form. Resolution "leave as-is" was accepted from the previous review. |
| all `applications/{app-name}/…` | — | Template | Shapes match `docs/workflow.md` § Package layout |

### Scaffold template contents

| Template | Symbol claimed | Claimed by | Present? | Notes |
|----------|---------------|------------|----------|-------|
| all 9 | the nine-template list | `boot-strap/SKILL.md:49-57` | Yes | Directory holds exactly those 9 plus `README.md` |
| `AppSimulator.java.template` | package `…{{PKG}}`; `@EnableJpaRepositories`/`@EntityScan` over `…{pkg}` **and** `…ms` | `boot-strap/SKILL.md:194` | Yes | — |
| `AppException/AppErrorMessage.java.template` | package `…{{PKG}}.microservices.exception` | `boot-strap/SKILL.md:98-99` | Yes | — |
| `SpockTest.groovy.template` | package `pt.ulisboa.tecnico.socialsoftware` (parent, not `{pkg}`) | `boot-strap/SKILL.md:104-105`, `docs/workflow.md:53` | Yes | — |
| `AppException.java.template` | sole user of `{{appClass}}` | `templates/README.md:21-22` | Yes | Verified: no other template contains `{{appClass}}` |
| `AppErrorMessage.java.template` | `PLACEHOLDER = "placeholder"`, removable once the first domain constant lands | `templates/README.md` § PLACEHOLDER | Yes | **Closes A3.** The template ships it; `README.md` now states its purpose and that it is the one exception to "do not remove existing constants" |
| `pom.xml.template` | surefire include `**/{pkg}/sagas/**` | `boot-strap/SKILL.md:164` | Yes | `**/{{PKG}}/sagas/**` at line 78 |
| `BeanConfigurationSagas.groovy.template` | `@PropertySource("classpath:application-test.properties")` | `boot-strap/SKILL.md:196` | Yes | — |
| `BeanConfigurationSagas.groovy.template` | markers `// Domain beans are added below…`, `// Domain imports…` | `boot-strap/SKILL.md:88-90` | Yes | 2 markers (lines 28, 127) |
| `BeanConfigurationSagas.groovy.template` | more than one `CommandGateway` implementation registered | `testing.md:448-450` | Yes | `LocalCommandGateway` (line 85) + mocked `StreamCommandGateway` (line 102) |
| `AppSpockTest.groovy.template` | markers `// Domain imports`, `// Domain constants…`, `// Domain @Autowired fields…`, `// Domain create* helpers…` | `session-a.md:288-297`, `boot-strap/SKILL.md:88-90` | Yes | 4 markers (lines 12, 16, 25, 53) |
| `AppSpockTest.groovy.template` | `impairmentService` field + `loadBehaviorScripts()` helper | `testing.md:513-518` | Yes | — |
| `AppSpockTest.groovy.template` | `sagaStateOf(Integer)` | `testing.md:403, 496` | Yes | — |
| `AppSpockTest.groovy.template` | `loadForCheck(aggregateId, type)` | `testing.md:317-328` | Yes | — |
| `AppSpockTest.groovy.template` | `flushAndClear()` = `EntityManager.flush()` then `clear()` | `testing.md:205-207` | Yes | — |
| `AppSpockTest.groovy.template` | does **not** declare `commandGateway` | `testing.md:440` | Yes (absent, as claimed) | `SpockTest.groovy.template` is likewise empty |
| `AppSpockTest.groovy.template` | does **not** declare `aggregateIdGeneratorService` | `session-b.md:220-221` | Yes (absent, as claimed) | — |
| `AppSpockTest.groovy.template` | no class-level `mavenBaseDir` | — | Yes (absent) | **Closes A2.** The dead field was removed; the identical local inside `loadBehaviorScripts()` remains and is the only definition |

No undocumented helpers remain in the templates.

---

## Check 2 — Pattern Alignment (P1–P4 and R1–R8)

### P1–P4 Consistency

Authoritative one-liners, from `docs/concepts/rule-enforcement-patterns.md` § Quick Reference:

- **P1** — single-aggregate state rule, enforced in `verifyInvariants()`; strong consistency.
- **P2** — cache state from another aggregate via `getEventSubscriptions()` + handler chain; eventual; never blocks.
- **P3** — synchronous service guard in `*Service.java` (own-table uniqueness **or** saga-assembled DTO validation), inside `@Transactional(SERIALIZABLE)`; strong.
- **P4a** — precondition implicit in a saga fetch query; no enforcement code.
- **P4b** — invariant holds because the saga passes the same value to two aggregates; no enforcement code.

| Location | Pattern | Claim | Authoritative definition | Verdict |
|----------|---------|-------|--------------------------|---------|
| `architecture.md:247-252` — pattern selection table | P1/P2/P3/P4a/P4b | Verbatim match on all five rows | as above | Consistent |
| `architecture.md:112-147` — Request Lifecycle | P3 | "own-table reads, uniqueness checks, or DTO field validation; runs inside `@Transactional(SERIALIZABLE)`" | as above | Consistent |
| `architecture.md:112-147` — Request Lifecycle | P1 | "`verifyInvariants()` on each changed aggregate" at UoW commit | as above | Consistent |
| `architecture.md:112-147` — Request Lifecycle | P2 | "consumer aggregate caches publisher state", async ~1 s | as above | Consistent |
| `classify-and-plan/SKILL.md:211-217` — Step 4 | P1–P4a/b | Delegates to the doc's § Decision Guide, explicitly declining to re-derive; keyword matching declared "mechanical only" | as above | Consistent |
| `session-a.md:62` | P1 | "enforces all P1 rules … reads only fields already held by the aggregate — never a repository" | as above | Consistent |
| `session-c.md:67-68` | P3 | "P3 own-table uniqueness guards" / "P3 DTO field checks … receive the saga-assembled DTO as a parameter" | as above | Consistent |
| `session-d.md:133` | P2 | Invariant check inside the Functionalities ByEvent method; exception propagates and the event is not marked processed | as above | Consistent (consumer-commit behaviour, not blocking a caller's operation) |
| `rule-enforcement-patterns.md:225-233` — § P2 | P2 | **Now a pointer** to `events.md` § Canonical Wiring Snippet; the competing four-step recipe is gone | as above | **Consistent — closes C2** |
| `rule-enforcement-patterns.md:75-103` — Decision Guide Step 3 | P2→P3 | The re-classification now carries the full code rewrite, and states P2 and P3 are not mutually exclusive | as above | Consistent |

### R1–R8 Coverage

Authoritative one-liners from `docs/architecture.md` § Architectural Restrictions.

| Restriction | Expected location | Mentioned? | Consistent? | Notes |
|-------------|------------------|------------|-------------|-------|
| R1 — service loads only own aggregate | `service.md`, `session-b.md`, `session-c.md` | Yes — `service.md:46`, `session-b.md:60,173-176`, `session-c.md:64` | **Yes** | `session-b.md:60` now routes the foreign DTO through the saga and names R2 explicitly — **closes C3** |
| R2 — service injects only own components | same | Yes — `service.md:35-50`, `session-b.md:171-181`, `session-c.md:300-304` | Yes | Closed list restated identically in all three |
| R3 — cross-aggregate flow via DTOs only | same | Yes — `service.md:48`, `session-b.md:174`, `session-c.md:64` | Yes | — |
| R4 — saga steps declare lock intent | `sagas.md`, `session-c.md` | Yes — `sagas.md:99-107` (R4 Decision Table, 3 rows), `session-c.md:120` | Yes | `architecture.md:198` correctly names `sagas.md` § R4 Decision Table as authoritative |
| R5 — `getEventSubscriptions()` in consumer only | `events.md`, `session-d.md` | Yes — `events.md:61`, `session-d.md:44,48-49`, `aggregate.md:31` | Yes | — |
| R6 — `verifyInvariants()` must not read from DB | `aggregate.md`, `session-a.md` | Yes — `aggregate.md:26-29`, `session-a.md:62` | Yes | — |
| R7 — DTOs are immutable | `service.md`, `session-b.md`, `session-c.md` | Yes — `architecture.md:222-231` (new wrong/right pair), `service.md:215-217`, `session-b.md:102`, `session-c.md:21` | Yes | — |
| R8 — functionalities send commands upstream only | `commands.md`, `session-b.md`, `session-c.md` | Yes — `commands.md:135`, `sagas.md:418`, `session-b.md:99-101`, `session-c.md:120` | Yes | — |

---

## Check 3 — Improvement Opportunities

### Missing Examples

All five gaps named by `review-2026-08-08.md` Check 3.a are closed: `conventions.md` § Harness log
now carries three illustrative rows including a `Closes row {N}.` row; `architecture.md` § R7 a
wrong/right pair; `testing.md` § T2 Event Publication a full negative-case block plus the
`countBefore`-placement rationale; `sagas.md` § Step Ordering item 1 the `validateDatesStep` snippet
with its no-dependencies rule; and `rule-enforcement-patterns.md` § Decision Guide Step 3 the P2→P3
rewrite in code.

| Doc | Section | Why an example would help |
|-----|---------|---------------------------|
| — | — | nothing to report |

### Ambiguous Guidance

| File | Step | Ambiguous phrase | Missing decision criterion |
|------|------|------------------|---------------------------|
| `.claude/skills/classify-and-plan/SKILL.md` | Step 7, note under the 2.N.c file table (lines 538-539) | "A create functionality **typically** drops the pair; a lock-then-mutate functionality **typically** keeps it" | The same hedge that was just removed from `session-c.md:253-255`, which the note itself defers to as owner of the rule. See Minor row below. |

The four ambiguities from the previous review are closed: `events.md:295-299` now states the
field-overlap test in place of "rare" (and `session-d.md:137` correctly delegates rather than
paraphrasing, so the two cannot drift); `conventions.md` § Resolve app context now halts on multiple
`find` results absent a local rule; § Run the test suite no longer dangles its colon; and
`session-c.md` § compensation applicability no longer hedges.

---

## Check 4 — Neutral Domain

**Run in progress:** quizzes-full-2
**Base commit:** `0f22736c45d001f314442385ef4007d96ec8afee`, derived by `git merge-base HEAD master`
(non-degenerate: the run's harness commits are on branch `quizzes-full-2`, not on master, so the
fallback to "parent of the oldest `harness:` commit" was not needed)
**Added lines scanned:** 1656 (over `docs` and `.claude/skills`, excluding the generated
`docs/reviews/` outputs, which the skill's input enumeration excludes)

**Forbidden nouns** (from `applications/quizzes-full-2/plan.md` `### {N}. {Aggregate}` headers):
Course, User, Topic, Execution, Question, Quiz, QuizAnswer, Tournament.

| File | Added line | Noun | Verdict |
|------|-----------|------|---------|
| — | — | — | nothing to report |

**Counts: 0 Violation / 0 Moved / 0 False positive.**

The unfiltered diff produces 48 whole-word matches. All 48 are inside `docs/reviews/` — 3 in
`review-2026-08-04-2.md`, 1 each in `-2026-08-04.md`, `-2026-08-06.md`, `-2026-08-07.md` and
`-2026-08-08.md` — every one of them the "Forbidden nouns" line each report prints about itself.
Those files are generated outputs of this skill, excluded from the input artifact set by § Step 1.b,
so they are not violations. Across the harness artifacts proper the noun set does not appear at all.

The 898-line increase in scanned lines over the previous run (758 → 1656) is this session's three
`harness:` commits plus the `-2026-08-08` report itself entering the diff; the zero therefore covers
strictly more added prose than the previous zero did.

---

## Action Items

| Priority | Category | File | Finding | Suggested Fix |
|----------|---------|------|---------|---------------|
| **Critical** | competing-variant | `docs/architecture.md:87-93` (§ Package Structure Convention) | The package-layout block shows the event-handler node as `notification/handling/handlers/{Event}EventHandler.java` — **one handler class per event type**. Three artifacts say the opposite: `docs/concepts/events.md:93` ("Use **one concrete `{Consumer}EventHandler` class** per consumer aggregate … Do **not** create a separate handler subclass per event type"), `docs/workflow.md:78` (`Event handler \| {Aggregate}EventHandler.java \| ShipmentEventHandler.java`), and `session-d.md:86-96` ("A **single concrete class** that extends `EventHandler` and dispatches all subscribed event types … One `@Component` with an `instanceof` dispatch"). This is the surviving third head of the C2 finding: C2 removed the per-event handler from `rule-enforcement-patterns.md` § P2 but the package-layout block was never checked. It is also internally inconsistent — every other node in the same block uses `{Xxx}` (the aggregate) and only this one uses `{Event}`. An agent reading `architecture.md` for the package layout (which `session-b.md:19` sends it to do) emits N handler classes and N beans instead of one. | Change the node to `{Xxx}EventHandler.java` (single dispatcher), matching the `{Xxx}` convention every sibling node in the block already uses, and add the inline annotation `(single dispatcher — one per aggregate, not per event)`. Then confirm no other line of `architecture.md` restates event wiring: § Application Anatomy's pipeline diagram (lines 42-47) is prose-level and correct as written. |
| **Minor** | ambiguity | `.claude/skills/classify-and-plan/SKILL.md:538-539` | The note gating the compensation-test pair still reads "A create functionality **typically** drops the pair; a lock-then-mutate functionality **typically** keeps it", two lines after the same note declares `session-c.md` the owner of that rule and says "do not restate or re-derive it here". `session-c.md:253-255` now states it without the hedge. The softer copy is the one a planning agent reads first. | Drop both instances of "typically", or replace the sentence with a bare pointer: "Which functionalities need the pair is decided in session `c` by the applicability test named above." |

**Critical** = causes an agent to produce incorrect output (wrong code, missing file, wrong beans,
  wrong exception type, broken skill delegation).
**Major** = leaves an agent to infer or guess something the harness should have stated.
**Minor** = cosmetic inconsistency, missing example, ambiguous wording with low impact.
