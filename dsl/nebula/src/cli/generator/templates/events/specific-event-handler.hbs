package {{packageName}};

{{imports}}

{{#each eventHandlers}}
public class {{handlerName}} extends {{aggregateName}}EventHandler {
public {{handlerName}}({{aggregateName}}Repository {{lowerAggregate}}Repository, {{aggregateName}}EventProcessing
{{lowerAggregate}}EventProcessing) {
super({{lowerAggregate}}Repository, {{lowerAggregate}}EventProcessing);
}

@Override
public void handleEvent(Integer subscriberAggregateId, Event event) {
this.{{lowerAggregate}}EventProcessing.{{processMethod}}(subscriberAggregateId, ({{eventType}}) event);
}
}

{{/each}}