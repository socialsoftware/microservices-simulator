# Checkpoint A handoff

- **State:** complete
- **Outcome:** Java analysis maps supported aggregate-key command data flow to a zero-based Saga constructor argument index. Groovy analysis recovers one unambiguous Saga from the supported map-returning helper shape and preserves caller-expanded producer occurrence/property evidence. RemoveTournament and AddParticipant receive source/test-supported evidence only when their Tournament inputs refer to the exact same `createTournament` occurrence and `aggregateId`; existing type-only footprints remain unchanged.
- **Spec/plan coverage:** Checkpoint A only. No SetupPlan, package, runtime, executor, scheduler, or cap behavior was implemented.
- **Actual files changed:** `StepDispatchFootprint.java`; `ApplicationAnalysisState.java`; `GroovyTraceArgument.java`; new `GroovySourceValueReference.java`, `SourceAggregateKeyInputEvidence.java`, and `SourceSupportedSagaPairEvidence.java`; `GroovyConstructorInputTraceVisitor.java`; `WorkflowFunctionalityVisitor.java`; focused state/visitor specs; new `SourceDerivedSharedSagaWorkloadAnalysisSpec.groovy`.
- **Discovery/classification:** Exact Java declaration and constructor-entry-path resolution was necessary autonomous detail. Unresolved, overloaded, conflicting, transformed, or cyclic paths fail closed. Nested Groovy wrapper calls preserve the deepest producer/property while rebasing occurrence identity through each caller. No material scope delta was found.
- **Proof:** `cd verifiers && mvn -q -Dtest=WorkflowFunctionalityVisitorSpec,GroovyConstructorInputTraceVisitorSpec,ApplicationAnalysisStateSpec,SourceDerivedSharedSagaWorkloadAnalysisSpec test` passed 49 tests. `git diff --check` passed. Continuing reviewer passed on review pass 3 after two bounded fixes.
- **Plain-language evidence:** the unmodified ordinary Quizzes test yields five Remove and four Add inputs; all three Tournament dispatches map to constructor argument 1; both target argument-1 records point to the one `createTournament` call at the setup caller occurrence and property `aggregateId`. Separate equal-looking calls remain distinct. Ambiguous maps, helper cycles, and ambiguous Java constructor paths remain blocked.
- **Known exclusions:** ordered setup persistence and execution remain Checkpoints B and C; harmful/control execution remains Checkpoint D.
- **User-visible try:** run the focused command above and inspect `SourceDerivedSharedSagaWorkloadAnalysisSpec` assertions.
- **Reasonable veto points:** source/test support is additive evidence and does not replace or reorder weak type-only candidates.
