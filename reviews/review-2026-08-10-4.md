# Artifacts Review — 2026-08-10

**Date:** 2026-08-10
**Skill files reviewed:** 13
**Scaffold templates reviewed:** 9
**Agent contract files reviewed:** 1
**Doc files reviewed:** 12 (11 under `docs/` + `AGENTS.md`)
**Verdict:** Issues requiring action

> **Clean** = all alignment checks pass; only improvement opportunities found.
> **Minor issues** = inconsistencies present but none cause incorrect agent output.
> **Issues requiring action** = any inconsistency that would cause an agent to produce
>   incorrect output.

---

## Executive Summary

One Critical: `session-c.md` and `session-d.md` write the published-event class path as
`{src}events/{Event}.java`, while `classify-and-plan/SKILL.md` defines `{Event}` as the event name
**with the `Event` suffix stripped** and mandates `events/{Event}Event.java`. A session-`c` agent
following its own sub-file emits a class named without the `Event` suffix, which the plan.md file
list, the subscription class name and `aggregate-grouping` §4 all assume is present. Everything else
holds: all P1–P4 definitions agree across their six locations, all of R1–R8 are stated in their
expected doc and session file, every literal `docs/` / `.claude/` / `AGENTS.md` path resolves, every
symbol the scaffold templates are claimed to ship is present, and the neutral-domain scan is clean
over 1247 added lines. Fix the `{Event}` collision before the next session `c` or `d` runs.

---

## Check 1 — Path Validity

Every literal path of the form `docs/…`, `.claude/skills/…`, `.claude/agents/…` and every reference
to `AGENTS.md` was resolved against disk by script (all `*.md` and `*.template` under the three
trees, plus `AGENTS.md`, excluding `docs/reviews/`).

| Source file | Referenced path | Status | Notes |
|-------------|----------------|--------|-------|
| all artifacts | every literal `docs/**`, `.claude/skills/**`, `.claude/agents/**` reference | Exists | 0 broken references |
| `AGENTS.md:74` | `simulator/AGENTS.md` | Exists | 2.2K |
| `.claude/skills/boot-strap/SKILL.md:222` | `simulator/src/main/resources/application-test.properties` | Exists | the `@PropertySource` claim holds |
| `_shared/session-completion.md:175` | `simulator/path/to/ChangedFile.java` | Template | illustrative heading inside the retro template, not a reference |
| `classify-and-plan/SKILL.md:21` | `applications/my-app/my-app-domain-model.md` | Template | not a finding — `my-app` is the harness-wide neutral example name, fixed by `_shared/conventions.md` § "Resolve app context" and `templates/README.md`; line 22 gives the `{app-name}` form alongside it |
| `review-artifacts/SKILL.md:245` | `applications/{APP}/plan.md` | Template | Python f-string inside the Check 4 script; `APP` is a variable |
| all artifacts | every other `applications/…` reference | Template | all written `applications/{app-name}/…`; layout consistent with `docs/workflow.md` § Conventions |

No literal application name is hard-coded anywhere in a skill or doc.

### Scaffold template contents

| Template | Symbol claimed | Claimed by | Present? | Notes |
|----------|---------------|------------|----------|-------|
| `AppSpockTest.groovy.template` | `// Domain constants are added here` marker | `boot-strap/SKILL.md:88`, `session-a.md:290` | Yes | line 16 |
| `AppSpockTest.groovy.template` | `// Domain @Autowired fields are added here` marker | `session-a.md:294` | Yes | line 25 |
| `AppSpockTest.groovy.template` | `// Domain imports` marker | `session-a.md:297` | Yes | line 12 |
| `AppSpockTest.groovy.template` | `// Domain create* helpers` marker | `boot-strap/SKILL.md:88` | Yes | line 53 |
| `AppSpockTest.groovy.template` | `flushAndClear()` helper (`flush()` then `clear()`) | `testing.md:206` | Yes | lines 48–51 |
| `AppSpockTest.groovy.template` | `loadForCheck(aggregateId, type)` helper | `testing.md:318` | Yes | lines 39–42 |
| `AppSpockTest.groovy.template` | `sagaStateOf(aggregateId)` helper | `testing.md` § T4, § Compensation Test | Yes | lines 33–37 |
| `AppSpockTest.groovy.template` | `loadBehaviorScripts()` helper | `testing.md:517`, `session-c.md` | Yes | lines 27–31 |
| `AppSpockTest.groovy.template` | `impairmentService` autowired field | `testing.md:516` | Yes | lines 18–19 |
| `AppSpockTest.groovy.template` | `commandGateway` **absent** | `testing.md:440` | Yes (absent) | claim holds — neither base class declares it |
| `AppSpockTest.groovy.template` | `aggregateIdGeneratorService` **absent** | `session-b.md:220` | Yes (absent) | claim holds — session `b` adds it |
| `BeanConfigurationSagas.groovy.template` | `ImpairmentService` bean | `testing.md:515` | Yes | line 65 |
| `BeanConfigurationSagas.groovy.template` | two `CommandGateway` implementations | `testing.md:448` | Yes | `commandGateway` (line 85) + `streamCommandGateway` (line 102) — the ambiguity claim holds |
| `BeanConfigurationSagas.groovy.template` | `@PropertySource("classpath:application-test.properties")` | `boot-strap/SKILL.md:222` | Yes | line 32 |
| `BeanConfigurationSagas.groovy.template` | `// Domain beans are added below` marker | `boot-strap/SKILL.md:88` | Yes | line 127 |
| `AppErrorMessage.java.template` | `PLACEHOLDER = "placeholder"` | `templates/README.md:28` | Yes | line 29 |
| `AppException.java.template` | `{{appClass}}` is the only consumer of that token | `templates/README.md:22` | Yes | verified: token appears in no other template |
| `AppSimulator.java.template` | `@EnableJpaRepositories` / `@EntityScan` over `{pkg}` and `ms` | `boot-strap/SKILL.md:220` | Yes | lines 16–23 |
| `SpockTest.groovy.template` | package `pt.ulisboa.tecnico.socialsoftware` (parent, not `{pkg}`) | `boot-strap/SKILL.md:104`, `workflow.md:53` | Yes | line 1 |
| `pom.xml.template` | surefire include `**/{pkg}/sagas/**` | `boot-strap/SKILL.md:190` | Yes | line 78 |
| `pom.xml.template` | `test-sagas` profile | `AGENTS.md` § Build Commands | Yes | lines 52–84 |
| `application-test.yaml.template` | `local.messaging.serialize: true` | `commands.md:34`, `testing.md:562` | Yes | line 34 |

No undocumented helper found on the scaffold: every method and field `AppSpockTest.groovy.template`
ships is named by some doc or skill.

---

## Check 2 — Pattern Alignment (P1–P4 and R1–R8)

### P1–P4 Consistency

Authoritative one-liners, `docs/concepts/rule-enforcement-patterns.md` § Quick Reference:
**P1** = single-aggregate state rule, `verifyInvariants()`, strong. **P2** = cache state from another
aggregate, `getEventSubscriptions()` + handler chain, eventual, never blocks. **P3** = synchronous
service-level check (own-table uniqueness OR saga-assembled DTO validation), service guard, strong.
**P4a** = precondition implicit in a saga fetch query, no code. **P4b** = invariant holds because the
saga passes the same value to two aggregates, no code.

| Location | Pattern | Claim | Authoritative definition | Verdict |
|----------|---------|-------|--------------------------|---------|
| `architecture.md:263-269` pattern selection table | P1/P2/P3/P4a/P4b | "Always true within one aggregate; derivable from its own fields" / "Cross-aggregate; eventual consistency is acceptable" / "Synchronous service-level check (own-table uniqueness OR saga-assembled DTO field validation)" / "Precondition implicit in saga fetch / same value to two aggregates" | as above | Consistent |
| `architecture.md:116-147` Request Lifecycle | P1/P2/P3 | `[P3]` service guard inside `@Transactional(SERIALIZABLE)` before mutation → `[P1] verifyInvariants()` at UoW commit → `[P2]` async EventProcessing caches publisher state | matches § Enforcement Lifecycle (`rule-enforcement-patterns.md:344-372`) | Consistent |
| `classify-and-plan/SKILL.md:211-273` Step 4 | P1–P4a/b | delegates to the doc's Decision Guide by name; pseudocode order P1 → P4a → P4b → P3 → P2 | flowchart order P1 → P4a → P4b → P3 → P2 | Consistent — and the file states explicitly that the keyword proxies are parse heuristics and "where proxy and guide disagree the guide wins" |
| `session-a.md:62` | P1 | `verifyInvariants()` enforces the plan.md P1 rules; reads only fields held by the aggregate, never a repository (R6) | P1 + R6 | Consistent |
| `session-c.md:67-68` | P3 | own-table uniqueness guards and saga-assembled DTO field checks, both in `*Service.java`, before mutation | P3 both sub-cases | Consistent |
| `session-d.md:138` | P2 | invariant check happens inside the Functionalities update method; `registerChanged` invariant-checks the cached-field update | P2 caches only; never blocks | Consistent |

### R1–R8 Coverage

Authoritative one-liners from `docs/architecture.md` § Architectural Restrictions.

| Restriction | Expected location | Mentioned? | Consistent? | Notes |
|-------------|------------------|------------|-------------|-------|
| R1 — service loads only own aggregate | `service.md`, session-b, session-c | Yes | Yes | `service.md:48`; `session-b.md:60`; `session-c.md:64` |
| R2 — service injects only own components | `service.md`, session-b, session-c | Yes | Yes | `service.md:46`, closed five-item list at `service.md:43`; `session-b.md:171-176`; `session-c.md:300-304` |
| R3 — cross-aggregate flow via DTOs only | `service.md`, session-b, session-c | Yes | Yes | `service.md:52`; `session-b.md:176`; `session-c.md:64,304` |
| R4 — saga steps declare lock intent | `sagas.md`, session-c | Yes | Yes | `sagas.md` § R4 Decision Table (three cases: primary / foreign / newly created) matches `architecture.md:187-198` including the create carve-out; `session-c.md:120` defers to it |
| R5 — `getEventSubscriptions()` in consumer only | `events.md`, session-d | Yes | Yes | `events.md:61`; `session-d.md:44,48-50`; also `aggregate.md:31` |
| R6 — `verifyInvariants()` must not read from DB | `aggregate.md`, session-a | Yes | Yes | `aggregate.md:26-29`; `session-a.md:62` |
| R7 — DTOs are immutable value objects | `service.md`, session-b, session-c | Yes | Yes | `service.md` § DTO Immutability (R7); `session-b.md:101-102`; `session-c.md:21` |
| R8 — functionalities only send commands upstream | `commands.md`, session-b, session-c | Yes | Yes | `commands.md:155`; `sagas.md:433`; `session-b.md:99-100`; `session-c.md:120` |

---

## Check 3 — Improvement Opportunities

### Missing Examples

| Doc | Section | Why an example would help |
|-----|---------|---------------------------|
| `docs/architecture.md` | R4 (and R1–R3, R5–R6) | R7 and R8 each carry a WRONG/RIGHT Java pair; the other six restrictions carry prose only. R4 is the strongest candidate for the next pair — it is the one restriction with three distinct correct shapes (semantic lock / `forbiddenStates` / neither), and a reader currently has to leave for `sagas.md` § R4 Decision Table to see any of them |
| `docs/concepts/testing.md` | § T3, "reflects event — carve-out" | The sanctioned form (payload assertion **plus** cached-publisher-version advance) is stated in prose in both `testing.md:59` and `session-d.md:191-206`, but no tier shows it as Groovy. The `versionBefore` capture point is the part agents get wrong, and it is exactly what a four-line snippet would pin |
| `docs/concepts/rule-enforcement-patterns.md` | § P2 | The section shows a `getEventSubscriptions()` fragment "to make the direction concrete", then points at `events.md` for wiring. No example shows the classification failure it is guarding against — a P2 rule that also wants to block, which § Step 3 describes only in prose and in the P2→P3 rewrite above it |

### Ambiguous Guidance

A scripted scan of all `.claude/skills/**` and `.claude/agents/**` markdown for "depends on",
"may be", "if applicable", "as appropriate", "as needed", "if necessary" returned 22 hits, every one
of which is either descriptive prose or immediately followed by a concrete criterion (e.g.
`classify-and-plan/SKILL.md:181` "depends on one thing only:" followed by the subscribes-to-events
test). No finding from that scan.

| File | Step | Ambiguous phrase | Missing decision criterion |
|------|------|------------------|---------------------------|
| `.claude/skills/implement-aggregate/session-d.md` | § "ByEvent methods in Functionalities" | line 142 scopes the mandate to "every event that **mirrors an operation also exposed as a saga `Functionalities` method**"; line 144 then states the method "is **always** new — one per event" | Whether an event mirroring **no** saga operation still needs its own `{operation}ByEvent` method. `classify-and-plan/SKILL.md:565` and line 144 both say yes unconditionally; the section's opening sentence (inherited from `events.md:260`) implies no. Self-resolving for an agent reading in order, hence Minor |
| `.claude/skills/implement-aggregate-full/SKILL.md` | § "Slice brief", session-`b` row of the `-Dtest` table | `{Query}Test` (T4) for this slice's read op | An aggregate whose plan.md reads "Read functionalities: none" produces no `{Query}Test` at all (`session-b.md:9`), so the manager has no value to substitute — and the skill states that "a slice that receives an unfilled placeholder cannot do its job" |

---

## Check 4 — Neutral Domain

**Run in progress:** quizzes-full-2
**Base commit:** `0f22736c45d001f314442385ef4007d96ec8afee` (derived by `git merge-base HEAD master`; the `harness:`-commit fallback was not needed)
**Forbidden nouns:** Course, Execution, Question, Quiz, QuizAnswer, Topic, Tournament, User
**Added lines scanned:** 1247

| File | Added line | Noun | Verdict |
|------|-----------|------|---------|
| — | — | — | nothing to report |

**Violation: 0. Moved: 0. False positive: 0.** Zero raw hits over 1247 added lines across
`docs/`, `.claude/skills/`, `.claude/agents/` and `AGENTS.md` (with `docs/reviews/` excluded from the
pathspec). Clean run — the harness delta of this branch introduces none of this application's domain
nouns, not even ones that would have cleared the move-test.

---

## Action Items

| Priority | Category | File | Finding | Suggested Fix |
|----------|---------|------|---------|---------------|
| Critical | placeholder collision | `.claude/skills/implement-aggregate/session-c.md:264`, `.claude/skills/implement-aggregate/session-d.md:35` | Both write the published-event class path as `` `{src}events/{Event}.java` ``. `classify-and-plan/SKILL.md:586-589` is authoritative for the token and defines it as *"event name **without** the `Event` suffix (e.g., `UpdateWarehouseName` for `UpdateWarehouseNameEvent`) … the event class itself is `events/{Event}Event.java`, which restores the suffix"*, and `classify-and-plan/SKILL.md:557` emits `` `events/{Event}Event.java` `` into the session-`c` file list. A session-`c` agent following its own sub-file writes `events/UpdateWarehouseName.java`, contradicting the plan.md row it is producing against, `aggregate-grouping` §4 (which names events **with** the suffix), and the sibling `notification/subscribe/{Aggregate}Subscribes{Event}.java` path in the same session-`d` file that correctly uses the bare token | Change both paths to `` `{src}events/{Event}Event.java` ``. Do not redefine `{Event}` — `session-d.md:46` depends on the suffix-less meaning for the subscription class name |
| Major | slice brief | `.claude/skills/implement-aggregate-full/SKILL.md:225-233` | The `-Dtest` table's session-`b` row names `{Query}Test`, which does not exist for an aggregate with no read functionality (`session-b.md:9` says such a session produces "no `{Query}Test.groovy`"). The manager must invent a fallback for a brief field the skill declares fatal if unfilled | Add to the table row: *"omit when the aggregate has no read functionality — `{Aggregate}ServiceTest` alone is the slice's verification"*, mirroring the session-`c` row's existing `{Operation}CompensationTest` conditional |
| Minor | harness-delta command | `AGENTS.md:21` | *"the harness delta of a run is exactly `git log --oneline docs/ .claude/`"* omits `AGENTS.md` itself, which Step 1.b of `review-artifacts` and Step 3.a of `harness-retrospective` both treat as part of the harness (a `Type 1` row may name it, and `harness-retrospective/SKILL.md:122` uses `-- docs .claude/skills .claude/agents AGENTS.md`). A `harness:` commit touching only `AGENTS.md` is invisible to the command `AGENTS.md` itself prescribes | Change to `git log --oneline docs/ .claude/ AGENTS.md` |
| Minor | ByEvent scope | `.claude/skills/implement-aggregate/session-d.md:142`, `docs/concepts/events.md:260` | The section opens by scoping the ByEvent mandate to events that mirror a saga `Functionalities` method, then states two lines later that the method is "always new — one per event". `classify-and-plan/SKILL.md:565` is unconditional | Drop the mirroring clause from the opening sentence in `events.md` § ByEvent sagaState guard (the owning section) and let `session-d.md` inherit; the mirroring case is what motivates the *guard*, not what scopes the *method* |
| Minor | undefined constant | `docs/concepts/testing.md:244` | The T2 template comment says *"call with `NONEXISTENT_AGGREGATE_ID`"*. That constant is named nowhere else in the harness: the scaffold's `AppSpockTest.groovy.template` does not ship it, and `session-b.md` § Not-found tests does not tell the agent to add it | Either add it to `AppSpockTest.groovy.template` as an infrastructure constant (it is aggregate-neutral, unlike the `{AGGREGATE}_{FIELD}` constants session `a` appends), or replace the reference with "a non-existent aggregate id" |
| Minor | missing example | `docs/architecture.md` § R4 | R7 and R8 carry WRONG/RIGHT pairs; R4 — the restriction with three distinct correct shapes — carries none | Add a WRONG/RIGHT pair contrasting `setForbiddenStates` used for primary-aggregate lock acquisition (wrong) against `SagaCommand` + `setSemanticLock` (right), which is the confusion `sagas.md` § R4 Decision Table's rule-of-thumb exists to settle |
| Minor | missing example | `docs/concepts/testing.md` § T3 | The re-affirming-payload carve-out is prose-only in both `testing.md` and `session-d.md`; the `versionBefore` capture point is the failure mode | Add a four-line Groovy snippet showing `versionBefore` captured after setup and the paired `versionAfter > versionBefore` assertion |

**Critical** = causes an agent to produce incorrect output (wrong code, missing file, wrong beans,
  wrong exception type, broken skill delegation).
**Major** = leaves an agent to infer or guess something the harness should have stated.
**Minor** = cosmetic inconsistency, missing example, ambiguous wording with low impact.
