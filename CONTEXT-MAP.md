# Context Map

This repository does not use a root `CONTEXT.md` glossary.

Canonical verifier context:

- Verifier, fault-analysis scenario generation, dynamic enrichment, execution, impact, and terminology:
  `docs/verifiers-impl/current-state.md`

Agents and `sp-*` skills should update the canonical handbook above instead of creating a root `CONTEXT.md` or a parallel verifier glossary.
