# Artifacts Review — 2026-08-08

**Date:** 2026-08-08
**Skill files reviewed:** 13 (`.claude/skills/**/*.md`)
**Scaffold templates reviewed:** 9 (`.claude/skills/boot-strap/templates/*.template`)
**Doc files reviewed:** 11 (`docs/**/*.md`, excluding the generated `docs/reviews/`)
**Verdict:** Issues requiring action

> **Clean** = all alignment checks pass; only improvement opportunities found.
> **Minor issues** = inconsistencies present but none cause incorrect agent output.
> **Issues requiring action** = any inconsistency that would cause an agent to produce
>   incorrect output.

---

## Executive Summary

Three Critical contradictions, all of the same shape: a doc that was authoritative earlier in the
run has been superseded by a later Type 1 fix or by a section elsewhere, and the superseded text was
never removed. The most consequential is `docs/concepts/aggregate.md` § Repositories, which still
prescribes `{Aggregate}Repository extends AggregateRepository` — exactly the polymorphic typing that
`harness:` commit `a13c548` (2026-08-08) reverted away from in `session-a.md`, with a stated
`ClassCastException` failure mode. `docs/concepts/rule-enforcement-patterns.md` § P2 carries a
complete event-wiring recipe that contradicts `docs/concepts/events.md` — the file that declares
itself the single canonical source for that wiring — in three separate places. Path validity, the
scaffold-template claims, P1–P4 alignment and R1–R8 coverage are otherwise clean, and the
neutral-domain check is clean at 758 scanned lines. Act on the three Critical rows before starting
aggregate 9 or the next application.

---

## Check 1 — Path Validity

Every literal `docs/...`, `.claude/...` path mentioned across `docs/**` and `.claude/skills/**` was
extracted and existence-checked. 52 distinct path references; all resolve. Template-pattern paths
under `applications/{app-name}/` were checked for shape, not existence.

| Source file | Referenced path | Status | Notes |
|-------------|----------------|--------|-------|
| `docs/workflow.md:226`, `_shared/conventions.md:171`, `implement-aggregate-full/SKILL.md:222` | `.claude/agents/aggregate-slice.md` | Exists | Outside the Step 1.b `find` (which scans `.claude/skills`), present on disk |
| all skills | `.claude/skills/_shared/conventions.md` (31 refs) | Exists | — |
| all skills | `.claude/skills/_shared/session-completion.md` (8 refs) | Exists | — |
| `docs/workflow.md`, `implement-aggregate/SKILL.md` | `.claude/skills/implement-aggregate/session-{a,b,c,d}.md` | Exist | — |
| `implement-aggregate-full/SKILL.md:200` | `.claude/skills/implement-aggregate/session-{type}.md` | Template | Resolves to the four sub-files above |
| `review-artifacts/SKILL.md:116` | `.claude/skills/boot-strap/templates/README.md` | Exists | — |
| `harness-retrospective/SKILL.md:273` | `.claude/skills/review-artifacts/SKILL.md` | Exists | — |
| every concept doc cross-link | `docs/concepts/*.md` (7 files) | Exist | — |
| `classify-and-plan/SKILL.md:21` | `applications/my-app/my-app-domain-model.md`, `…-aggregate-grouping.md` | Literal app name | Illustrative invocation example; `my-app` is the neutral placeholder used throughout `conventions.md` § "Resolve app context", and line 22 gives the `{app-name}` form immediately below. Not the current run's app. See Minor row A1. |
| all `applications/{app-name}/…` (26 distinct) | — | Template | Shapes match `docs/workflow.md` § Package layout |

No broken references.

### Scaffold template contents

| Template | Symbol claimed | Claimed by | Present? | Notes |
|----------|---------------|------------|----------|-------|
| all 9 | the nine-template list | `boot-strap/SKILL.md:47-57` | Yes | Directory holds exactly those 9 plus `README.md` |
| `AppSimulator.java.template` | package `…{{PKG}}`; `@EnableJpaRepositories`/`@EntityScan` over `…{pkg}` **and** `…ms` | `boot-strap/SKILL.md:194` | Yes | — |
| `AppException/AppErrorMessage.java.template` | package `…{{PKG}}.microservices.exception` | `boot-strap/SKILL.md:98-99` | Yes | — |
| `SpockTest.groovy.template` | package `pt.ulisboa.tecnico.socialsoftware` (parent, not `{pkg}`) | `boot-strap/SKILL.md:104-105`, `docs/workflow.md:53` | Yes | — |
| `AppException.java.template` | sole user of `{{appClass}}` | `templates/README.md:22` | Yes | Verified: no other template contains `{{appClass}}` |
| `pom.xml.template` | surefire include `**/{pkg}/sagas/**` | `boot-strap/SKILL.md:164` | Yes | `**/{{PKG}}/sagas/**` |
| `BeanConfigurationSagas.groovy.template` | `@PropertySource("classpath:application-test.properties")` | `boot-strap/SKILL.md:196` | Yes | — |
| `BeanConfigurationSagas.groovy.template` | marker `// Domain beans are added below…`, `// Domain imports…` | `boot-strap/SKILL.md:88-90` | Yes | 2 markers |
| `AppSpockTest.groovy.template` | markers `// Domain constants…`, `// Domain @Autowired fields…`, `// Domain imports`, `// Domain create* helpers…` | `session-a.md:288-297`, `boot-strap/SKILL.md:88-90` | Yes | 4 markers |
| `AppSpockTest.groovy.template` | `impairmentService` field + `loadBehaviorScripts()` helper | `testing.md:499-505` | Yes | — |
| `AppSpockTest.groovy.template` | `sagaStateOf(Integer)` | `testing.md:388, 481` | Yes | — |
| `AppSpockTest.groovy.template` | `loadForCheck(aggregateId, type)` | `testing.md:302-313` | Yes | — |
| `AppSpockTest.groovy.template` | `flushAndClear()` = `EntityManager.flush()` then `clear()` | `testing.md:204-205` | Yes | — |
| `AppSpockTest.groovy.template` | does **not** declare `commandGateway` | `testing.md:425` | Yes (absent, as claimed) | `SpockTest.groovy.template` is likewise empty |
| `AppSpockTest.groovy.template` | does **not** declare `aggregateIdGeneratorService` | `session-b.md:220-221` | Yes (absent, as claimed) | — |
| `BeanConfigurationSagas.groovy.template` | more than one `CommandGateway` implementation registered | `testing.md:433-435` | Yes | `LocalCommandGateway` + mocked `StreamCommandGateway` |
| `AppSpockTest.groovy.template` | `public static final String mavenBaseDir` | — | Undocumented | No doc or skill mentions it; `loadBehaviorScripts()` shadows it with an identical local. Minor row A2. |
| `AppErrorMessage.java.template` | `PLACEHOLDER = "placeholder"` | — | Undocumented | No artifact says what it is for or whether a session may remove it. Minor row A3. |

---

## Check 2 — Pattern Alignment (P1–P4 and R1–R8)

### P1–P4 Consistency

Authoritative one-liners, from `docs/concepts/rule-enforcement-patterns.md` § Quick Reference:

- **P1** — single-aggregate state rule, enforced in `verifyInvariants()`; strong consistency.
- **P2** — cache state from another aggregate via `getEventSubscriptions()` + handler chain; eventual; never blocks.
- **P3** — synchronous service guard in `*Service.java` (own-table uniqueness **or** saga-assembled DTO validation), inside `@Transactional(SERIALIZABLE)`; strong.
- **P4a** — precondition implicit in a saga fetch query; no enforcement code.
- **P4b** — invariant holds because the saga passes the same value to two aggregates; no enforcement code.

| Location | Pattern | Claim | Authoritative definition | Verdict |
|----------|---------|-------|--------------------------|---------|
| `architecture.md:236-242` — pattern selection table | P1/P2/P3/P4a/P4b | Verbatim match on all five rows | as above | Consistent |
| `architecture.md:116-147` — Request Lifecycle | P3 | "own-table reads, uniqueness checks, or DTO field validation; runs inside `@Transactional(SERIALIZABLE)`" | as above | Consistent |
| `architecture.md:116-147` — Request Lifecycle | P1 | "`verifyInvariants()` on each changed aggregate" at UoW commit | as above | Consistent |
| `architecture.md:116-147` — Request Lifecycle | P2 | "consumer aggregate caches publisher state", async ~1 s | as above | Consistent |
| `classify-and-plan/SKILL.md:211-217` — Step 4 | P1–P4a/b | Delegates to the doc's § Decision Guide, explicitly declining to re-derive; keyword matching declared "mechanical only" | as above | Consistent |
| `session-a.md:62` | P1 | "enforces all P1 rules … reads only fields already held by the aggregate — never a repository" | as above | Consistent |
| `session-c.md:67-68` | P3 | "P3 own-table uniqueness guards" / "P3 DTO field checks … receive the saga-assembled DTO as a parameter" | as above | Consistent |
| `session-d.md:133` | P2 | Invariant check inside the Functionalities ByEvent method; exception propagates and the event is not marked processed | as above | Consistent (this is consumer-commit behaviour, not blocking a caller's operation) |
| `rule-enforcement-patterns.md:201-259` — § P2 **implementation recipe** | P2 | Three code claims that contradict `events.md` | see Critical C2 | **Inconsistent** |

### R1–R8 Coverage

Authoritative one-liners from `docs/architecture.md` § Architectural Restrictions.

| Restriction | Expected location | Mentioned? | Consistent? | Notes |
|-------------|------------------|------------|-------------|-------|
| R1 — service loads only own aggregate | `service.md`, `session-b.md`, `session-c.md` | Yes — `service.md:46`, `session-b.md:173-176`, `session-c.md:64` | **No, in one place** | `session-b.md:60` instructs the read method to fetch a foreign DTO "via its service" — see Critical C3 |
| R2 — service injects only own components | same | Yes — `service.md:46`, `session-b.md:173-176`, `session-c.md:64,302-304` | Same exception | `session-c.md:302-304` restates the closed list correctly |
| R3 — cross-aggregate flow via DTOs only | same | Yes — `service.md:48`, `session-b.md:175`, `session-c.md:64` | Yes | — |
| R4 — saga steps declare lock intent | `sagas.md`, `session-c.md` | Yes — `sagas.md:99-107` (R4 Decision Table, 3 rows), `session-c.md:120` | Yes | `architecture.md:198` correctly names `sagas.md` § R4 Decision Table as authoritative |
| R5 — `getEventSubscriptions()` in consumer only | `events.md`, `session-d.md` | Yes — `events.md:61`, `session-d.md:44,48-50`, `aggregate.md:31` | Yes | — |
| R6 — `verifyInvariants()` must not read from DB | `aggregate.md`, `session-a.md` | Yes — `aggregate.md:26-29`, `session-a.md:62` | Yes | — |
| R7 — DTOs are immutable | `service.md`, `session-b.md`, `session-c.md` | Yes — `service.md:215-217`, `session-b.md:102`, `session-c.md:21` | Yes | — |
| R8 — functionalities send commands upstream only | `commands.md`, `session-b.md`, `session-c.md` | Yes — `commands.md:135`, `sagas.md:407`, `session-b.md:99-101`, `session-c.md:120` | Yes | — |

---

## Check 3 — Improvement Opportunities

### Missing Examples

| Doc | Section | Why an example would help |
|-----|---------|---------------------------|
| `.claude/skills/_shared/conventions.md` | § "Harness log" | The schema table is emitted with headers only. One filled row — including a `Closes row {N}.` example — would fix the `Problem` phrasing conventions that every session has to guess at, and would show what "keep rows thin" means concretely. |
| `docs/architecture.md` | § R7 — Aggregate DTOs must be immutable | The only R7 with no code at all, in either `architecture.md` or `service.md` § DTO Immutability. A two-line wrong/right pair (mutating a received DTO vs. extracting fields into a new command) would make the rule mechanical. |
| `docs/concepts/testing.md` | § T2 — Event Publication, negative case | The positive case is a full code block; the negative case is a two-line trailing comment. The `countBefore`/`countAfter` shape and where it goes relative to `given:` deserve the same treatment. |
| `docs/concepts/sagas.md` | § Step Ordering, item 1 (conditional validate-dates step) | The only step in the ordering list with no code shape. Its whole point is that it must be the *very first* step, which a snippet would pin. |
| `docs/concepts/rule-enforcement-patterns.md` | § Decision Guide Step 3 (P2 never blocks) | The re-classification advice is prose only; the P2→P3 rewrite it prescribes is exactly the transformation an agent gets wrong. |

### Ambiguous Guidance

| File | Step | Ambiguous phrase | Missing decision criterion |
|------|------|------------------|---------------------------|
| `.claude/skills/implement-aggregate/session-d.md` | § "When to skip the guard" | "Only when the event must apply even while the aggregate is mid-saga (**rare**)" | No test for which events those are. `events.md:292` carries the same wording and adds only one concrete carve-out (`copy.remove()` where a T3 test requires it). An agent facing a new event has no rule. |
| `docs/concepts/events.md` | § ByEvent sagaState guard | same phrase | same |
| `.claude/skills/_shared/conventions.md` | § "Resolve app context" | "Use the **first** result" of `find applications -name plan.md` | `find` order is filesystem-dependent. `implement-aggregate/SKILL.md` § 1.a supplies local disambiguation, but `review-artifacts` and `harness-retrospective` both inherit the bare rule and would silently pick an arbitrary app if a second one is ever added. |
| `.claude/skills/_shared/conventions.md` | § "Run the test suite" | "Run the build with no pipe, then read the exit status **on the following line:**" | The promised code block is two paragraphs further down, after the redirect-to-file digression. The dangling colon reads as if the block were missing. |
| `.claude/skills/implement-aggregate/session-c.md` | § "One `{Op}CompensationTest.groovy`…" | "A create functionality **typically** has no compensation test" | Softens an applicability test that is otherwise crisp ("a `setSemanticLock` step with a dependent step registered after it"). The "typically" invites re-derivation of a rule already stated. |

---

## Check 4 — Neutral Domain

**Run in progress:** quizzes-full-2
**Base commit:** `0f22736c45d001f314442385ef4007d96ec8afee`, derived by `git merge-base HEAD master`
(non-degenerate: the run's harness commits are on branch `quizzes-full-2`, not on master, so the
fallback to "parent of the oldest `harness:` commit" was not needed)
**Added lines scanned:** 758 (over `docs` and `.claude/skills`, excluding the generated
`docs/reviews/` outputs, which the skill's input enumeration excludes)

**Forbidden nouns** (from `applications/quizzes-full-2/plan.md` `### {N}. {Aggregate}` headers):
Course, User, Topic, Execution, Question, Quiz, QuizAnswer, Tournament.

| File | Added line | Noun | Verdict |
|------|-----------|------|---------|
| — | — | — | nothing to report |

**Counts: 0 Violation / 0 Moved / 0 False positive.**

The unfiltered diff produces 40 whole-word matches, every one of them inside
`docs/reviews/review-2026-08-04.md`, `-2026-08-04-2.md`, `-2026-08-06.md` and `-2026-08-07.md` — the
"Forbidden nouns" line each of those reports prints about itself. Those files are generated outputs
of this skill, excluded from the input artifact set by § Step 1.b, so they are not violations. Across
the harness artifacts proper the noun set does not appear at all.

Per-file added-line distribution, for interpreting the zero:
`classify-and-plan/SKILL.md` 133, `session-c.md` 116, `testing.md` 91, `session-b.md` 80,
`session-a.md` 78, `rule-enforcement-patterns.md` 46, `review-artifacts/SKILL.md` 32, `sagas.md` 31,
`events.md` 30, `session-d.md` 25, `service.md` 24, `commands.md` 21,
`AppSpockTest.groovy.template` 13, `conventions.md` 12, `workflow.md` 10,
`implement-aggregate-full/SKILL.md` 9, `architecture.md` 5, `implement-aggregate/SKILL.md` 1,
`aggregate.md` 1.

That last figure is itself evidence for Critical C1 below: `docs/concepts/aggregate.md` received one
added line in the entire run — a `version: Integer → Long` correction — while `session-a.md` was
rewritten eight times, including the repository-typing reversal the doc never received.

---

## Action Items

| Priority | Category | File | Finding | Suggested Fix |
|----------|---------|------|---------|---------------|
| **Critical** | stale-doc-contradiction | `docs/concepts/aggregate.md` § Repositories (lines 91-127) | The doc still prescribes the polymorphic repository shape that `harness:` commit `a13c548` reverted away from. Doc: "`aggregate/{Aggregate}Repository.java` \| interface extending `AggregateRepository` (no type arguments)" and "`AggregateRepository` … is declared `interface AggregateRepository extends JpaRepository<Aggregate, Integer>`, so `{Aggregate}Repository` extends it bare." Skill (`session-a.md:186-190`): "**Type the repository against the concrete aggregate, never against the shared `AggregateRepository`.** … `Aggregate` uses `InheritanceType.TABLE_PER_CLASS`, so any query it inherits is polymorphic and unions every aggregate's table: `findAll()` would return foreign aggregates, and the consumer that casts them to its own type fails with a `ClassCastException`." `git show --stat a13c548` confirms `docs/concepts/aggregate.md` was not in that commit. A session-`a` agent that reads the concept doc before the skill (the Reads list sends it to `aggregate.md` § Repositories first) emits the reverted shape. | Rewrite § Repositories to `@Repository @Transactional interface {Aggregate}Repository extends JpaRepository<{Aggregate}, Integer>`, carry over `session-a.md`'s `TABLE_PER_CLASS`/`ClassCastException` rationale and the two redeclared methods (`findLastAggregateVersion`, `findTopByOrderByVersionDesc`, subquery staying `from Aggregate a2`), and delete the "extends it bare" paragraph. While there, confirm the `{Consumer}EventHandler` constructor parameter typing in `events.md:101-103` and `session-d.md:92` still holds under the concrete shape — that pairing is what `a13c548` was reverting, and it is the one claim this static review cannot settle without reading `simulator/`. |
| **Critical** | competing-variant | `docs/concepts/rule-enforcement-patterns.md` § P2 — Inter-Invariant (lines 201-259) | The § P2 "Implementation recipe" is a second, stale event-wiring recipe contradicting `docs/concepts/events.md`, which declares itself sole owner ("Use this exact pattern in all skills and implementations. All skill files must reference this snippet rather than defining their own variant"). Three contradictions: (1) **Step 1** overrides `subscribesEvent(Event)` to filter on id and liveness, but `events.md:73` states "In the **sagas profile** … `EventApplicationService.handleSubscribedEvent()` does **not** call `subscribesEvent()`. Any additional filtering … must be implemented in the service-layer ByEvent method", and `session-d.md:54,74` repeats "Do **not** attempt to move this check into a `subscribesEvent()` override". (2) **Step 3** passes `new {EventName}Handler(repository, {aggregate}EventProcessing)` — a handler class per event type — but `events.md:93` says "Use **one concrete `{Consumer}EventHandler` class** per consumer aggregate … Do **not** create a separate handler subclass per event type", and § Polling passes the shared autowired bean. (3) **Step 4** calls `{aggregate}Functionalities.handleExternalChange(...)`, a method name no other artifact knows; `events.md` § ByEvent sagaState guard and `session-d.md:137` make `{operation}ByEvent` mandatory. An agent following § P2 writes a filter that never runs, N surplus handler beans, and a method the EventProcessing contract does not name. | Delete the four-step recipe from § P2 and replace it with a pointer: "Implementation: `docs/concepts/events.md` § Canonical Wiring Snippet owns the P2 wiring end to end — subscription class, single-dispatcher handler, polling bean, EventProcessing and the ByEvent method." Keep § P2's § "Use when" and § "Upstream / Downstream" paragraphs, which are pattern semantics and are not duplicated elsewhere. |
| **Critical** | R1/R2-violation | `.claude/skills/implement-aggregate/session-b.md:60` | Under `### {Aggregate}Service.java (read methods)`: "If the read joins a foreign aggregate: fetch the foreign aggregate's DTO **via its service** and include in the response." An agent following this injects a foreign `*Service` into `{Aggregate}Service`, which `architecture.md` R2 forbids ("Injecting a foreign service class mixes transaction boundaries and bypasses the UoW") and which `session-b.md`'s own BeanConfiguration section (lines 173-176) forbids in the same file. The file also already carries the correct mechanism 33 lines later — § "Two-step read saga variant", where step 1 fetches the foreign DTO through a command sent upstream. | Replace the bullet with: "If the read needs data from a foreign aggregate, the service does **not** fetch it. The saga resolves it in a preceding step and passes the DTO in as a parameter — see § Two-step read saga variant below, and `docs/concepts/service.md` § Injected Dependencies (R1/R2/R3)." |
| **Major** | stale-justification | `.claude/skills/classify-and-plan/SKILL.md:186-192` (Step 3.d) | The rule that a subscribing single snapshot needs an owned-entity class is justified by: "The subscription class overrides `subscribesEvent(Event)` to filter on the snapshot's own id and liveness, and the harness standardises the thing it filters on as an owned entity." That override is inert in the sagas profile (`events.md:73`). The *conclusion* survives — the paragraph itself says "This is a uniformity rule, not a framework constraint" — but the stated reason is false, and a later agent reasoning from it will write the dead override. | Rewrite the justification without the override: the uniformity is so that every subscription, single or collection, is constructed from a reference object of the same shape, and so that the service-layer ByEvent discriminator has a stable thing to match on. Leave the "not a framework constraint" caveat and the `EventSubscription` constructor note as they are. |
| **Major** | canonical-snippet-gap | `docs/concepts/events.md:238-251` (§ Canonical Wiring Snippet → EventProcessing class) | The canonical snippet shows `<consumer>Functionalities.<updateMethod>(aggregateId, event.get<RelevantField>())` — no `ByEvent` suffix — while § "ByEvent sagaState guard" 6 lines later and `session-d.md:125` both make `{operation}ByEvent` mandatory, and the same doc explains that calling the saga `Functionalities` method from an event handler "creates a circular saga loop". A snippet that declares itself the exact pattern to copy should not elide the one naming rule that prevents the loop. | Change the snippet body to `{consumer}Functionalities.{operation}ByEvent(aggregateId, event.get<RelevantField>())` and add one line under it: "the `ByEvent` suffix is mandatory — see § ByEvent sagaState guard." |
| **Minor** | example-app-name (A1) | `.claude/skills/classify-and-plan/SKILL.md:21` | Two literal `applications/my-app/…` paths in the invocation examples. Not a leak: `my-app` is the neutral placeholder used in `conventions.md` § "Resolve app context" and `boot-strap/SKILL.md`, and the `{app-name}` form appears on the very next line. Recorded because Check 1 treats a literal application name as reportable. | Leave as-is, or drop the `my-app` line and keep only the `{app-name}` form. No action required. |
| **Minor** | undocumented-scaffold-symbol (A2) | `.claude/skills/boot-strap/templates/AppSpockTest.groovy.template` | `public static final String mavenBaseDir` is shipped but named by no doc or skill, and `loadBehaviorScripts()` immediately shadows it with an identical local `def mavenBaseDir`. Agents hand-roll the path rather than use it. | Either delete the class-level field and keep the local, or document it in `docs/concepts/testing.md` § Compensation Test as the resource-root handle. |
| **Minor** | undocumented-scaffold-symbol (A3) | `.claude/skills/boot-strap/templates/AppErrorMessage.java.template` | `PLACEHOLDER = "placeholder"` is shipped with no artifact explaining its purpose or whether a Phase 2 session may remove it. `session-a.md` and `session-c.md` both say only "append … do not remove existing constants", which locks it in permanently by default. | Add a one-line comment in the template (it keeps the file non-empty before any domain constant exists) or state in `templates/README.md` that it may be removed once the first domain constant lands. |
| **Minor** | incomplete-enumeration | `docs/workflow.md:85-91` (§ Test naming) | The table enumerates T1/T2/T3/T4-read/T4-write but omits `{Operation}CompensationTest.groovy`, which `testing.md` § T4 lists as core scope and `classify-and-plan` § Step 7 lists in the 2.N.c file row. | Add a row: `T4 Compensation \| {Operation}CompensationTest.groovy \| 2.N.c`. |
| **Minor** | incomplete-enumeration | `docs/concepts/testing.md:135-150` (§ Directory Layout) | The `sagas/coordination/<aggregate>/` node shows only `<FunctionalityName>Test.groovy (T4)`, while § "CRITICAL gotcha" 390 lines later insists the compensation test is "a sibling file in the same `sagas/coordination/<aggregate>/` package (not a separate `sagas/behaviour/` package)". The layout block is where an agent looks to place the file. | Add `<FunctionalityName>CompensationTest.groovy  (T4)` beneath it in the tree. |
| **Minor** | missing-example | `.claude/skills/_shared/conventions.md` § "Harness log" | See Check 3.a. | Add one filled example row plus one `Closes row {N}.` row. |
| **Minor** | missing-example | `docs/architecture.md` § R7 | See Check 3.a. | Add a wrong/right two-liner. |
| **Minor** | missing-example | `docs/concepts/testing.md` § T2 Event Publication | See Check 3.a. | Promote the negative-case comment to a code block. |
| **Minor** | missing-example | `docs/concepts/sagas.md` § Step Ordering item 1 | See Check 3.a. | Add the `validateDatesStep` snippet. |
| **Minor** | ambiguity | `session-d.md` / `events.md` § "When to skip the guard" | "rare", with no test for which events qualify. | State the test: skip the guard only when the cached field the event writes is one no saga step of this aggregate ever writes. |
| **Minor** | ambiguity | `.claude/skills/_shared/conventions.md` § "Resolve app context" | "Use the first result" — filesystem-order dependent for read-only skills that add no local disambiguation. | Add: "If `find` returns more than one and the referencing skill defines no local rule, halt and ask which application." |
| **Minor** | wording | `.claude/skills/_shared/conventions.md` § "Run the test suite" | "read the exit status on the following line:" dangles two paragraphs above its code block. | Move the redirect-to-file paragraph above the sentence, or change it to "read the exit status from the `MAVEN_EXIT` line in the block below". |
| **Minor** | wording | `.claude/skills/implement-aggregate/session-c.md` § compensation applicability | "typically" softens a crisp applicability test. | Drop "typically" from both sentences; the test above them already decides every case. |

**Critical** = causes an agent to produce incorrect output (wrong code, missing file, wrong beans,
  wrong exception type, broken skill delegation).
**Major** = leaves an agent to infer or guess something the harness should have stated.
**Minor** = cosmetic inconsistency, missing example, ambiguous wording with low impact.
