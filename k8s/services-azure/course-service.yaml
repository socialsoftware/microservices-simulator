apiVersion: v1
kind: Service
metadata:
  name: course-service
  namespace: microservices-simulator
spec:
  selector:
    app: course-service
  ports:
    - port: 8089
      targetPort: 8089
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: course-service
  namespace: microservices-simulator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: course-service
  template:
    metadata:
      labels:
        app: course-service
    spec:
      serviceAccountName: microservices-sa
      containers:
        - name: course-service
          image: simulatorcr.azurecr.io/quizzes:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8089
          resources:
            requests:
              memory: "256Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          env:
            - name: START_CLASS
              value: "pt.ulisboa.tecnico.socialsoftware.quizzes.microservices.course.CourseServiceApplication"
            - name: SPRING_PROFILES_ACTIVE
              value: "kubernetes,course-service,sagas,stream,distributed-version"
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgres:5432/coursedb"
            - name: SPRING_RABBITMQ_HOST
              value: "rabbitmq"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://jaeger:4317"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8089
            initialDelaySeconds: 60
            periodSeconds: 10
