# Artifacts Review — 2026-08-02

**Date:** 2026-08-02
**Skill files reviewed:** 13
**Doc files reviewed:** 11 (non-`docs/reviews/` files: `architecture.md`, `concepts/aggregate.md`, `concepts/commands.md`, `concepts/events.md`, `concepts/rule-enforcement-patterns.md`, `concepts/sagas.md`, `concepts/service.md`, `concepts/testing.md`, `templates/aggregate-grouping-template.md`, `templates/domain-model-template.md`, `workflow.md`)
**Verdict:** Clean

> **Clean** = all alignment checks pass; only improvement opportunities found.
> **Minor issues** = inconsistencies present but none cause incorrect agent output.
> **Issues requiring action** = any inconsistency that would cause an agent to produce
>   incorrect output.

---

## Executive Summary

No broken references, no P1–P4 or R1–R8 contradictions, and zero neutral-domain violations in the
live harness scope. The one recurring soft spot is that several `session-*.md` files implement an
R-restriction (R1–R3, R5–R8) without citing its number, relying instead on a pointer to the concept
doc that does cite it — functionally sound but worth tightening for traceability. No action required
before the next aggregate boundary.

---

## Check 1 — Path Validity

Every literal `docs/...` and `.claude/skills/...` reference inside the live artifact set (`docs/**`
excluding `docs/reviews/**`, and `.claude/skills/**`) resolves to a real file in the Step 1.b
enumeration. `.claude/agents/aggregate-slice.md`, referenced from `conventions.md`,
`implement-aggregate-full/SKILL.md` and `workflow.md`, is outside the `docs/**`/`.claude/skills/**`
scope this check covers, but exists on disk (confirmed by direct listing) so no reference is dangling.

| Source file | Referenced path | Status | Notes |
|-------------|----------------|--------|-------|
| `.claude/skills/_shared/session-completion.md` | `docs/concepts/aggregate.md` | Yes | — |
| `.claude/skills/classify-and-plan/SKILL.md` | `docs/concepts/rule-enforcement-patterns.md`, `docs/workflow.md` | Yes | — |
| `.claude/skills/implement-aggregate-full/SKILL.md` | `docs/workflow.md`, `.claude/skills/_shared/conventions.md`, `.claude/skills/_shared/session-completion.md`, `.claude/skills/classify-and-plan/SKILL.md`, `.claude/agents/aggregate-slice.md` | Yes | — |
| `.claude/skills/implement-aggregate/session-a.md` | `docs/concepts/aggregate.md`, `docs/concepts/service.md`, `docs/concepts/testing.md` | Yes | — |
| `.claude/skills/implement-aggregate/session-b.md` | `docs/architecture.md`, `docs/concepts/commands.md`, `docs/concepts/sagas.md`, `docs/concepts/service.md`, `docs/concepts/testing.md` | Yes | — |
| `.claude/skills/implement-aggregate/session-c.md` | `docs/concepts/commands.md`, `docs/concepts/sagas.md`, `docs/concepts/service.md`, `docs/concepts/testing.md`, `.claude/skills/_shared/session-completion.md` | Yes | — |
| `.claude/skills/implement-aggregate/session-d.md` | `docs/concepts/aggregate.md`, `docs/concepts/events.md`, `docs/concepts/testing.md` | Yes | — |
| `.claude/skills/implement-aggregate/SKILL.md` | `docs/concepts/testing.md`, `.claude/skills/_shared/conventions.md`, `.claude/skills/_shared/session-completion.md`, `.claude/skills/classify-and-plan/SKILL.md`, `.claude/skills/implement-aggregate-full/SKILL.md` | Yes | — |
| `.claude/skills/review-artifacts/SKILL.md` | `docs/architecture.md`, `docs/concepts/{aggregate,commands,events,rule-enforcement-patterns,sagas,service,testing}.md`, `docs/workflow.md`, `.claude/skills/_shared/conventions.md`, `.claude/skills/classify-and-plan/SKILL.md`, `.claude/skills/implement-aggregate/session-{a,c,d}.md` | Yes | — |
| `.claude/skills/harness-retrospective/SKILL.md` | `.claude/skills/_shared/conventions.md`, `.claude/skills/review-artifacts/SKILL.md` | Yes | — |
| `.claude/skills/boot-strap/SKILL.md`, `templates/README.md` | `.claude/skills/_shared/conventions.md` | Yes | — |
| `docs/concepts/testing.md` | `docs/concepts/sagas.md` | Yes | — |
| `docs/templates/aggregate-grouping-template.md` | `docs/concepts/events.md` | Yes | — |
| `docs/templates/domain-model-template.md` | `docs/concepts/rule-enforcement-patterns.md` | Yes | — |
| `docs/workflow.md` | `docs/concepts/rule-enforcement-patterns.md`, `docs/concepts/testing.md`, `.claude/skills/_shared/{conventions,session-completion}.md`, `.claude/skills/boot-strap/SKILL.md`, `.claude/skills/classify-and-plan/SKILL.md`, `.claude/skills/implement-aggregate/session-{a,b,c,d}.md`, `.claude/agents/aggregate-slice.md` | Yes | — |

`docs/reviews/*.md` files (historical review/retro reports) reference several retired skills
(`retro/SKILL.md`, `review-aggregate/SKILL.md`, `review-tests/SKILL.md`,
`adversarial-review-aggregate/SKILL.md`, `review-aggregate-vs-quizzes/SKILL.md`) that no longer exist
on disk. These are **not findings** — per this skill's own Step 1.b exclusion, generated review
reports are historical snapshots, not part of the live artifact set, and their references are frozen
at the time they were written.

`applications/{app-name}/...` template-pattern usage is consistent with `docs/workflow.md`'s package
layout everywhere it appears. The one literal `applications/my-app/` string
(`.claude/skills/classify-and-plan/SKILL.md`, Input § Examples) is an illustrative invocation example
using a generic placeholder name, not a leaked real application name — not a finding.

**Nothing else to report.**

---

## Check 2 — Pattern Alignment (P1–P4 and R1–R8)

### P1–P4 Consistency

Authoritative one-liners (`docs/concepts/rule-enforcement-patterns.md` § Quick Reference):
- **P1** — single-aggregate state rule → `verifyInvariants()` in aggregate. Strong.
- **P2** — cache state from another aggregate, no blocking → `getEventSubscriptions()` + event
  handler chain. Eventual.
- **P3** — synchronous service-level check (own-table uniqueness OR saga-assembled DTO validation)
  → service guard in `*Service.java`. Strong.
- **P4a** — precondition implicit in saga fetch query → no code, saga data-assembly step.
- **P4b** — invariant holds because the same value is passed to two aggregates in one saga → no
  code, saga construction.

| Location | Pattern | Claim | Authoritative definition | Verdict |
|----------|---------|-------|--------------------------|---------|
| `docs/architecture.md` — pattern selection table | P1/P2/P3/P4 | Matches the four one-liners verbatim in intent (P1 "always true within one aggregate"; P3 "synchronous service-level check (own-table uniqueness OR saga-assembled DTO field validation)"; P2 "cross-aggregate; eventual consistency acceptable"; P4a/b "precondition implicit in saga fetch / same value to two aggregates") | Consistent | Consistent |
| `docs/architecture.md` — Request Lifecycle | P1/P2/P3 | `[P3]` own-table/uniqueness/DTO validation inside `@Transactional(SERIALIZABLE)`; `[P1]` `verifyInvariants()` at commit; `[P2]` EventProcessing → Update Functionality caches publisher state | Consistent | Consistent |
| `.claude/skills/classify-and-plan/SKILL.md` — Step 4 | P1–P4a/b | Explicitly defers to `rule-enforcement-patterns.md` § Decision Guide ("do not re-derive the classification logic here"); parser heuristic mirrors the same order (same-aggregate → P4a/P4b → P3 → P2) | Consistent | Consistent |
| `.claude/skills/implement-aggregate/session-a.md` | P1 | "`verifyInvariants()`: enforces all P1 rules for this aggregate listed in plan.md" | Consistent | Consistent |
| `.claude/skills/implement-aggregate/session-c.md` | P3 | "P3 own-table uniqueness guards... P3 DTO field checks (if listed in plan.md cross-aggregate prerequisites)... § P3 Guard Placement" | Consistent | Consistent |
| `.claude/skills/implement-aggregate/session-d.md` | P2 | "P2 rule enforcement: The invariant check happens inside the Functionalities update method... If the invariant fails, the exception propagates and the event is not marked as processed" | Consistent | Consistent |

### R1–R8 Coverage

Authoritative one-liners (`docs/architecture.md` § Architectural Restrictions).

| Restriction | Expected location | Mentioned? | Consistent? | Notes |
|-------------|------------------|------------|-------------|-------|
| R1 — service loads only own aggregate type | `service.md`, session-b.md, session-c.md | `service.md` ✓ (cites R1/R2 directly); session-b.md ✓ (§ Update BeanConfigurationSagas.groovy: "a foreign service or foreign repository violates R1/R2"); session-c.md — not cited by number | Consistent where present | session-c.md relies on "follow `service.md` § Method Patterns exactly" rather than restating R1; functionally compliant, not a labeling gap that misleads |
| R2 — service injects only own components | same | same as R1 | Consistent where present | same as above |
| R3 — cross-aggregate via DTOs only | `service.md`, session-b.md, session-c.md | `service.md` ✓ (line 37); session-b.md / session-c.md — not cited by number, but both consistently pass DTOs (not aggregate instances) between saga steps and service methods | Functionally consistent | No contradicting text found; R3 is never violated in the skill instructions, only unlabeled |
| R4 — saga steps declare lock intent | `sagas.md`, session-c.md | `sagas.md` ✓ (§ R4 Decision Table, § Step Ordering); session-c.md ✓ (Reads list cites "§ R4 Decision Table"; Produce section restates the primary-vs-foreign distinction) | Consistent | Consistent |
| R5 — `getEventSubscriptions()` in consumer only | `events.md`, session-d.md | `events.md` ✓ (line 60); session-d.md — not cited by number, but the entire session is structured around "required ACTIVE guard" and consumer-only construction, matching R5's substance | Functionally consistent | Same unlabeled-but-compliant pattern |
| R6 — `verifyInvariants()` must not read DB | `aggregate.md`, session-a.md | `aggregate.md` ✓ (line 26); session-a.md — not cited by number, but explicitly instructs "does not call `verifyInvariants()`... P1 rules... Throws" without ever describing a DB read inside it | Functionally consistent | Same pattern |
| R7 — DTOs are immutable | `service.md`, session-b.md, session-c.md | `service.md` ✓ (§ DTO Immutability (R7)); session-c.md ✓ (Reads list cites "§ DTO Immutability (R7)"); session-b.md — not cited (session-b only reads DTOs, doesn't construct write-path DTO mutation logic) | Consistent | — |
| R8 — functionalities send commands upstream only | `commands.md`, `sagas.md`, session-b.md, session-c.md | `commands.md` ✓ (line 119); `sagas.md` ✓ (§ Step Ordering, "R8 — upstream-only commands"); session-c.md ✓ (Reads list cites "§ Step Ordering... R8 upstream-only rule"); session-b.md — not cited (session-b is read-only, no command-sending direction to violate) | Consistent | — |

**Pattern observed, not a defect:** several session sub-files implement an R-restriction correctly
but reach it by pointing at the concept doc that states the number, rather than restating the number
locally. This is consistent with this repo's stated design (concept docs are the single source of
truth; skills reference them). No case was found where a session file's actual instructions
contradict or omit the restriction's substance — only the R-label citation is sometimes absent.
Recorded as an improvement opportunity (see Check 3), not an issue requiring action.

---

## Check 3 — Improvement Opportunities

### Missing Examples

Nothing to report. Every algorithmic or pattern description in the concept docs
(`aggregate.md`, `commands.md`, `events.md`, `rule-enforcement-patterns.md`, `sagas.md`,
`service.md`, `testing.md`) is paired with at least one concrete code block, and cross-references
resolve to a template rather than a prose-only description.

### Ambiguous Guidance

Nothing to report. A scan for `depends on`, `may be`, `if applicable`, `as appropriate`, `as needed`
across `.claude/skills/**` surfaced only: (1) uses inside `review-artifacts/SKILL.md` itself (the
check's own instruction text, not guidance to an implementing agent); (2) the `> **Slice scope.** You
may be assigned...` boilerplate repeated in every `session-*.md`, which is not a decision point — it
is a scope statement resolved entirely by the manager's brief; (3) instances immediately followed by
a concrete resolution rule in the same sentence, e.g. `classify-and-plan/SKILL.md`:181 ("depends on
one thing only:" then states the exact criterion). No hit lacked a decision criterion.

One related observation from Check 2: the R1–R8 citation gap in several `session-*.md` files (above)
is a candidate for a light-touch fix — adding a one-word `(R3)` / `(R5)` / `(R6)` / `(R7)` / `(R8)`
parenthetical next to the relevant instruction in session-b.md/session-c.md/session-a.md/session-d.md
would close the traceability gap without restating the rule body. Not urgent; no agent output is
currently at risk.

---

## Check 4 — Neutral Domain

**Run in progress:** quizzes-full
**Base commit:** `908d121883a2512ab368c10a16213d9057bd5f80^` (derived via the merge-base fallback:
`git merge-base HEAD master` resolved to `HEAD` itself, since the run's harness edits are committed
on `master`, so the base was walked back to the parent of the oldest `harness:` commit reachable from
`HEAD` touching `docs`/`.claude/skills`)
**Added lines scanned:** 2870
**Forbidden nouns (from `applications/quizzes-full/plan.md` `### {N}. {Aggregate}` headers):** Course,
Execution, Question, Quiz, QuizAnswer, Topic, Tournament, User

| File | Added line | Noun | Verdict |
|------|-----------|------|---------|
| `.claude/skills/classify-and-plan/SKILL.md` (§ Step 8.5, slice ordering) | `...numbered in **execution order**. Execution order is not arbitrary: ...` | Execution | **False positive** — ordinary English "execution order" (of saga slices), not the Quiz app's `Execution` aggregate. No other forbidden noun appears nearby. |
| `docs/reviews/review-2026-08-01-2.md`, `review-2026-08-02.md`, `review-2026-08-02-2.md` (multiple table rows) | Quoted excerpts of `session-b.md`/`session-c.md`/`session-d.md`/`commands.md` lines naming `Course`, `Execution`, `Question`, `Quiz`, `Tournament` | all | **Not applicable** — these are inside `docs/reviews/**`, excluded from the artifact scope by this skill's own Step 1.b rule (generated reports are historical snapshots, not live harness content). They are earlier reviews' own analysis of a prior relocation, reproduced as report prose — not new guidance text introduced into a skill or doc. |

**Direct confirmation:** a fresh scan of the actual current content of the referenced live files
(`docs/concepts/commands.md`, `docs/concepts/events.md`, `docs/concepts/sagas.md`,
`.claude/skills/implement-aggregate/session-{a,b,c,d}.md`) for all eight forbidden nouns returns zero
matches. The domain-noun examples that earlier reviews flagged as neutrality debt
(`Course`/`Execution`/`Tournament`/`Question`/`Quiz`/`User` inside worked examples) have since been
rewritten in placeholder vocabulary — consistent with the two most recent harness commits
(`cd0f0aff`, `49ac9fb1`), which neutralized this exact debt.

**Violations: 0. Moved: 0 (nothing to reclassify as moved — no genuine hit existed). False positives: 1.**

---

## Action Items

Nothing at Critical or Major severity.

| Priority | Category | File | Finding | Suggested Fix |
|----------|---------|------|---------|---------------|
| Minor | Traceability | `.claude/skills/implement-aggregate/session-{a,b,c,d}.md` | R1, R2, R3, R5, R6, R7, R8 are correctly implemented but not cited by number in the session files where architecture.md expects them (only in the concept docs they point to) | Add a parenthetical `(R{n})` next to the relevant instruction sentence in each session file, without restating the rule body |

**Critical** = causes an agent to produce incorrect output (wrong code, missing file, wrong beans,
  wrong exception type, broken skill delegation).
**Major** = leaves an agent to infer or guess something the harness should have stated.
**Minor** = cosmetic inconsistency, missing example, ambiguous wording with low impact.
