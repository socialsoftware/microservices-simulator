# Artifacts Review — 2026-08-04

**Date:** 2026-08-04
**Skill files reviewed:** 13
**Doc files reviewed:** 11 (`docs/**/*.md` excluding the generated `docs/reviews/`)
**Verdict:** Minor issues

> **Clean** = all alignment checks pass; only improvement opportunities found.
> **Minor issues** = inconsistencies present but none cause incorrect agent output.
> **Issues requiring action** = any inconsistency that would cause an agent to produce
>   incorrect output.

---

## Executive Summary

Run at the aggregate-2 boundary, immediately after the harness edits that closed harness-log row 20 (commit `e54b1353`, domain sentinel constants). Path validity, P1-P4 alignment, R1-R8 coverage and the neutral-domain check are all clean across 361 added lines of this run's harness delta. Two Major findings, both introduced by `e54b1353` and both concerning the new shared `microservices/domain/` package: neither layout doc lists it, and `session-a.md` lacks the halt-on-absent-line instruction that its sibling `SagaState` section carries and that `classify-and-plan` § Step 8 assumes exists. Recommended next action: fix both before starting aggregate 3, since 2.3.a is the next session to read `session-a.md` § Produce.

---

## Check 1 — Path Validity

Every literal `docs/...` and `.claude/...` reference in all 24 artifact files was resolved against disk (regex sweep over `.md`, `.groovy`, `.java`, `.template` targets; template patterns containing `{` excluded).

| Source file | Referenced path | Status | Notes |
|-------------|----------------|--------|-------|
| all artifacts | all literal `docs/**` and `.claude/**` references | Valid | 0 broken references |
| `docs/workflow.md` | `.claude/agents/aggregate-slice.md` | Valid | exists (6.1K); outside the `find .claude/skills` set but a real file |
| `.claude/skills/implement-aggregate-full/SKILL.md` | `.claude/agents/aggregate-slice.md` | Valid | same file, cited as the slice return contract |
| all artifacts | `applications/**` | Template | 0 literal application names found; every reference uses `{app-name}` |

Nothing to report beyond the table.

---

## Check 2 — Pattern Alignment (P1–P4 and R1–R8)

### P1–P4 Consistency

Authoritative one-liners taken from `docs/concepts/rule-enforcement-patterns.md` § Quick Reference (lines 13-24).

| Location | Pattern | Claim | Authoritative definition | Verdict |
|----------|---------|-------|--------------------------|---------|
| `docs/architecture.md:233` — pattern selection table | P1 | "Always true within one aggregate; derivable from its own fields" | "Single-aggregate state rule (§3.1) → `verifyInvariants()`" | Consistent |
| `docs/architecture.md:234` | P3 | "Synchronous service-level check (own-table uniqueness OR saga-assembled DTO field validation)" | verbatim match with Quick Reference row 2 | Consistent |
| `docs/architecture.md:235` | P2 | "Cross-aggregate; eventual consistency is acceptable" | "Cache state from another aggregate (no blocking); Eventual" | Consistent |
| `docs/architecture.md:236` | P4a/P4b | "Precondition implicit in saga fetch / same value to two aggregates" | matches both Saga-Structural rows | Consistent |
| `docs/architecture.md:127-140` — Request Lifecycle | P1/P2/P3 | P3 at service guard, P1 at UoW commit, P2 in the async tail | matches the Implemented-by column | Consistent |
| `classify-and-plan/SKILL.md` Step 4 | P1–P4a/b | delegates: "That section is authoritative for what each pattern means and when it applies — do not re-derive the classification logic here" | n/a — pointer, not a restatement | Consistent (correct delegation) |
| `session-a.md:62` | P1 | "enforces all P1 rules for this aggregate listed in plan.md" via `verifyInvariants()` | matches | Consistent |
| `session-c.md:66-67` | P3 | own-table uniqueness guards and DTO field checks in the service | matches | Consistent |
| `session-d.md:130` | P2 | "invariant check happens inside the Functionalities update method … `verifyInvariants()` before committing" | matches P2 = cache + subscription chain | Consistent |

No drift. The one new claim added this run — `rule-enforcement-patterns.md` § P1 "Comparing against a fixed value" — introduces no pattern semantics; it names a file location only.

### R1–R8 Coverage

| Restriction | Expected location | Mentioned? | Consistent? | Notes |
|-------------|------------------|------------|-------------|-------|
| R1 — service loads only own aggregate | `service.md`, session-b, session-c | Yes | Yes | `service.md:46`, `session-b.md:168` |
| R2 — service injects only own components | `service.md`, session-b, session-c | Yes | Yes | closed dependency list at `service.md:43-44`, restated as a closed list in `session-b.md:166-171` |
| R3 — cross-aggregate flow via DTOs only | `service.md`, session-b, session-c | Partial | Yes | `service.md:48` only; neither session file names R3. Pre-existing, low impact — the sessions inherit it via the `service.md` read. |
| R4 — saga steps declare lock intent | `sagas.md`, session-c | Yes | Yes | § R4 Decision Table is authoritative; `architecture.md:193` defers to it explicitly |
| R5 — getEventSubscriptions in consumer only | `events.md`, session-d | Yes | Yes | `aggregate.md:31`, `events.md:60` |
| R6 — verifyInvariants must not read from DB | `aggregate.md`, session-a | Yes | Yes | `aggregate.md:26-29` |
| R7 — DTOs immutable | `service.md`, session-b, session-c | Yes | Yes | `service.md` § DTO Immutability (R7) |
| R8 — commands upstream only | `commands.md`, session-b, session-c | Yes | Yes | `commands.md:129`, `sagas.md:376` |

R3 is the only partial. It is not new and not a contradiction — the restriction is stated once, authoritatively, and both session files read `service.md`.

---

## Check 3 — Improvement Opportunities

### Missing Examples

| Doc | Section | Why an example would help |
|-----|---------|---------------------------|
| `docs/concepts/rule-enforcement-patterns.md` | § P1 → "Comparing against a fixed value" (added this run) | The two adjacent recipe items show Java. This one is prose-only, so an agent must infer whether the constant is imported statically or qualified. A two-line `if (!field.equals({App}DomainConstants.CONSTANT))` snippet would close it. |
| `.claude/skills/classify-and-plan/SKILL.md` | § Step 6.e | Steps 6.c/6.d both end in a pseudocode block *and* a worked shape. 6.e has the pseudocode but no worked `**Domain sentinels:**` line showing a filled-in origin, so the emitted-shape contract lives only in Step 8. |

### Ambiguous Guidance

| File | Step | Ambiguous phrase | Missing decision criterion |
|------|------|------------------|---------------------------|
| `.claude/skills/classify-and-plan/SKILL.md` | Step 6.e, Detection | "a bare numeric or boolean constant that is not another field" | The string case is unambiguous. For numerics the section relies on the following sentence ("the test is whether some §4 functionality *writes* it") to separate a sentinel from a threshold; that test is stated but not shown against an example, and `count <= 5` vs a written `0` is exactly where an agent will hesitate. |
| `.claude/skills/implement-aggregate/session-a.md` | § Domain sentinel constants | no halt clause | See Major finding 2 below — this is the same gap seen from the ambiguity side. |

---

## Check 4 — Neutral Domain

**Run in progress:** quizzes-full-2
**Base commit:** `0f22736c45d001f314442385ef4007d96ec8afee` ("docs: add review-artifacts report for 2026-08-02"), derived by `git merge-base HEAD master` — the primary rule; it did not degenerate to HEAD, so the `harness:`-commit fallback was not needed.
**Added lines scanned:** 361 (whole-run harness delta over `docs/` and `.claude/skills`)
**Forbidden nouns:** Course, Execution, Question, Quiz, QuizAnswer, Topic, Tournament, User

| File | Added line | Noun | Verdict |
|------|-----------|------|---------|
| — | — | — | no hits |

**Violations: 0. Moved: 0. False positives: 0.**

Clean on a real scan, not a degenerate one: 361 lines were measured. The commit under most scrutiny here is `e54b1353`, whose 101 added lines were authored while looking directly at `AnonymizeUser`/`ANONYMOUS` and which nonetheless names neither — the placement rule is stated entirely in `{Aggregate}` / `{Operation}` / `{AppClass}` vocabulary.

---

## Action Items

| Priority | Category | File | Finding | Suggested Fix |
|----------|---------|------|---------|---------------|
| Major | package-layout | `docs/workflow.md` § "Package layout" (line 32), `docs/architecture.md` § "Package Structure Convention" (line 56) | Both layout trees show `microservices/` as containing only `exception/` and `{aggregate}/`. `session-a.md` § "Domain sentinel constants" now mandates `microservices/domain/{AppClass}DomainConstants.java`, a third sibling that neither tree admits. An agent reconciling the two has no basis to choose. Introduced by `e54b1353`. | Add `├── domain/  ← {App}DomainConstants.java (shared sentinels; only if any aggregate declares one)` beside the existing `exception/` line in both trees. |
| Major | halt-symmetry | `.claude/skills/implement-aggregate/session-a.md` § "Domain sentinel constants" | The sibling `### {Aggregate}SagaState.java` section carries an explicit clause: "If the `**Saga states:**` line is **absent** … halt and report it rather than deriving a set." `classify-and-plan` § Step 8 asserts the sentinel line gets "the same never-omitted treatment", but the session-a section states no halt, so an agent facing a plan.md that predates Step 6.e will silently produce nothing. Two harness files disagree on whether the halt exists. Introduced by `e54b1353`. | Add the matching clause: an absent `**Domain sentinels:**` line halts; a line reading `none.` or `none declared.` is a valid instruction to produce nothing. |
| Minor | missing-example | `docs/concepts/rule-enforcement-patterns.md` § P1 | New "Comparing against a fixed value" paragraph is prose-only among Java-snippet siblings. | Add a two-line snippet showing the qualified constant reference inside a predicate method. |
| Minor | missing-example | `.claude/skills/classify-and-plan/SKILL.md` § Step 6.e | Has pseudocode but no worked emitted line; Steps 6.c/6.d both show one. | Add one filled-in `**Domain sentinels:**` example line with a neutral-domain constant. |
| Minor | ambiguous | `.claude/skills/classify-and-plan/SKILL.md` § Step 6.e, Detection | The sentinel-vs-threshold test for numeric literals is stated but not demonstrated. | Add the contrast inline: a bound in a comparison is a rule parameter; a value some §4 Description says an operation *sets* is a sentinel. |
| Minor | coverage | `docs/architecture.md` R3 / session files | R3 is stated only in `service.md`; sessions `b`/`c` name R1, R2, R7, R8 but not R3. | Optional — add R3 to the closed-list note in `session-b.md` § "Update BeanConfigurationSagas.groovy" where R1/R2 already appear. |

**Critical** = causes an agent to produce incorrect output (wrong code, missing file, wrong beans,
  wrong exception type, broken skill delegation).
**Major** = leaves an agent to infer or guess something the harness should have stated.
**Minor** = cosmetic inconsistency, missing example, ambiguous wording with low impact.
