# Harness Log - quizzes-full-2

Append-only. Schema and rules: `.claude/skills/_shared/conventions.md` § "Harness log".

| # | Session | Type | Artifact | Problem | Outcome | Ref |
|---|---------|------|----------|---------|---------|-----|
| 1 | 2.1.a | 2 | `.claude/skills/implement-aggregate/session-a.md` | Needed the sanctioned JPA realization of a P1 rule classified as "Java `final` field" (COURSE_NAME_FINAL, COURSE_TYPE_FINAL); the skill names the `non-final P1 rule` category but prescribes no field/constructor shape, so the no-arg-constructor and Hibernate-load-path handling was inferred. T1 never loads a row, so the inference is unproven until 2.1.b reads a Course back. | deferred | |
| 2 | 2.1.a | 2 | `.claude/skills/classify-and-plan/SKILL.md` | session-a.md requires `READ_{AGGREGATE}` in the saga-state enum when a later aggregate uses this one as a cross-aggregate prerequisite, but plan.md emits no per-aggregate saga-state set, so the slice had to scan later aggregates' plan.md sections and guess the enum; a mismatch would only surface in a later session's `c`. | deferred | |
| 3 | 2.1.b | 1 | `.claude/skills/classify-and-plan/SKILL.md`, `.claude/skills/implement-aggregate/session-b.md`, `.claude/skills/implement-aggregate/session-c.md` | The 2.N.b/2.N.c file-table templates and both session sub-files write command classes as `commands/{aggregate}/...`, which under the tables' stated root resolves inside `microservices/{aggregate}/`, contradicting `docs/concepts/commands.md` § "File Location" (app source root, so commands are shareable across services). Two harness files prescribed two packages for the same class. | fixed | 700600ac |
| 4 | 2.1.b | 1 | `docs/concepts/commands.md` | Closes row 1's sibling risk for commands. The § "What a Command Is" example declares only a multi-arg constructor and getters, but the test profile sets `local.messaging.serialize: true`, so commands round-trip through Jackson; the example as written fails to deserialize on the first command sent (`CreateCourse`/`GetCourseById`). | fixed | 90e5fe00 |
| 5 | 2.1.b | 1 | `docs/concepts/commands.md` | The § "Routing Commands" CommandHandler snippet calls `logger.warning(...)` in its `default ->` branch, but `ms.messaging.CommandHandler` declares no `logger` field, so the snippet does not compile as written. | fixed | 90e5fe00 |
| 6 | 2.1.b | 1 | `.claude/skills/implement-aggregate/session-b.md` | The § "Update `{AppClass}SpockTest.groovy`" fixture-helper template calls `aggregateIdGeneratorService`, but the section only instructs adding the functionalities field and the scaffolded base class declares no such field - silent compile error at first use. | fixed | 90e5fe00 |
| 7 | 2.1.b | 2 | `docs/concepts/testing.md` | Closes row 1. The final-field inference from 2.1.a is confirmed: a persisted Course loads back with correct `name`/`type`. But the mandated T2 shape does not prove it - under `@DataJpaTest` a fresh `UnitOfWork` still shares one persistence context, so the read-back returns the managed instance rather than a Hibernate-instantiated one. The slice proved it with a throwaway flush/clear probe, since deleted; no permanent test guards the load path. | deferred | |
| 8 | 2.1.c | 1 | `.claude/skills/implement-aggregate/session-c.md` | § Produce declares its `###` subheadings "the authority on what this session must emit", but the mandatory `create{Aggregate}()` helper replacement is a top-level `##` section outside it; a session treating § Produce as its manifest ships a stale placeholder helper and leaves session `b`'s read tests off the create path. Self-contradiction within one file. | fixed | 688cb5f7 |
| 9 | 2.1.c | 2 | `.claude/skills/implement-aggregate/session-b.md`, `.claude/skills/implement-aggregate/session-c.md` | session-c.md prescribes deriving `functionalityName` via `new Throwable().getStackTrace()[0].getMethodName()`; session-b.md is silent, so 2.1.b's coordinators used string literals. Both evaluate identically, so neither is demonstrably wrong, but every session `c` that appends to a session-`b` file faces the same choice. 2.1.c matched the existing file rather than introduce a second idiom mid-append. | deferred | |
| 10 | 2.1.c | 2 | `docs/concepts/service.md` | § "Injected Dependencies" shows `AggregateIdGeneratorService` as an `@Autowired` field while the same example takes repositories via the constructor, without saying whether the mix is deliberate. A session appending to the all-constructor-injection service that session `b` produces must decide whether to widen the constructor and the `@Bean` method instead. Following the example verbatim is what makes session-c.md § "BeanConfigurationSagas - No Change Needed" true. | deferred | |
| 11 | 2.1/2.2 | 2 | `.claude/skills/classify-and-plan/SKILL.md`, `.claude/skills/implement-aggregate/session-a.md` | Closes row 2. The human decided the planner computes the saga-state set and session `a` transcribes it. Deciding the rule surfaced a second conflict the row had not named: session-a.md's `READ_{AGGREGATE}` is prescribed by no other file, while `sagas.md` § Lock-Acquisition / § Step Ordering / § R4 and session-c.md § T4 all name `IN_{OP}_{AGGREGATE}` for the same step. Human chose `IN_{WRITE_FUNCTIONALITY}` per non-create write; `READ_` removed, so `CourseSagaState` (CreateCourse only) becomes an empty enum. | fixed | 9a1b1b4c |
| 12 | 2.1/2.2 | 2 | `docs/concepts/testing.md`, `.claude/skills/boot-strap/templates/AppSpockTest.groovy.template` | Closes row 7. Human decided the T2 read-back must cross the persistence context for every aggregate, not conditionally for those with `final` fields. A `flushAndClear()` helper now ships in the scaffolded base class and the mandated read-back shape calls it. Full suite green after the retrofit, so flush/clear disturbs neither the UnitOfWork nor version bookkeeping. | fixed | 0843bdef |
| 13 | 2.1/2.2 | 2 | `.claude/skills/implement-aggregate/session-b.md`, `.claude/skills/implement-aggregate/session-c.md` | Closes row 9. Human chose the string literal. session-c.md's stack-trace derivation is replaced and session-b.md now prescribes the literal explicitly rather than leaving it inferred. `CourseFunctionalities.java` already used literals, so no code changed. | fixed | ea7202e7 |
| 14 | 2.1/2.2 | 2 | `docs/concepts/service.md`, `.claude/skills/implement-aggregate/session-c.md` | Closes row 10. Human decided every service dependency goes through the constructor into a `final` field, with no `@Autowired` fields. The § "Injected Dependencies" example is rewritten accordingly; `CourseService` and its `@Bean` method now pass `AggregateIdGeneratorService`. session-c.md's "BeanConfigurationSagas - No Change Needed" section was false under this rule and now states that the session first needing an aggregate id widens both the constructor and the `@Bean` method. | fixed | 303a5978 |
| 15 | 2.2.c | 1 | `docs/concepts/testing.md` | The § T4 lock-acquisition template constructs the saga with `commandGateway` as if inherited, but neither `SpockTest` nor the scaffolded `{AppClass}SpockTest` declares it; a T4 test written verbatim does not compile. Injection by the `CommandGateway` interface is also ambiguous, since the test profile registers two implementations. Reported by slice 2.2.c2 (ActivateUser). | fixed | 16b441f6 |
| 16 | 2.2.c | 1 | `.claude/skills/implement-aggregate/session-c.md` | § Produce emits one T4 file per write functionality and never names the compensation test, while `docs/concepts/testing.md` § T4 / § Compensation Test place it in core T4 scope and require it for every write functionality holding a lock across a later step. Two harness files disagree on whether a mandatory file exists; ActivateUser, UpdateUserName, AnonymizeUser and DeleteUser all have that shape. Reported by slice 2.2.c2. | fixed | 40e3414b |
| 17 | 2.2.c | 1 | `.claude/skills/implement-aggregate/session-c.md` | Closes row 16 in part. The subheading added by 40e3414b named the compensation test's path but not its impairment CSV, without which the test injects no fault and fails for a reason unrelated to compensation. Reported by slice 2.2.c2 after writing the first one. | fixed | c49f95e4 |
| 18 | 2.2.c | 1 | `.claude/skills/_shared/conventions.md` | § "Run the test suite" permits a no-clean narrow form for slices, then tells the agent to report the aggregation script's totals - but without `clean` the script sums every earlier slice's stale reports, so `tests=` does not describe the narrowed run. Every slice of 2.2.b and 2.2.c reported an inflated total before the fix. Reported by slice 2.2.c3 (UpdateUserName). | fixed | ca2ebbee |
| 19 | 2.2.c | 2 | `docs/concepts/testing.md`, `.claude/skills/implement-aggregate/session-c.md` | § T4 mandates a happy-path case per write functionality asserting `sagaStateOf(id) == NOT_IN_SAGA`, but `DeleteUser` soft-deletes its own aggregate, so `sagaStateOf` throws from `aggregateLoadAndRegisterRead` instead of returning a state. Both substitutes break another stated rule: a persistence read-back violates § Assertion Ownership, and a bare `noExceptionThrown()` is the § Fake/Wrong/Weak smell. Human chose to omit the happy-path case for any operation whose success makes its aggregate unresolvable, covering it by the lock-acquisition case, the compensation test and T2. Recurs at `DeleteExecution` (aggregate 4) and `DeleteTournament` (8). | fixed | 6ee9a985 |
| 20 | 2.2.c | 2 | `.claude/skills/implement-aggregate/session-a.md` | The harness says nothing about where a domain sentinel constant lives - a literal that a *later* aggregate's P1 predicate compares against. `AnonymizeUser` sets both name fields to `ANONYMOUS`, which Tournament's `CREATOR_IS_NOT_ANONYMOUS` will compare against; aggregate class, a shared constants class and the DTO are all defensible. Slice 2.2.c4 reports it would have halted here. The manager pre-empted the halt by assigning the constant to the owning aggregate class in the slice brief, which the gate reserves to the human; the decision stands but was not the manager's to make, and the harness is still silent. | deferred | |
| 21 | 2.2.c | 2 | `.claude/skills/classify-and-plan/SKILL.md`, `.claude/skills/implement-aggregate/session-a.md`, `.claude/skills/implement-aggregate/session-c.md`, `docs/concepts/rule-enforcement-patterns.md` | Closes row 20. Human decided a domain sentinel lives in a shared `microservices/domain/{AppClass}DomainConstants.java`, not on the writing aggregate, its DTO or the test base class, so neither microservice package depends on the other; and that the planner computes the sentinel set (new Step 6.e) while session `a` of the *writing* aggregate transcribes it, as already ruled for the saga-state set in row 11. `ANONYMOUS` moved off `User`; `Tournament` (2.8.a) will import the constant rather than re-declare the literal. | fixed | e54b1353 |
| 22 | 2.2.c | 1 | `docs/workflow.md`, `docs/architecture.md`, `.claude/skills/implement-aggregate/session-a.md` | Fallout of row 21, found by the aggregate-2 boundary `/review-artifacts`. Both package-layout trees admitted only `exception/` and `{aggregate}/` under `microservices/`, contradicting the `microservices/domain/` path session-a.md now mandates; and session-a.md's new sentinel section carried no halt-on-absent-line clause, though its sibling `SagaState` section does and classify-and-plan § Step 8 asserts the same treatment. | fixed | 8aac2ccd |
| 23 | 2.3.b | 2 | `docs/concepts/commands.md` | § "What a Command Is" states the `rootAggregateId` rule for create commands (pass `null`) but is silent on a bulk read filtered by a non-PK field, which likewise has no single root aggregate. Slice 2.3.b1 resolved `GetTopicsByCourseCommand` by analogy to the in-app `GetCoursesCommand` and did not halt; passing the filter's foreign id instead would be wrong, since it is not the aggregate whose lock lifecycle the command joins. Harness still silent. | deferred | |
| 24 | 2.3.b | 2 | `.claude/skills/implement-aggregate/session-b.md` | § "Update `{AppClass}SpockTest.groovy`" says a create fixture helper's params are "defaulted to the domain constants", but a foreign-aggregate id param has no usable constant and must come from a real upstream fixture at call time. Slice 2.3.b1 left `createTopic(courseAggregateId, name = TOPIC_NAME)` required-and-first and did not halt. Harness still silent on the foreign-id case. | deferred | |
| 25 | 2.3.c | 2 | `docs/concepts/testing.md` | § "Compensation Test"'s template makes its third assertion a read-back "through the functionality's own getter", presuming a by-id read coordinator exists for the aggregate. An aggregate may deliberately expose only a by-foreign-key list read, as `Topic` does. Slice 2.3.c1 read back through the list coordinator filtered by aggregate id and did not halt; the risk the silence carries is that an agent instead invents an unplanned by-id coordinator and test to satisfy the template. | deferred | |
| 26 | 2.3.b | 2 | `docs/concepts/commands.md` | Closes row 23. Human decided the create rule generalises: a bulk read filtered by a non-PK field also passes `null`, and the § "What a Command Is" callout now says so explicitly and forbids passing the filter's foreign id, which is the plausible wrong answer the silence admitted. `GetTopicsByCourseCommand` already matched the ruling, so no application code changed. | fixed | c35c4a88 |
| 27 | 2.3.b | 2 | `.claude/skills/implement-aggregate/session-b.md` | Closes row 24. Human decided a foreign-aggregate id param stays required, undefaulted and first in the fixture helper's signature, since its value is minted per test by the upstream helper; only own-field params get constant defaults. § "Update `{AppClass}SpockTest.groovy`" now states it and the session-`c` signature contract is reworded to match. `createTopic(courseAggregateId, name = TOPIC_NAME)` already had this shape, so no application code changed. | fixed | 886d531a |
| 28 | 2.3.c | 2 | `docs/concepts/testing.md` | Closes row 25. Human decided the read-back asserts through whatever read coordinator the aggregate already exposes, selecting by id from a filtered list read where no by-id coordinator exists; § "Compensation Test" now says so and forbids adding a coordinator, service method or command to satisfy the template, which was the risk the silence carried. `UpdateTopicCompensationTest` / `DeleteTopicCompensationTest` already read back through the list coordinator, so no application code changed. | fixed | 8a0da2d3 |
| 29 | 2.4.a | 1 | `.claude/skills/implement-aggregate/session-a.md` | § "`{Aggregate}.java`" described the owned-collection mapping `@OneToMany(cascade = ALL, orphanRemoval = true)` as making the aggregate "the inverse side with no FK column". Without `mappedBy` the aggregate is the owning side and the association is materialised as a join table; an agent trusting the prose would add `mappedBy` and fail persistence-unit init, since the owned entity has no back-reference. First owned collection in this application (`Execution.students`). | fixed | fda4dd45 |
| 30 | 2.4.a | 2 | `.claude/skills/implement-aggregate/session-a.md` | § "`{Aggregate}.java`" listed "getters and setters for all mutable fields" and "no business logic methods" but was silent on add/remove helpers for a collection field, which the T1 intra-invariant test needs to trip a collection-scoped P1 rule. Slice 2.4.a wrote `Execution.addStudent()` reading it as a setter and did not halt. Human decided the helpers belong to session `a` as setters, so a later session never re-opens the aggregate for them and T1 mutates through them rather than the getter's live list; the slice's code already matched, so no application code changed. | fixed | 9dde2a4c |
| 31 | 2.4.b | 2 | `.claude/skills/implement-aggregate/session-b.md` | § "Update `{AppClass}SpockTest.groovy`" sanctioned only the `create{Aggregate}` fixture, whose params are minimal own fields plus leading foreign ids and whose 2.{N}.c replacement body is a single create-functionality call. A session-`b` read that filters on a field of an owned collection (`GetUserExecutions` over `Execution.students`) needs that collection populated, which only a session-`c` write can do, so every available path violated a stated rule. Slice halted before writing code. Human chose the sibling-helper option: `create{Aggregate}` stays minimal and session `b` adds one direct-on-aggregate helper per write functionality its reads depend on, under the same signature contract; session-c.md now replaces those bodies too. Widening the create helper and per-test-class fixtures were both rejected. | fixed | a85cd8f7 |
| 32 | 2.4.b | 1 | `.claude/skills/implement-aggregate/session-b.md` | The sibling-helper code block added by row 31 loaded the aggregate with `{aggregate}Service.get{Aggregate}(...)`, but the only read the same session puts on the service returns a DTO, which has no collection mutator and no route back to the aggregate - the template did not compile against the service surface the session itself mandates. Replaced with `aggregateLoadAndRegisterRead` plus the `Saga{Aggregate}` copy constructor, matching the surrounding prose. | fixed | 8c019542 |
| 33 | 2.4.c | 1 | `.claude/skills/implement-aggregate/session-b.md` | § "Fixture state a create cannot reach" (added by row 31) promised session-`b` call sites survive the 2.{N}.c body swap unchanged, but its sibling-helper template took a bare foreign id filled from constants, while the 2.{N}.c replacement calls a functionality that fetches that foreign aggregate and runs its P3 guards. Slice 2.4.c3 found six call sites across two files throwing on the swap. Row 27's "id comes from the upstream fixture helper" rule was never carried into the sibling-helper section; now stated there, with the added clause that the upstream aggregate must be in the state those guards require. | fixed | 3271780e |
| 34 | 2.4.c | 1 | `.claude/skills/implement-aggregate/session-c.md` | § "`{Aggregate}ServiceTest.groovy`" said to autowire `EventService` without noting the field is class-scoped. Under slicing every event-publishing slice of a session appends to the same class, so two such slices each declaring it is a compile error - 2.4.c has two (`DisenrollStudent`, `DeleteExecution`). Reported by slice 2.4.c4; fixed before c5 ran. | fixed | a153d946 |
| 35 | 2.4.c | 1 | `.claude/skills/_shared/conventions.md` | § "Run the test suite" mandated `mvn ...` followed by `echo "MAVEN_EXIT=$?"`, but a full run's stdout can exceed the tool output budget and be truncated, taking the verdict line with it - leaving the agent to infer pass/fail from the surefire totals alone, which the same section forbids. Hit by slice 2.4.c5 and by the manager. Now mandates redirecting to a file, which is not a pipe and preserves the exit code. | fixed | 42ba0086 |
| 36 | 2.4.c | 2 | `.claude/skills/implement-aggregate/session-c.md` | § "`{Aggregate}ServiceTest.groovy`" mandates "one negative case" for event publication without saying whether that is per test class or per published event type. Under slicing neither slice can tell: 2.4.c4 contributed one, 2.4.c5 read the rule as class-scoped and added none, and a reviewer expecting one per event type would read that as a gap. Not decidable mechanically - it is a coverage-policy choice. Raised at the aggregate boundary rather than mid-session, since both slices shipped green either way. | deferred | |
| 37 | 2.4.d | 2 | `.claude/skills/implement-aggregate/session-d.md` | § "`{Aggregate}InterInvariantTest.groovy`" mandates a "reflects event" test asserting the cached field against the event payload, for every subscribed event. `ActivateUserEvent` carries `active=true`, but enrollment already rejects an inactive user (`INACTIVE_USER`), so the cached `active` is true before the event and the mandated assertion is trivially satisfied - which `testing.md` § "Fake" independently forbids. Every available assertion violated one of the two rules. Proceeded without halting, asserting the payload value plus an advance of the cached publisher version; the human has not ruled on whether that is the sanctioned resolution. | deferred | |
| 38 | 2.4.d | 2 | `docs/concepts/events.md` | § EventSubscription tells the consumer to pass "the anchor entity's current version" as `subscribedVersion`, and is silent on the fact that a snapshot seeded from a fetched DTO carries the publisher's *aggregate* version while `registerEvent` stamps events with a *later* value of the same global counter. Every event published before the snapshot was taken therefore stays eligible and is delivered on the consumer's first poll. Surfaced as a T3 failure: the fixture's own activation event moved the cached version before the test fired anything. Not decidable mechanically - whether to re-seed snapshots from the event counter, filter on the publisher side, or document the backlog as expected is a framework design call. | deferred | |
| 39 | 2.4 | 2 | `.claude/skills/implement-aggregate/session-c.md` | Closes row 36. Human ruled the negative case is class-scoped, asserts the *total* event-store count over a write operation that publishes nothing, and is added once under slicing - a per-type case would be a weaker assertion, not extra coverage. A per-type fallback covers aggregates with no totally silent operation. `ExecutionServiceTest` already conformed; no application change. | fixed | 2cd9e00d8 |
| 40 | 2.4 | 2 | `.claude/skills/implement-aggregate/session-d.md`, `docs/concepts/testing.md` | Closes row 37. Human confirmed the shipped resolution as the fallback arm of a two-step rule: prefer a legally reachable state where the cached field differs from the payload; only when none exists, assert the payload value plus the cached publisher-version advance. Generalises to any re-affirming payload. Carved out of § Fake so the checklist cannot flag the sanctioned form. `ExecutionInterInvariantTest` unchanged - the contrary state is unreachable, since no subscribed event can make the cached student inactive. | fixed | f214d2994 |
| 41 | 2.4 | 2 | `docs/concepts/events.md`, `.claude/skills/implement-aggregate/session-d.md` | Closes row 38. Human ruled docs-only: the backlog is documented as expected behaviour, not patched in `simulator/` - aggregate and event versions are two draws on one counter and `registerChanged` precedes `registerEvent`, so an event always outranks the aggregate version a DTO can carry. T3 setups must drain it in `given:`, sanctioning `drainActivationBacklog`. The framework alternative (stamp the event with the committed aggregate version) was considered and declined as 2-fw. Also documented the previously unmandated rule that *every* ByEvent mutation advances the cached publisher version, not only the version-only case; `ExecutionService` already did, so no application change. | fixed | cd0a2bd08 |
| 42 | 2.5.c | 2 | `docs/concepts/sagas.md` | § Step Ordering and § Known DTO Gaps describe data-assembly steps that fetch a single upstream aggregate, and are silent on a `× N` fetch - `CreateQuestion` / `UpdateQuestion` need one `GetTopicByIdCommand` per topic to seed each `QuestionTopic`. One step per element and one looping step were both defensible, and the choice fixes whether step names stay literal (which `executeUntilStep`, the impairment CSVs and the lock audit all key on). Halted before writing either saga. Human chose the single looping `getTopicsStep`; per-element fault granularity was accepted as the cost. Generalises to Quiz, QuizAnswer and Tournament. | fixed | 62c3cf6bb |
| 43 | 2.5 | 1 | `docs/concepts/aggregate.md`, `docs/concepts/events.md`, `.claude/skills/implement-aggregate/session-d.md` | Found by the aggregate-5 boundary `/review-artifacts`. § Key Fields typed `Aggregate.version` as `Integer` "from `VersionService`", but `Aggregate.java:30` declares `Long` and no such class exists (`IVersionService` and its two implementations do). session-d.md's worked ByEvent method inherited the error: it took `Integer publisherVersion` and assigned it from `event.getPublisherAggregateVersion()`, which returns `Long` - code that does not compile, and that contradicted events.md, which already used `Long` for the same mutation. events.md now states once that every cached publisher version is `Long`; only aggregate ids are `Integer`. No application code carried the wrong type yet. | fixed | 9bd18a767 |
| 44 | 2.5 | 1 | `docs/concepts/events.md` | Same review. The doc states an event subclass "must be annotated `@Entity` too", but neither the `CreateShipmentEvent` example nor the § Canonical Wiring Snippet event class - which every skill file is told to follow - carried the annotation. | fixed | f4bfc19cd |
| 45 | 2.5 | 1 | `docs/workflow.md`, `docs/concepts/testing.md` | Same review. Both package-layout trees placed `SpockTest.groovy` under the application package, while `boot-strap/SKILL.md` writes it to `.../socialsoftware/` and the template declares that parent package. The generated application matches boot-strap, so the two docs described a layout no app has. | fixed | ffb7d1970 |
| 46 | 2.5 | 1 | `docs/concepts/commands.md`, `.claude/skills/implement-aggregate/session-d.md` | Same review. The § Routing Commands switch called `cmd.getAggregateId()`; the base `Command` exposes only `getRootAggregateId()`, and the doc's own command template exposes `getShipmentAggregateId()`. Fixed to the latter, matching the template 140 lines above. Found in passing during the same sweep: session-d.md's reading list named `getAggregateId()` on `EventSubscription`, whose accessor is `getSubscribedAggregateId()`. | fixed | 331796339 |
| 47 | 2.5 | 1 | `.claude/skills/implement-aggregate-full/SKILL.md` | Same review, delegated execution. § "Shared files per session type" listed only the four notification/eventProcessing/test files for session `d`, though session-d.md requires each slice to append a `{operation}ByEvent` method to `{Aggregate}Functionalities.java`, a mutate helper to `{Aggregate}Service.java` and a subscription to `{Aggregate}.java` - all three forbidden to a slice whose brief omits them. The session-`c` note was worse than incomplete: it said `create{Aggregate}()`'s file belongs to slice `c1` and "no later slice may touch it", which session-c.md § "Update `{AppClass}SpockTest.groovy`" directly contradicts by requiring each later slice to replace its own sibling fixture helper's body there. Ownership is now per helper body, not per file. | fixed | 6ecc1528b |
| 48 | 2.5 | 2 | `.claude/skills/classify-and-plan/SKILL.md` | Same review, plan generation. The 2.N.c file-list template omitted `{Operation}CompensationTest.groovy` and its impairment CSV, which session-c.md makes core T4 scope, so every lock-holding write functionality forced a plan.md amendment. Not decidable mechanically: the planner cannot always see whether a saga will hold a lock across a later step. Human chose to list both files unconditionally with an inline gate note deferring the call to session `c`; having the planner evaluate the applicability test at plan time, and a convention note sanctioning unplanned files, were both rejected - the first asks the planner to reason about a saga that does not exist, the second leaves plan.md an incomplete inventory. | fixed | cd81011ba |
| 49 | 2.5 | 2 | `.claude/skills/review-artifacts/SKILL.md` | Same review, self-referential gap. Steps 1.b and 2 build the review set from `*.md` only, so the nine `boot-strap/templates/*.template` files are never checked, although `boot-strap/SKILL.md` and `templates/README.md` make specific claims about their beans, fields, markers and package declarations. Whether to widen the input set is a cost choice, not a fact. Human chose to widen it, plus a new Check-1 sub-table asserting every claimed symbol is present in the template it is attributed to; documenting the exclusion as deliberate, and a narrow claims-only cross-check without widening the scan, were rejected. | fixed | 4cddc5020 |
| 50 | 2.5 | 1+2 | `docs/concepts/testing.md`, `.claude/skills/implement-aggregate/SKILL.md`, `.claude/skills/implement-aggregate/session-b.md`, `.claude/skills/implement-aggregate/session-c.md` | Same review, minor bundle. Type 1: the Weak checklist wrote `registerChanged(aggregate)` against a two-arg signature; session-c.md rooted the impairment CSV app-relative beside a repo-rooted `{test}` path in the same block; `implement-aggregate/SKILL.md` derived a `{appClass}` variable no session sub-file consumes; session-b.md's "and nothing else" contradicted its own § Produce. Type 2: `loadForCheck(Integer, Class<T>)` ships in every scaffolded test base class and was named by no doc, while § T3 told agents to hand-roll the same load - human chose to document it as the sanctioned aggregate read-back rather than delete it from the template, since the current application already calls it. | fixed | ec99396dc |
| 51 | 2.6.a | 1 | `.claude/skills/implement-aggregate/session-a.md` | § "`{Aggregate}.java`" and § "Owned entity classes" both wrote the single-snapshot mapping as `@OneToOne(mappedBy = "{entityField}")`, but JPA's `mappedBy` names the back-reference field on the *entity*, which the same file declares as `{aggregate}`; taken literally it fails at EntityManagerFactory init with "mappedBy reference an unknown target entity property". First single subscribing snapshot in this application (`Quiz.execution` → `QuizExecution`). Also added the clause for an immutable reference, which has no setter to do the back-reference wiring. | fixed | 817fb30fc |
| 52 | 2.6.c | 1 | `.claude/skills/implement-aggregate/session-c.md` | § "Update `{AppClass}SpockTest.groovy`" requires the fixture helper to be swapped onto the real create functionality while keeping session `b`'s defaults exactly, and covers only one way that breaks: a synthetic foreign id. Where the create path stamps a field from the clock that a P1 rule orders against a caller-supplied one (`Quiz.creationDate = now()` vs `availableDate`, rule QUIZ_DATE_ORDERING), session `b`'s absolute past-dated constants make both instructions unsatisfiable - 14 errors across every quiz fixture, observed before the fix. Session `b` cannot avoid it: it writes its fixture before the create functionality exists. Added the value-repair clause (re-pin relative to the same clock, keep the constant names) plus the consequence for a read test asserting equality against a now clock-stamped field. | fixed | 2457b5bbe |
| 53 | 2.7.c | 2 | `.claude/skills/implement-aggregate/session-c.md`, `docs/concepts/testing.md` | § "One `{Op}Test.groovy` per write functionality (T4)" and § Spec-First Ordering both make plan.md the spec and say what to do when it *disagrees* with the implementation, but neither says what to do when it is *silent* about a write method's behaviour on an out-of-domain input. `AnswerQuestion(quizAnswerAggregateId, questionAggregateId, ...)` targets a `QuestionAnswer` seeded only for the quiz's own questions, and no rule in plan.md §7 or the domain model covers a `questionAggregateId` outside that set; the three candidate answers (throw a new constant, silently no-op like the event-handler idiom, reuse an existing constant) differ in observable behaviour, so the choice is a design decision. Human chose to add the `QUESTION_NOT_IN_QUIZ_ANSWER` guard. Harness gap itself left open - no docs/skills edit made this session. | deferred |  | 
| 54 | 2.7.d | 2-fw | `simulator/src/main/java/pt/ulisboa/tecnico/socialsoftware/ms/aggregate/EventHandler.java`, `docs/concepts/events.md`, `.claude/skills/implement-aggregate/session-d.md` | `EventHandler.getAggregateIds()` returned every aggregate of every type, because `AggregateRepository` is declared over `Aggregate` and nothing downstream narrows by consumer type; a handler was therefore handed the ids of foreign aggregates subscribing to the same event, and the consumer's ByEvent service cast them to its own class. Latent since 2.4.d - `Quiz` and `Execution` already share event types, but no test until `QuizAnswerInterInvariantTest` built both consumer types in one fixture. 5 ClassCastExceptions across `UpdateStudentNameEvent`, `AnonymizeStudentEvent`, `DeleteUserEvent` (shared with `Execution`) and `UpdateQuestionEvent`, `DeleteCourseExecutionEvent` (shared with `Quiz`). Halted before fixing. Human chose the framework filter - a new abstract `aggregateType()` on `EventHandler` - over per-ByEvent-method or per-handler `instanceof` guards in the application, since those repeat the workaround in every generated app and would have recurred eight more times in 2.8. | fixed | 73d29e458 |
| 55 | 2.7.d | 2-fw | `simulator/src/main/java/pt/ulisboa/tecnico/socialsoftware/ms/aggregate/EventHandler.java`, `.claude/skills/implement-aggregate/session-a.md`, `docs/concepts/events.md`, `.claude/skills/implement-aggregate/session-d.md`, the seven `{Aggregate}Repository.java` of this application | Reverses row 54. The polymorphic `findAll()` was not a framework gap the filter had to close - it was a typing hole this application dug for itself. `Aggregate` is mapped `TABLE_PER_CLASS`, so any query inherited from `AggregateRepository` (declared over `Aggregate`) unions every aggregate's table; but nothing obliges a generated repository to inherit it. `master`'s four applications never saw the bug because their repositories extend `JpaRepository<{ConcreteEntity}, Integer>` directly, scoping every query to one physical table by construction. The real defect was session-a.md § `{Aggregate}Repository.java`, which prescribed `extends AggregateRepository` with no type arguments - a standing template, not a 2.7.d slip, so all seven repositories carried it and 2.8 would have carried it again. Human chose to fix the typing and drop the filter: a runtime `isInstance` check in `simulator/` is a compensating patch charged to every consumer of the framework for a hole no correct consumer has. `EventHandler.java` is now byte-identical to `master`; `aggregateType()` removed from all four handlers; 251/251 sagas tests pass, `QuizAnswerInterInvariantTest` 14/14 - the test that caught the original bug now passes for the right reason. | fixed | a13c54869 |
| 56 | 2.7.c | 2 | `docs/concepts/rule-enforcement-patterns.md`, `docs/concepts/testing.md`, `.claude/skills/implement-aggregate/session-c.md` | Closes row 53. The gap left open there - the harness covers plan.md *disagreeing* with the implementation but is silent on plan.md being *silent* about a write method's behaviour on a syntactically valid, out-of-domain input, as `answerQuestion(quizAnswerAggregateId, questionAggregateId, ...)` was for a question the quiz does not contain. Human chose an ordered default in the Decision Guide over halting per occurrence: the P4a-over-P3 preference already in § P3 decides the two cases that actually recur (a saga fetch keyed to fail naturally → P4a; an entity owned by the aggregate, where no such fetch exists → P3 guard on a named constant before any mutation, recorded in plan.md's rule list so T2/T4 can cite it), leaving the halt for the genuinely undecidable remainder. Silent no-op explicitly rejected as an event-handler idiom misapplied to a caller error. Verified against the shipped 2.7.c guard: `QuestionAnswer` is owned by `QuizAnswer`, so branch 2 classifies `QUESTION_NOT_IN_QUIZ_ANSWER` as P3, consistent with what was already committed. | fixed | 475e157ef |
| 57 | review | 1 | `docs/concepts/aggregate.md` | Found by `docs/reviews/review-2026-08-08.md` C1. § Repositories still prescribed `{Aggregate}Repository extends AggregateRepository` (no type arguments), the exact polymorphic typing row 55 reverted away from on the same day; that revert touched `session-a.md` but not the concept doc, and the session `a` Reads list sends an agent to the doc first, so aggregate 8 onward would have re-emitted the reverted shape and its `ClassCastException`. The review could not settle statically whether `{Consumer}EventHandler`'s constructor survives the concrete shape - checked against `simulator/`, it does: `EventHandler` takes `JpaRepository<? extends Aggregate, Integer>`, so `events.md` and `session-d.md` needed no change and `simulator/` was not touched. | fixed | 5ae093665 |
| 58 | review | 1 | `docs/concepts/rule-enforcement-patterns.md` | Found by `docs/reviews/review-2026-08-08.md` C2. § P2 carried a full four-step event-wiring recipe competing with `events.md`, which declares itself sole owner of that wiring, and contradicted it three ways: a `subscribesEvent()` override that is inert in the sagas profile, one handler class per event type against the one-dispatcher-per-consumer rule, and a `handleExternalChange(...)` method no other artifact names. An agent following it writes a filter that never runs, N surplus handler beans, and a method the EventProcessing contract does not name. Recipe deleted in favour of a pointer; § "Use when" and § "Upstream / Downstream" kept as pattern semantics owned here. | fixed | 5ae093665 |
| 59 | review | 1 | `.claude/skills/implement-aggregate/session-b.md` | Found by `docs/reviews/review-2026-08-08.md` C3. The read-methods bullet told the service to fetch a foreign aggregate's DTO "via its service", which requires injecting a foreign `*Service` - forbidden by R2, and contradicted 33 lines later by the same file's § "Two-step read saga variant" and by its own BeanConfiguration section. Replaced with the saga-resolves-and-passes-the-DTO mechanism the file already documents. | fixed | 5ae093665 |
| 60 | review | 1 | `.claude/skills/classify-and-plan/SKILL.md` | Found by `docs/reviews/review-2026-08-08.md` M1. Step 3.d's rule that a subscribing single snapshot needs an owned-entity class was justified by the subscription class overriding `subscribesEvent(Event)` to filter on id and liveness - an override that is inert in the sagas profile per `events.md`. The conclusion survives (the paragraph already calls itself a uniformity rule, not a framework constraint) but the stated reason is false, and an agent reasoning forward from it writes the dead override. Justification rewritten around the reference-object shape and the ByEvent discriminator; the caveat and `EventSubscription` constructor note left as they were. | fixed | 546a966d6 |
| 61 | review | 1 | `docs/concepts/events.md` | Found by `docs/reviews/review-2026-08-08.md` M2. The § Canonical Wiring Snippet EventProcessing block called `<updateMethod>` with no `ByEvent` suffix, while § "ByEvent sagaState guard" six lines below and `session-d.md` both make `{operation}ByEvent` mandatory and the same doc explains that calling the saga `Functionalities` method from an event handler creates a circular saga loop. A snippet that declares itself the exact pattern to copy must not elide the one naming rule that prevents the loop. | fixed | 546a966d6 |
| 62 | review | 1 | `docs/architecture.md` | Found by `docs/reviews/review-2026-08-08-2.md` C1. The surviving third head of row 58. § Package Structure Convention showed the event-handler node as `handlers/{Event}EventHandler.java` - one class per event type - which `events.md` (sole owner of that wiring), `docs/workflow.md` § Naming conventions and `session-d.md` all contradict with one dispatcher per consumer aggregate. Row 58 deleted the per-event handler from `rule-enforcement-patterns.md` § P2 but nobody checked the package-layout block, which `session-b.md` sends every implementing agent to read; aggregate 9 would have emitted N handler classes and N beans. Also the only node in the block using `{Event}` where every sibling uses `{Xxx}`. | fixed | 4b898dbff |
| 63 | review | 1 | `.claude/skills/classify-and-plan/SKILL.md` | Found by `docs/reviews/review-2026-08-08-2.md` M1. The note gating the 2.N.c compensation-test pair still hedged - "a create functionality *typically* drops the pair; a lock-then-mutate functionality *typically* keeps it" - two lines after the same note declares `session-c.md` owner of the rule and says not to restate it, and after `session-c.md` had dropped the same hedge. The softer copy is the one a planning agent reads first. Replaced with a bare pointer to the session-`c` applicability test. | fixed | 4b898dbff |
| 64 | 2.8.b | 2 | `docs/concepts/rule-enforcement-patterns.md`, `.claude/skills/implement-aggregate/session-b.md` | `docs/concepts/testing.md` § Spec-First Ordering makes plan.md the spec for a read's filter predicate, and § Decision Guide Step 4 supplies an ordered default for the analogous silence on a *write* method's out-of-domain input, but nothing covers plan.md being silent about a *read* functionality's filter predicate. `GetOpenedTournamentsForExecution` / `GetClosedTournamentsForExecution` were specified only as "currently open" / "have closed" while `Tournament` carries `startTime`, `endTime` and `cancelled`, so the three candidate predicates differ in observable result set and in whether the two lists partition. Human chose `open = !cancelled && endTime > now`, `closed = !cancelled && endTime <= now`, cancelled in neither. Harness gap itself left open - no docs/skills edit made this session, since whether to add an ordered default or keep the per-occurrence halt is the same class of decision as row 56. | deferred |  |
| 65 | 2.8.c | 2 | `docs/concepts/sagas.md`, `.claude/skills/implement-aggregate/session-c.md` | plan.md §8 states `CreateTournament(executionAggregateId, creatorAggregateId, startTime, endTime, numberOfQuestions, topicAggregateIds)` and separately that the same saga creates the tournament's `Quiz`, but nothing settles where the quiz's `title`, `resultsDate` and `quizType` come from - none is a `CreateTournament` parameter and `Quiz` defaults none of them. § Create Functionality Sagas covers the shape of a create step, not the provenance of fields the caller does not supply, so the three candidates (widen the signature, default them on `Quiz`, derive them in the saga) differ in the published API and are a design decision. Human chose derivation in the saga: `quizType = GENERATED`, `resultsDate = endTime`, `title` from a new `TOURNAMENT_QUIZ_TITLE` domain sentinel, signature unchanged. Harness gap itself left open - no docs/skills edit made this session. | deferred |  |
| 66 | 2.8.c | 2 | `docs/concepts/sagas.md` | § Create Functionality Sagas item 2 requires the create step to register a removal compensation whenever a later step follows it, but the aggregate being created here is `Quiz`, whose session 2.6 shipped no delete of any kind - no service method, no command, no handler case. The doc assumes the compensating command exists and does not say who writes it when it does not, and the alternatives (reorder so the create is last, which `Tournament.quiz` being constructor-final forbids; skip the compensation and leak an orphan quiz; add the delete) differ in what ships to the Quiz microservice. Human chose adding `QuizService.deleteQuiz` + `DeleteQuizCommand` + a `QuizCommandHandler` case, reopening 2.6.c for one method. Harness gap itself left open - no docs/skills edit made this session. | deferred |  |
| 67 | 2.8.c | 2 | `docs/concepts/rule-enforcement-patterns.md` | plan.md §8 says the create saga draws `numberOfQuestions` questions "from the tournament's topics" but names no constant for the case where the course cannot supply that many, and § Decision Guide Step 4.3 requires a named constant in plan.md's rule list before any `// Spec:` can cite one. Silent, not contradictory - whether a shortfall throws, truncates or pads is a design decision. Human chose a throw on a new `TOURNAMENT_NOT_ENOUGH_QUESTIONS`, guarded in `TournamentService` rather than in the saga step per § Common Mistakes. Harness gap itself left open - no docs/skills edit made this session. | deferred |  |
| 68 | 2.8.c | 2 | `docs/concepts/testing.md` | `session-c.md` § "Update `{AppClass}SpockTest.groovy`" and row 52's repair cover a fixture constant that must be re-pinned *forward* of `DateHandler.now()` once the real create path stamps a clock field - but not a constant whose whole purpose is to sit in the *past*. `GetClosedTournamentsForExecution` needs an elapsed tournament window, and after the swap `CreateTournament` cannot produce one: it derives the generated quiz's `availableDate` from `startTime` (P4b), and `QUIZ_DATE_ORDERING` requires `creationDate < availableDate`, so every reachable tournament starts in the future. The harness has no rule for reaching a time-gated state at all. Human chose option A of `2026-08-08-quiz-tournament-date-contradiction-design.md`: a past window is unreachable by design, and tests reach "started"/"closed" by the passage of real time through a fixture helper that creates a short future window and waits it out. No change to `Quiz`, `QuizService`, the domain-model spec or `simulator/`; whether `testing.md` gains a general rule for reaching a time-gated state is left open. | deferred |  |
| 69 | 2.8.c | 1 | `2026-08-08-tournament-2.8.c-plan.md` (session working document) | D1 fixed the question-selection predicate as "questions carrying at least one of the tournament's topics", but plan.md §8's own P4b rule `NUMBER_OF_QUESTIONS / QUIZ_TOPICS` states `Tournament.topics ⊇ topics(Quiz.questions)`. Overlap admits a question carrying a topic outside the tournament's set, so following D1 literally makes the saga violate the rule it exists to satisfy; it also empties the selection for the helper's default `topics = []`, so every 2.8.b read fixture would throw. Mechanically demonstrable against plan.md, so fixed without asking: the implemented predicate is the rule's containment. Recorded in plan.md's P4b row so the two cannot drift again. No harness artifact was at fault, so the fix ships in the application commit rather than a `harness:` one. | fixed |  |
| 70 | 2.8.c | 1 | `docs/concepts/sagas.md` | § Semantic Locks in Practice and § Create Functionality Sagas both showed `cmd.setForbiddenStates(...)` called on the plain domain command, but `setForbiddenStates` is declared on `SagaCommand`, not on `Command` - the snippet does not compile. The second one also passed `List.of(...)`, which infers `List<{Aggregate}SagaState>` and will not convert to the `List<SagaAggregate.SagaState>` parameter. First use of a foreign-aggregate guard in this application, so nothing had exercised either snippet before. Both fixed to wrap in `SagaCommand`, with the invariance trap stated once. | fixed | a17ec02fd |
| 71 | 2.8.d | 2 | `quizzes-full-2-aggregate-grouping.md` §2/§4 | `QuizAnswerQuestionAnswerEvent` is anchored on `quizAnswerAggregateId` and `Tournament` is its only declared consumer, but no functionality writes `TournamentParticipantQuizAnswer.quizAnswerAggregateId` and none links a QuizAnswer to a tournament participant, so the anchor a Tournament subscription would need can never be constructed - while §4's own note asks the Tournament to link the QuizAnswer "on the first answer", which requires receiving the event first. The circularity is in the application spec, not the harness, and the three ways out (re-anchor the event, drop the subscription, add a linking operation) differ in what ships. Human chose re-anchoring on `quizAggregateId`, with the participant discriminated by `studentAggregateId` in the service ByEvent method. Reopened 2.7.c for the event constructor and its T2 payload assertion. Harness gap itself left open - no docs/skills edit made this session. | deferred |  |
| 72 | 2.8.d | 1 | `docs/concepts/events.md`, `.claude/skills/implement-aggregate/session-d.md` | Both stated that the sagas profile never calls `subscribesEvent()` and that overriding it "has no effect", but `EventService.getSubscribedEvents` applies it as a `.filter` on every poll - so the rule against overriding it rested on a mechanism that does not exist, and an agent that checked the framework would conclude the rule was void. The rule is right for other reasons (the override is state-blind and duplicates the query predicate); the false mechanism was replaced with those. | fixed | 2b2b67ad3 |
| 73 | review | 2 | `.claude/skills/review-artifacts/SKILL.md` | Found by `docs/reviews/review-2026-08-10-2.md` M2. `AGENTS.md` § "Harness evolution" defines the Type 1 / Type 2 / `2-fw` gate the self-healing loop turns on, and `implement-aggregate/SKILL.md`, `implement-aggregate-full/SKILL.md` and `.claude/agents/aggregate-slice.md` each read it in full at runtime, but it appeared in none of Step 1.b's `find` commands, was never consistency-checked, and sat outside Check 4's pathspec - the same gap `.claude/agents/` had one level down. How far to widen was a design decision, not a contradiction: repo-root also holds `README.md` and `CLAUDE.md`. Human chose `AGENTS.md` only, with the inclusion criterion stated as "repo-root markdown a skill or agent contract reads at runtime" rather than a wildcard over root `*.md`. Step 6.a's fragile `PATHSPEC[:3]` slice became an explicit `TREES` list in the same edit. | fixed | bdc39dbb0 |
| 74 | review | 1 | `.claude/skills/harness-retrospective/SKILL.md` | Found by `docs/reviews/review-2026-08-10-2.md` m2 and m1. Step 3.a.i called `git log ... -- docs .claude/skills` "the run's complete harness delta" while Step 8 delegates Check 4 to `review-artifacts`, which covers `.claude/agents` too, so a `harness:` commit touching only an agent contract escaped the "every commit is referenced by some row" cross-check. Step 2 also decided run-completeness from `grep -c -- "- \[ \]"`, which `_shared/conventions.md` § "Commands whose output feeds a verdict" forbids. Pathspec widened to match the review scope; the count now comes from `python3` and reports lines scanned so a zero is interpretable. Step 3.a's misfiled-artifact test was widened to match the new scope. | fixed | a3cea15dc |
| 75 | review | 1 | `.claude/skills/boot-strap/SKILL.md` | Found by `docs/reviews/review-2026-08-10-2.md` m1. Step 6.1 asserted the scaffold was fully substituted from `rg -n '\{\{' applications/{app-name}` returning no matches - the exact "clean verdict from a possibly-rewritten command" shape `_shared/conventions.md` § "Commands whose output feeds a verdict" forbids, since the hook can turn `rg` into `grep` and a command that failed outright also prints nothing. Replaced with a `python3` walk that reports files scanned alongside surviving tokens. | fixed | 7b8afb6e8 |
| 76 | review | 1 | `docs/concepts/rule-enforcement-patterns.md`, `docs/concepts/aggregate.md` | Found by `docs/reviews/review-2026-08-10-2.md` Check 3. § P2 stated the upstream/downstream direction in prose and pointed at `events.md`, so P2's classification consequence - which aggregate carries the subscription - had to be read from a different file than the one an agent classifies from; and § Repositories left the `{Aggregate}CustomRepositorySagas` body as a comment describing the delegation instead of showing it. Added a consumer-side `getEventSubscriptions()` fragment labelled illustrative with the owning section named, and one worked delegation, keeping the `service.md` pointer that owns the JPQL. | fixed | 02a527d8a |
| 77 | review | 1 | `.claude/skills/classify-and-plan/SKILL.md` | Found by `docs/reviews/review-2026-08-10-3.md` M1. Step 7's 2.N.b file-list template wrote `{src}commands/{aggregate}/Get{Query}Command.java`, but Step 7's own substitution rule defines `{Query}` as the read-op name that already carries `Get` ("GetOpenShipments"), so the template substitutes to `GetGetOpenShipmentsCommand.java` - while `session-b.md` § "One `{Query}Command.java` per read functionality" uses the bare token, as do the saga and test entries on the same template line. `session-b.md` also tells the slice to "match plan.md file list exactly", so a literal parse would have propagated the doubled prefix into real filenames. It never did: the classify agent silently corrected it and `plan.md` emitted `GetCoursesCommand.java`, which is why eight aggregates went by without exposing it. Token made bare. | fixed | bad7b7b8e |
| 78 | review | 1 | `.claude/skills/classify-and-plan/SKILL.md` | Found by `docs/reviews/review-2026-08-10-3.md` Check 3. Step 7's 2.N.d row listed only the four new notification/eventProcessing files plus the T3 test, omitting the three files `implement-aggregate-full/SKILL.md` § "Shared files per session type" requires every `d` session to modify - `{Aggregate}Functionalities.java`, `{Aggregate}Service.java` and `{Aggregate}.java`. plan.md is a blueprint, so this was legal rather than wrong, but it guaranteed a Step 5b plan.md amendment on every `d` session of every run. Added as "(appended)" entries in the style the 2.N.c row already uses. | fixed | 727f359d3 |
| 79 | review | 1 | `AGENTS.md` | Found by `docs/reviews/review-2026-08-10-3.md` M2. § Module Map placed `reviews/` under `applications/{app-name}/`, but no skill has ever written there: `review-artifacts/SKILL.md` and `harness-retrospective/SKILL.md` both bind their report path under `docs/reviews/` and both `mkdir -p docs/reviews`. `applications/quizzes-full-2/` holds `retros/`, `logs/`, `plan.md`, `harness-log.md` and the spec pair, and no `reviews/` - so an agent asked to find or file a review was pointed at a directory that has never existed, while `logs/`, which does exist, went unmentioned. Row corrected and a sentence added naming `docs/reviews/` as the home of both report kinds. | fixed | 05cfa8263 |
| 80 | review | 1 | `.claude/skills/implement-aggregate-full/SKILL.md` | Found by `docs/reviews/review-2026-08-10-3.md` Check 3. The § Slice brief `VERIFY` block hard-coded `-Dtest=<this slice's T4 test>,{Aggregate}ServiceTest`, but only sessions `b` and `c` produce a T4 test - `a` produces `{Aggregate}IntraInvariantTest` (T1) and `d` produces `{Aggregate}InterInvariantTest` (T3), so for half the session types the manager had to silently reinterpret a placeholder the brief says must be substituted literally. Placeholder widened to "this slice's own test class(es)" with a per-session-type table beneath, derived from `implement-aggregate/SKILL.md`'s session-to-test-file map. | fixed | ba6f1b437 |
| 81 | review | 1 | `.claude/skills/implement-aggregate/SKILL.md` | Found by `docs/reviews/review-2026-08-10-3.md` Check 3. The § Steps 5-8 table announces "It owns, in this order" and then listed the harness-log step as `7.d`, after step 7, while `_shared/session-completion.md` § "Retro assembly" opens with "Run § 'Harness-log rows' first" because the retro's `## Harness Changes` section cites the rows. A parenthetical carried the correction, so the row order contradicted both the table's own preamble and the file it delegates to. Renumbered `6b` and moved above the retro row; the parenthetical is now redundant and was dropped. | fixed | c9531411d |
| 82 | review | 1 | `docs/concepts/service.md` | Found by `docs/reviews/review-2026-08-10-3.md` Check 3. § Mutate method with optional sub-collection parameter justified the pattern as "disproportionate when the update intent covers only scalar fields", a word that reads as the decision test but names no threshold; the real criterion is the § When to use / § When not to use pair directly beneath ("is the sub-collection update the primary intent?"). Reworded to state the cost and point at the criterion that actually decides. | fixed | 22548c52b |
| 83 | review | 1 | `.claude/skills/classify-and-plan/SKILL.md` | Second instance of row 77, found by the post-fix sweep rather than by `docs/reviews/review-2026-08-10-3.md`, which named only the Step 7 file-list site. Step 8's plan.md output template listed read functionalities as `- Get{Query}({args})`, doubling the prefix the same way; the review's mechanical extraction covered path-shaped references and missed the bare signature line. Also silently corrected in practice - `plan.md` emitted `GetCourseById(...)`. Token made bare. `Get{Aggregate}ByIdCommand.java` is untouched at both sites: it is a literal, not a `{Query}` substitution. | fixed | 43960fe5f |
| 84 | review | 1 | `docs/concepts/rule-enforcement-patterns.md` | Found by `docs/reviews/review-2026-08-10-3.md` Check 3. § Decision Guide Step 4 shipped a Java block for case 2 (the owned-entity P3 guard) but left case 1, the P4a default, as the bare instruction "Add the § P4 comment at the saga step" - an obligation whose shape is defined two hundred lines away in § P4a, so the agent had to cross-jump mid-decision to learn what it was being asked to write. Case 1 now carries the comment inline, indented into the list item so it does not break the ordered list, and names the § P4 documentation obligation it discharges. | fixed | e8b3e0637 |
| 85 | review | 1 | `docs/architecture.md` | Found by `docs/reviews/review-2026-08-10-3.md` Check 3. § R8 was prose only while R7 directly above it carried a WRONG/RIGHT Java pair. R8 is the restriction most easily violated by accident - a saga reaching downstream for "just one field" - and the wrong shape is not self-evidently wrong on the page, since it is an ordinary `sendAndCollect` differing from a legal one only in the direction of the dependency edge. Added a WRONG/RIGHT pair whose RIGHT branch updates the publisher only and points at `concepts/events.md` § Canonical Wiring Snippet for the subscription side rather than restating it. | fixed | 112f3eec1 |
| 86 | review | 1 | `docs/concepts/commands.md` | Found by `docs/reviews/review-2026-08-10-3.md` Check 3. The two `null`-`rootAggregateId` cases (create command, bulk read filtered by a non-PK field) were stated in blockquote prose against a worked example whose `super(...)` passes a real id, so an agent copying the example had to infer *which* argument becomes `null` - and the bulk-read blockquote's warning not to pass the filter value shows the wrong inference was available. Added a create-command constructor showing `super(unitOfWork, serviceName, null)` and a one-line echo under the bulk-read case naming the same position. | fixed | 2c87432c7 |
| 87 | review | 1 | `docs/concepts/events.md` | Found by `docs/reviews/review-2026-08-10-3.md` Check 3. § Cascade Invalidation Pattern gave steps 1 and 2 code and left step 3 - the consumer side, where the pattern actually closes - as one prose sentence offering "either removing the sub-entity from a collection or cascading their own invalidation" with no criterion for choosing. The reader had to reassemble subscription + ByEvent from § Canonical Wiring Snippet without being told which branch applied. Step 3 now carries the subscription line and the ByEvent method in the § ByEvent sagaState guard shape, plus the criterion: cache-only consumers remove the sub-entity and stop, consumers non-functional without the reference re-enter step 1 one level down. | fixed | 176df6262 |
| 88 | review | 1 | `.claude/skills/classify-and-plan/SKILL.md` | Found by `docs/reviews/review-2026-08-10-3.md` Check 3. Step 4's parser heuristic is labelled "mechanical only" and every other branch is - a same-aggregate check and two keyword lists - but the P4a and P4b branches called `rule_is_implicitly_enforced_by_fetch` and `rule_holds_by_shared_value_in_same_saga`, predicates defined nowhere, so an unattended parse had no test at all for either and fell through to the P3 default. Human decision (2026-08-10) was to give each a proxy rather than route both branches to NEEDS_REVIEW, which would halt an unattended run on every P4 candidate. P4a's proxy: the predicate names a lookup §4 already describes the saga performing, keyed so the command throws when unmet. P4b's: the rule equates a field across two aggregates one §4 saga writes in the same run. Both stated as parse proxies, with `rule-enforcement-patterns.md` § Decision Guide winning any disagreement. | fixed | 8baa57850 |
| 89 | review | 1 | `.claude/skills/implement-aggregate-full/SKILL.md` | Found by `docs/reviews/review-2026-08-10-4.md` Major. The § Slice brief `-Dtest` table (added by row 80) named `{Query}Test` unconditionally for session `b`, but `session-b.md` line 9 says an aggregate whose plan.md reads "Read functionalities: none" produces no `{Query}Test.groovy` at all - so the manager had no value for a brief field the same skill declares fatal if unfilled, and would have had to invent one. Row now carries the omit-when conditional in the style the session-`c` row already uses for `{Operation}CompensationTest`, naming `{Aggregate}ServiceTest` (which holds that session's T2 not-found cases) as the slice's whole verification in that case. | fixed | eac1d0e5f |
| 90 | premerge | 1 | `.claude/skills/review-artifacts/SKILL.md`, `AGENTS.md` | Found by `docs/reviews/harness-retro-quizzes-full-2-2026-08-11.md` gap G1. Six of the run's 49 Type 1 edits originated from `/review-artifacts` Check 3, which collects missing examples and ambiguous guidance - silences by construction, so `AGENTS.md` § "Harness evolution" classifies them Type 2 and they should have halted, yet neither artifact ever said which type a given check's findings carry, and the skill's own Action Items severities (Critical/Major/Minor) read as a repair licence. Step 7 now carries a per-check type table naming Check 3 as Type 2 that must halt, the Check 3 report section carries the same note inline, and § "Harness evolution" states the rule directly: a missing example, a cross-reference or imprecise wording is silence however obvious the improvement looks. | fixed | f33988d4d |
| 91 | premerge | 1 | `.claude/skills/_shared/conventions.md` | Found by `docs/reviews/harness-retro-quizzes-full-2-2026-08-11.md` gap G5. § "Run the test suite" prose mandates `> build.log 2>&1` and explains why a redirection is the only form whose exit code still reaches `$?`, but neither the block it labels "the mandated form for session-level and final verification" nor the permitted narrow form carried the redirect - so an agent copying the block it is told to copy gets exactly the un-redirected command row 35 was filed to eliminate, and a truncated stdout takes the `MAVEN_EXIT` line with it. This bit the retrospective's own build. Both blocks now carry the redirect. | fixed | 94f8134b1 |
| 92 | premerge | 1 | `AGENTS.md` | Found by `docs/reviews/harness-retro-quizzes-full-2-2026-08-11.md` gap G6. § "Harness evolution" asserts the harness delta of a run is exactly `git log --oneline docs/ .claude/`, which presupposes every such commit is logged, yet the section stated no obligation to log one - and 12 of this run's `harness:` commits carry no row (one of them closing 13 review findings at once), so the asserted equivalence between the commit range and the log is false as written. The section now states the obligation directly: every `harness:` commit carries at least one row, and a commit closing N review findings carries N rows or one row naming all N. Doc rule only - no enforcement mechanism, which is future `/review-artifacts` scope. | fixed | b2cf9e3c3 |
| 93 | premerge | 1 | `docs/concepts/rule-enforcement-patterns.md` | Found by `docs/reviews/harness-retro-quizzes-full-2-2026-08-11.md` gap G2. § Decision Guide Step 4 was scoped by its own heading to a *write* method's out-of-domain input, so it read as the harness's whole answer to plan.md silence while covering one class of it; rows 64 and 67 hit two siblings with no default at all - `GetOpenedTournamentsForExecution` / `GetClosedTournamentsForExecution` specified only as "currently open" / "have closed" over an aggregate carrying `startTime`, `endTime` and `cancelled`, and `CreateTournament` drawing `numberOfQuestions` questions from a course that may hold fewer - and both were settled by hand with the harness gap left open. Step 4 is now § "When plan.md is silent" with the existing default as Case A and two mandatory siblings: Case B (read predicate - prefer the reading under which the specified result sets partition, a field neither description names excludes from both) and Case C (quantity shortfall - throw on a named constant, never truncate or pad, guarded in the service per § Common Mistakes). The record-in-plan.md-rule-list obligation and the halt for the undecidable remainder are hoisted to cover all three. | fixed | 9a3bb71a3 |
| 94 | premerge | 1 | `docs/concepts/sagas.md` | Found by `docs/reviews/harness-retro-quizzes-full-2-2026-08-11.md` gap G3. § Create Functionality Sagas covered the create step's own mechanics and nothing the created aggregate needs in order to exist, and a scan of all 485 lines found no text on either point. Row 65: `CreateTournament` creates the tournament's `Quiz`, but nothing settled where the quiz's `title`, `resultsDate` and `quizType` come from - none is a parameter, none is defaulted - so widening the signature, defaulting on `Quiz` and deriving in the saga were all available and differ in the published API. Row 66: item 2 requires a removal compensation whenever a later step follows, and the Shape 2 snippet assumes `Delete{Aggregate}Command` exists, but `Quiz`'s session 2.6 shipped no delete at all, and the doc named nobody to write it. Both were settled by hand with the harness gap left open. § Create Functionality Sagas now carries two mandatory subsections: field provenance ordered derive → domain sentinel → widen the signature, with defaulting on the created aggregate forbidden because it applies to the direct create path too; and the compensating delete, owned by the saga's session via reopening the created aggregate's session `c`, with reordering-to-dodge and leaking the aggregate both named as forbidden. | fixed | 3300e95a8 |
