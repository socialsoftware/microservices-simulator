# Artifacts Review — 2026-08-01

**Date:** 2026-08-01
**Skill files reviewed:** 14 (`.claude/skills/review-aggregate-vs-quizzes/SKILL.md` excluded — retired, historic record only)
**Doc files reviewed:** 11 (`docs/reviews/**` excluded as generated output)
**Verdict:** Issues requiring action

> **Clean** = all alignment checks pass; only improvement opportunities found.
> **Minor issues** = inconsistencies present but none cause incorrect agent output.
> **Issues requiring action** = any inconsistency that would cause an agent to produce
>   incorrect output.

This is the first run of `/review-artifacts` in its static pre-flight form (A6.1). Retro chasing was
removed, so the whole review set is `docs/**` + `.claude/skills/**` and the ground truth is the
harness's own internal consistency plus the `simulator/` API it describes.

---

## Executive Summary

Six Critical findings: four session sub-files prescribe code shapes that contradict the concept doc
they cite and would not compile or would be flagged by `/review-aggregate` (`session-b.md` mutate
recipe, CommandHandler shape and saga base class; `session-d.md` polling call), one `classify-and-plan`
finding where the generated `plan.md` aggregate heading omits the ordinal that
`conventions.md` § "Resolve aggregate context" halts without, and one bean-wiring arity mismatch in
`session-d.md`. Eight Major findings, led by a set of stale `simulator/` package paths across four
concept docs that predate the `ms.transactional.sagas → ms.transaction.sagas` rename that
`boot-strap/templates/README.md` itself documents. All Critical and Major findings were fixed in this
same session per the Phase A-iv instruction; the tables below record what was found and what changed.

---

## Check 1 — Path Validity

No `docs/...` or `.claude/skills/...` reference resolved to a missing file. The failures were all in
cited `simulator/` package paths, which several skills turn directly into Java `import` statements.

| Source file | Referenced path | Status | Notes |
|-------------|----------------|--------|-------|
| `docs/architecture.md` § Key Simulator Classes | `simulator/.../ms/domain/aggregate/Aggregate.java` | Broken | actual: `ms/aggregate/Aggregate.java` — **fixed** |
| `docs/architecture.md` § Key Simulator Classes | `simulator/.../ms/sagas/aggregate/SagaAggregate.java` | Broken | actual: `ms/transaction/sagas/aggregate/` — **fixed** |
| `docs/architecture.md` § Key Simulator Classes | `simulator/.../ms/coordination/workflow/WorkflowFunctionality.java` | Broken | actual: `ms/coordination/WorkflowFunctionality.java` — **fixed** |
| `docs/concepts/aggregate.md` § Base Class | `.../ms/domain/aggregate/Aggregate.java` | Broken | **fixed** |
| `docs/concepts/aggregate.md` § Variants | `ms.sagas.aggregate.SagaAggregate` | Broken | **fixed** |
| `docs/concepts/sagas.md` § Key Classes | `ms/sagas/unitOfWork/`, `ms/sagas/workflow/` (4 rows) | Broken | all under `ms/transaction/sagas/` — **fixed** |
| `docs/concepts/events.md` § Event Classes | `Event` from `ms.domain.event` | Broken | actual: `ms.aggregate.Event`; `session-b.md` already had it right — **fixed** |
| `docs/templates/aggregate-grouping-template.md` § 4 | `../../docs/concepts/events.md` | Resolves | non-idiomatic (`../concepts/events.md` is direct) but correct — Minor, not fixed |
| all other `docs/`, `.claude/skills/` references | — | OK | nothing to report |
| all `applications/{app-name}/...` references | — | Template | every occurrence is written as a template pattern; no literal application name hard-coded in any skill or doc |

---

## Check 2 — Pattern Alignment (P1–P4 and R1–R8)

### P1–P4 Consistency

Authoritative one-liners taken from `docs/concepts/rule-enforcement-patterns.md` § Quick Reference
and § Decision Guide.

| Location | Pattern | Claim | Authoritative definition | Verdict |
|----------|---------|-------|--------------------------|---------|
| `docs/architecture.md` — pattern selection table | P1/P2/P3/P4 | four rows matching the Quick Reference verbatim in substance | same | Consistent |
| `docs/architecture.md` — Request Lifecycle | P1/P2/P3 | P3 before mutation inside `@Transactional(SERIALIZABLE)`; P1 at commit; P2 async ~1 s | same, § Enforcement Lifecycle | Consistent |
| `.claude/skills/classify-and-plan/SKILL.md` — Step 4 | P1–P4a/b | delegates to the doc's § Decision Guide, keeps only a mechanical parser heuristic and labels it as such | same | Consistent |
| `.claude/skills/implement-aggregate/session-a.md` | P1 | `verifyInvariants()` throws `{AppClass}Exception`; `final` fields need no test | same | Consistent |
| `.claude/skills/implement-aggregate/session-b.md` | P3 | own-table uniqueness **or** saga-assembled DTO validation, at the top of the service method | same | Consistent |
| `.claude/skills/implement-aggregate/session-d.md` | P2 | consumer-side caching, invariant re-check in the ByEvent path, never blocking | same | Consistent |
| `.claude/skills/review-aggregate/SKILL.md` — Step 6 | P1–P4 | one check per classification, all matching | same | Consistent |
| `.claude/skills/adversarial-review-aggregate/SKILL.md` — Steps 3, 8 | P1 + Family F re-derivation | Family F re-derives from the doc and the domain model alone, explicitly not from plan.md | same | Consistent |

### R1–R8 Coverage

Authoritative one-liners from `docs/architecture.md` § Architectural Restrictions. Family D is
`.claude/skills/adversarial-review-aggregate/SKILL.md` Step 6.

| Restriction | Expected location | In Family D table? | Mentioned? | Consistent? | Notes |
|-------------|------------------|--------------------|------------|-------------|-------|
| R1 — service loads only own aggregate | `service.md`, `session-b.md` | Yes | Yes | Yes | — |
| R2 — service injects only own components | `service.md`, `session-b.md` | Yes | Yes | Yes | Family D's allowed-injection list matches `service.md` § Injected Dependencies exactly |
| R3 — cross-aggregate flow via DTOs only | `service.md`, `session-b.md` | Yes | Yes | Yes | — |
| R4 — `setForbiddenStates` on mutating saga steps | `sagas.md`, `session-b.md` | Yes | Yes | Yes | Family D correctly reproduces the primary/foreign split from § R4 Decision Table |
| R5 — `getEventSubscriptions()` in consumer only | `events.md`, `session-d.md` | Yes | Yes | Yes | Family D adds the transitive-DAG definition of "upstream"; stricter than, not in conflict with, the doc |
| R6 — `verifyInvariants()` must not read from DB | `aggregate.md`, `session-a.md` | Yes | Yes | Yes | — |
| R7 — DTOs are immutable value objects | `service.md`, `session-b.md` | Yes | Yes | Yes | — |
| R8 — functionalities only send commands upstream | `commands.md`, `session-b.md` | Yes | Yes | Yes | — |

All eight are present in the Family D table. No restriction is documented-but-unenforced.

---

## Check 3 — Improvement Opportunities

### Missing Examples

| Doc | Section | Why an example would help |
|-----|---------|---------------------------|
| `docs/concepts/commands.md` | § Routing Commands | the `@Bean` method whose name must equal `{serviceName}CommandHandler` is described in prose only; the actual `BeanConfigurationSagas` snippet lives in `session-b.md`, so a reader of the doc alone cannot see the constraint being satisfied |
| `docs/concepts/service.md` | § Custom Repository — Latest-Active-Version Query | shows the JPQL but not the `{Aggregate}CustomRepositorySagas` method that calls it, which is where the R1/R2 injection rules actually bite |
| `docs/concepts/events.md` | § ByEvent sagaState guard | the guard body is given, but not the `{Consumer}Functionalities` method around it that opens and commits its own UoW |

### Ambiguous Guidance

| File | Step | Ambiguous phrase | Missing decision criterion |
|------|------|------------------|---------------------------|
| `.claude/skills/implement-aggregate/session-c.md` | § `{Query}FunctionalitySagas.java` | "Extends the read-functionality base class from simulator core" | named no class; **fixed** — now names `WorkflowFunctionality` and the `buildWorkflow` shape |
| `.claude/skills/implement-aggregate/session-b.md` | § `{Op}FunctionalitySagas.java` | "or the appropriate saga base class from the simulator core" | same; **fixed** |
| `.claude/skills/implement-aggregate/session-a.md` | § `{Aggregate}SagaState.java` | "only when the saga has additional steps **after** the primary write step" | the criterion is stated but the reader must have written the saga (session `b`) to evaluate it; unresolvable in session `a` — Minor, not fixed |
| `.claude/skills/implement-aggregate/SKILL.md` | Step 5b | "For **ambiguous** cases (you are unsure whether a file belongs)" | no test for ambiguity beyond the agent's own uncertainty — Minor, not fixed |

---

## Action Items

All Critical and Major rows were fixed in this session (Phase A-iv, harness unfrozen — no
`applications/*/plan.md` carries an unchecked box).

| Priority | Category | File | Finding | Suggested Fix |
|----------|---------|------|---------|---------------|
| Critical | Contradicts concept doc | `.claude/skills/implement-aggregate/session-b.md` § `{Aggregate}Service.java` | Mutate recipe read *"fetch the target aggregate via `{Aggregate}CustomRepositorySagas`, mutate its fields via setters, call `verifyInvariants()`, then `unitOfWork.registerChanged(aggregate)`"*. Contradicts `docs/concepts/service.md` § Copy-on-Write Rule (*"Never mutate the aggregate instance returned by `aggregateLoadAndRegisterRead`"*) and § Method Patterns → Mutate method; `unitOfWork.registerChanged(aggregate)` is not the API — `UnitOfWorkService.registerChanged(Aggregate, U)` is; and `session-a.md` says the constructor *"does **not** call `verifyInvariants()` — the framework calls it automatically via `registerChanged`"*. In-place mutation is the exact latent bug `service.md` § Copy-on-Write Rule warns about. | **Fixed** — replaced with the load → factory-copy → mutate-copy → `unitOfWorkService.registerChanged(copy, unitOfWork)` shape, pointing at `service.md` § Method Patterns |
| Critical | Contradicts concept doc | `.claude/skills/implement-aggregate/session-b.md` § `{Aggregate}CommandHandler.java` | Prescribed *"One `@CommandHandler` method per command class"* and *"Each handler: creates a `UnitOfWork`, calls the matching service method, commits the `UnitOfWork`"*. The real base class `pt.ulisboa.tecnico.socialsoftware.ms.messaging.CommandHandler` declares only `getAggregateTypeName()` and `handleDomainCommand(Command)`; `docs/concepts/commands.md` § Routing Commands shows the single-switch form taking `cmd.getUnitOfWork()`, and `/review-aggregate` Step 4 audits against that form. | **Fixed** — replaced with the two-override switch shape plus the bean-name constraint |
| Critical | Nonexistent class | `.claude/skills/implement-aggregate/session-b.md` § `{Op}FunctionalitySagas.java` | *"Extends `FunctionalitySagas` (or the appropriate saga base class from the simulator core)"* and *"Steps are defined … in the constructor or `defineSteps()` method"*. No `FunctionalitySagas` or `defineSteps()` exists; `docs/concepts/sagas.md` § Write Workflow Structure and `/review-aggregate` Step 4 both require `WorkflowFunctionality` + `buildWorkflow(...)`. | **Fixed** |
| Critical | Nonexistent method | `.claude/skills/implement-aggregate/session-d.md` § `{Aggregate}EventHandling.java` | *"for each event found, calls `{Aggregate}EventHandler.handle{Event}(event)`"*. No such method: the handler's override is `handleEvent(Integer subscriberAggregateId, Event event)`, and `docs/concepts/events.md` § Polling gives the whole method body as `eventApplicationService.handleSubscribedEvent({Event}.class, {aggregate}EventHandler)`. | **Fixed** |
| Critical | Generator/consumer mismatch | `.claude/skills/classify-and-plan/SKILL.md` Step 8 | Emitted the aggregate details heading as `### {Aggregate}`, with no ordinal. `.claude/skills/_shared/conventions.md` § "Resolve aggregate context" says *"Verify that a section `### N. {Aggregate}` exists in `plan.md`. If not found, halt"* — and `implement-aggregate` Step 2, `review-aggregate` Step 2.a, `adversarial-review-aggregate` Step 2.b and `review-tests` Step 2 all read `### {N}. {Aggregate}`. Every Phase 2/3/4 skill would halt on a freshly generated plan.md. | **Fixed** — heading is now `### {N}. {Aggregate}`, with a note stating why the ordinal is load-bearing |
| Critical | Compile error | `.claude/skills/implement-aggregate/session-d.md` § Update BeanConfigurationSagas | Bean method built `new {Aggregate}EventHandler({aggregate}Repository)` — one argument — while the same file's handler spec (and `events.md` § Handler) require `(repository, {Aggregate}EventProcessing)`. | **Fixed** — both the snippet and the note beneath it |
| Major | Stale simulator paths | `docs/architecture.md`, `docs/concepts/aggregate.md`, `docs/concepts/sagas.md`, `docs/concepts/events.md` | Nine cited paths/packages predate a library rename. `ms.domain.aggregate` → `ms.aggregate`; `ms.sagas.*` → `ms.transaction.sagas.*`; `ms.coordination.workflow.WorkflowFunctionality` → `ms.coordination.WorkflowFunctionality`; `Event` from `ms.domain.event` → `ms.aggregate.Event`. `.claude/skills/boot-strap/templates/README.md` § Maintenance documents this exact class of drift (*"`ms.transactional.sagas` → `ms.transaction.sagas`"*) and the docs were never updated with it. `events.md`'s wrong package becomes a literal broken `import`. | **Fixed** — all nine corrected against `simulator/src/main/java` |
| Major | Contradiction within one file | `docs/concepts/commands.md` | Line ~30 called `serviceName` *"the `CommandHandler.getAggregateTypeName()` value that routes to the right handler (e.g. `"Tournament"`)"*; § Routing Commands says the opposite — *"`getAggregateTypeName()` … is **not** the routing key"* — and that the bean name is `ServiceMapping.{AGGREGATE}.getServiceName() + "CommandHandler"` (lowercase camelCase). Following the first passage hardcodes the PascalCase name and produces the silent bean-lookup failure the same doc warns about for multi-word aggregates. | **Fixed** — the bullet now names `ServiceMapping` as the sole source and cross-references § Routing Commands |
| Major | Assertion-form split | `.claude/skills/implement-aggregate/session-a.md`, `.claude/skills/review-tests/SKILL.md` Step 6 | Both prescribed `ex.getErrorMessage() == {RULE_NAME}` while `docs/concepts/testing.md` — which declares itself the single source of truth for tier rules and templates — uses `ex.message == <RULE_NAME>` throughout, as do `service.md` § Exception-Throw Convention, `session-b.md`, `session-c.md` and `review-tests` Step 5.C. The two accessors differ whenever an exception is thrown with a format argument. Previously reported as Minor **M2** in `docs/reviews/review-2026-07-23.md` and still open. | **Fixed** — both now use `ex.message`, with `session-a.md` pointing at the owning sections |
| Major | Naming split | `.claude/skills/review-tests/SKILL.md` Step 3, `.claude/skills/review-aggregate/SKILL.md` Step 7 | Both expected T4 write tests named `{Operation}{Aggregate}Test.groovy`, while the generator (`classify-and-plan` Step 7) and the producer (`session-b.md`) emit `{Operation}Test.groovy`, matching `docs/workflow.md` § Test naming and `testing.md`'s `<FunctionalityName>Test`. Both reviewers would report correctly named files as missing. | **Fixed** — both reviewers now expect `{Operation}Test.groovy` as listed in plan.md |
| Major | Contradicts concept doc | `.claude/skills/implement-aggregate/session-c.md` § Read methods | *"fetch aggregate via `{Aggregate}CustomRepositorySagas.getLatestVersion(...)` … throw `{AppClass}Exception` with `AGGREGATE_NOT_FOUND` if not found"*. Contradicts `service.md` § Method Patterns → Read method (`aggregateLoadAndRegisterRead`), the same file's line 52, and the same file's own Path A / Path B rule, which says a by-ID read must surface `SimulatorException`. A by-ID read written this way makes the prescribed Path A T2 test fail. | **Fixed** — split into the Path A (by-PK) and Path B (composite-key) shapes explicitly |
| Major | Wrong API | `.claude/skills/implement-aggregate/session-d.md` (two code blocks) | `unitOfWork.registerChanged(aggregate)` — same non-existent API as the session-b finding. | **Fixed** — both now `unitOfWorkService.registerChanged(aggregate, unitOfWork)` |
| Major | Rule contradiction | `.claude/skills/boot-strap/SKILL.md` Step 5 | Step 5 copies `.mvn/maven.config` from a sibling application (`ls -d applications/*/.mvn`), which the same file's intro (*"Never read another application during bootstrap"*) and `conventions.md` § "Application isolation" forbid without qualification. An agent that follows the isolation rule literally skips a step it is told to run; one that follows Step 5 breaches a rule it was told is absolute. | **Fixed** — Step 5 now states the exemption explicitly, scoped to that single untracked machine-config file |
| Major | Unfollowable pointer | `docs/concepts/testing.md` (3 places), `docs/concepts/sagas.md` (2 places) | Pointers to reference-application test and saga classes by *class name* rather than path — *"Canonical pattern: `TournamentIntraInvariantTest`"*, *"see `EnrollStudentInExecutionCompensationTest`, `CreateQuizAnswerUserLockCompensationTest`, and `AddParticipantCompensationTest` for examples"*, `UpdateCourseTest`, `DeleteCourseFunctionalitySagas`/`DeleteCourseCompensationTest`, `CreateTournamentFunctionalitySagas`. A4 removed the path-shaped citations but these survived by naming no path; under § "Application isolation" an agent cannot open any of them, so each is an instruction to consult a file it is forbidden to read. | **Fixed** — the pointers were dropped and the surrounding rules restated in application-neutral terms; no rule content was lost |
| Minor | Emoji in harness text | `docs/concepts/sagas.md` § Semantic-lock release, `.claude/skills/implement-aggregate/SKILL.md` Step 7.b retro template, `.claude/skills/classify-and-plan/SKILL.md` Steps 5.5 / 7 | `⚠️` appears in doc and template text while every skill's Hard Rules end with *"No emojis"*, and `implement-aggregate`'s own retro hard rule 5 says *"No emojis, no hype"* about the template that contains one. | Removed from `sagas.md` while fixing that section; left in place elsewhere — the `⚠️ DEFERRED` marker in `classify-and-plan` is a token `session-b.md` matches on, so changing it is a coordinated edit, not a cosmetic one |
| Minor | Non-idiomatic link | `docs/templates/aggregate-grouping-template.md` § 4 | `../../docs/concepts/events.md` resolves correctly (via the repo root) but reads as a mistake next to `docs/templates/`'s sibling `../concepts/`. | Not fixed — no functional impact |
| Minor | Unresolvable at authoring time | `.claude/skills/implement-aggregate/session-a.md` § `{Aggregate}SagaState.java` | The criterion for including `IN_{OPERATION}` depends on the saga's step structure, which session `a` has not written yet. | Not fixed — would need a plan.md field carrying the decision forward from Phase 1 |
