Aggregate Customer {
    @GenerateCrud

    Root Entity Customer {
        String name
        String email
        Boolean active = true

        invariants {
            check nameNotBlank { name.length() > 0 } error "Customer name cannot be blank"
            check emailNotBlank { email.length() > 0 } error "Customer email cannot be blank"
        }
    }
}
