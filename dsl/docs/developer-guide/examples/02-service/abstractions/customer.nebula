// Example 02 - Service Layer with CRUD
// Customer with @GenerateCrud annotation to generate service methods

Aggregate Customer {
    @GenerateCrud;

    Root Entity Customer {
        String name;
        String email;
        Address address;
    }

    Entity Address {
        String street;
        String city;
        String zipCode;
    }
}
