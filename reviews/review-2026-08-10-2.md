# Artifacts Review — 2026-08-10 (second run)

**Date:** 2026-08-10
**Skill files reviewed:** 13 (`.claude/skills/**/*.md`)
**Scaffold templates reviewed:** 9 (`.claude/skills/boot-strap/templates/*.template`)
**Agent contract files reviewed:** 1 (`.claude/agents/aggregate-slice.md`)
**Doc files reviewed:** 11 (`docs/architecture.md`, `docs/workflow.md`, `docs/concepts/*` ×7, `docs/templates/*` ×2; `docs/reviews/` excluded as generated output)
**Verdict:** Minor issues

> **Clean** = all alignment checks pass; only improvement opportunities found.
> **Minor issues** = inconsistencies present but none cause incorrect agent output.
> **Issues requiring action** = any inconsistency that would cause an agent to produce
>   incorrect output.

---

## Executive Summary

Re-run after the repairs made against `review-2026-08-10.md`. All three Critical rows and all four
Major rows of that report are resolved and verified against `simulator/`: `setForbiddenStates` is now
shown on `SagaCommand` with an interface-typed list in both `commands.md` and the R4 prose,
`session-d.md`'s two service snippets are copy-on-write and DTO-free, the explicit
`verifyInvariants()` is gone from the ByEvent path, `events.md` owns the aggregate-id ByEvent
signature, and `review-artifacts` now enumerates `.claude/agents/**` and runs Check 4 through a
`python3` script with `docs/reviews` excluded. Three residuals remain, none of which produces wrong
code: one doc still carries `verifyInvariants()` inside two elided snippet comments, `AGENTS.md` is
runtime-delegated but still outside the review set, and two skills have not adopted the new
"commands whose output feeds a verdict" rule. Neutral domain is clean — 0 violations across 1068
added harness lines, now including `.claude/agents`.

---

## Check 1 — Path Validity

Every literal `docs/...`, `.claude/skills/...` and `.claude/agents/...` path mentioned across the
review set was resolved mechanically (a `python3` sweep over all 34 input files, template patterns
containing `{` excluded): **zero broken references**. `.claude/agents/aggregate-slice.md`, referenced
by `docs/workflow.md`, `implement-aggregate-full/SKILL.md` and `_shared/conventions.md`, is now inside
the enumerated set rather than only on disk.

| Source file | Referenced path | Status | Notes |
|-------------|----------------|--------|-------|
| all docs and skills (34 files swept) | every literal `docs/**`, `.claude/skills/**`, `.claude/agents/**` path | Exists | 0 unresolved |
| `docs/workflow.md`, `implement-aggregate-full/SKILL.md:222`, `_shared/conventions.md` | `.claude/agents/aggregate-slice.md` | Exists, **now in the review set** | `review-artifacts` Step 1.b enumerates `.claude/agents`, Step 2 reads it, Hard Rule 5 names it |
| `docs/concepts/commands.md`, `sagas.md`, `aggregate.md`, `testing.md` | `simulator/.../SagaCommand.java`, `Command.java`, `Aggregate.java`, `SagaAggregate.java`, `WorkflowFunctionality.java`, `SagaAggregateRepository.java` | Exists | symbols spot-checked: `setForbiddenStates` present on `SagaCommand`, **absent** on `Command`; `getEventSubscriptions`, `getSagaState`, `executeUntilStep`, `findNonDeletedSagaAggregate` all present |
| `docs/templates/aggregate-grouping-template.md` | `../../docs/concepts/events.md` | Exists | resolves to `docs/concepts/events.md` from `docs/templates/` |
| all skills and docs | `applications/{app-name}/...` | Template pattern | no literal application name hard-coded anywhere |
| `harness-retrospective/SKILL.md`, `review-artifacts/SKILL.md` | `docs/reviews/{...}.md` | Template (generated output) | excluded per Step 1.b |

### Scaffold template contents

All nine templates re-read as code. Every symbol a skill or doc attributes to them is present; no
undocumented helper.

| Template | Symbol claimed | Claimed by | Present? | Notes |
|----------|---------------|------------|----------|-------|
| `AppSpockTest.groovy.template` | `sagaStateOf(Integer)`, `loadForCheck(id, type)`, `flushAndClear()`, `impairmentService`, `loadBehaviorScripts()` | `testing.md` § T2/T3/T4/§ Compensation Test | Yes | lines 33, 39, 48, 19, 27 |
| `AppSpockTest.groovy.template` | does **not** declare `commandGateway` or `aggregateIdGeneratorService` | `testing.md` § T4; `session-b.md` § Update `{AppClass}SpockTest.groovy` | Yes — both absent | claims hold |
| `AppSpockTest.groovy.template` | markers `// Domain constants…`, `// Domain @Autowired fields…`, `// Domain imports`, `// Domain create* helpers` | `session-a.md`, `boot-strap/SKILL.md` Step 4 | Yes | lines 16, 25, 12, 53 |
| `BeanConfigurationSagas.groovy.template` | `@PropertySource("classpath:application-test.properties")`, `ImpairmentService`, `MessagingObjectMapperProvider`, two `CommandGateway` implementations, `// Domain beans are added below` | `boot-strap/SKILL.md` Notes; `testing.md` § Compensation Test, § Serialization Note, § T4 | Yes | lines 32, 65, 75, 85 + 102, 127 |
| `AppErrorMessage.java.template` | `PLACEHOLDER`, `INVARIANT_BREAK`, private constructor | `templates/README.md`; `rule-enforcement-patterns.md` § P1; `session-a.md` | Yes | lines 29, 25, 4 |
| `AppException.java.template` | `{{appClass}}` is the only user of that token | `templates/README.md` § Tokens | Yes | claim holds |
| `AppSimulator.java.template` | `@SpringBootApplication` + `@EnableJpaRepositories`/`@EntityScan` over `...{pkg}` and `...ms` | `boot-strap/SKILL.md` Notes | Yes | lines 12–23 |
| `SpockTest.groovy.template` | package `pt.ulisboa.tecnico.socialsoftware` (parent) | `boot-strap/SKILL.md` Step 4; `workflow.md`; `testing.md` | Yes | line 1 |
| `pom.xml.template` | surefire include `**/{pkg}/sagas/**` | `boot-strap/SKILL.md` Step 6.2 | Yes | line 78 |
| `application-test.yaml.template` | `local.messaging.serialize: true` | `commands.md`; `testing.md` § Test Profile | Yes | line 34 |
| all 9 templates | "Read all nine templates" | `boot-strap/SKILL.md` Step 2 | Yes | exactly 9 exist; Step 2's list matches one-for-one |

---

## Check 2 — Pattern Alignment (P1–P4 and R1–R8)

### P1–P4 Consistency

Authoritative definitions from `docs/concepts/rule-enforcement-patterns.md` § Quick Reference and
§ Decision Guide are unchanged since the previous run.

| Location | Pattern | Claim | Authoritative definition | Verdict |
|----------|---------|-------|--------------------------|---------|
| `docs/architecture.md:247-253` | P1/P2/P3/P4a/P4b | pattern-selection table, all four rows | as in § Quick Reference | Consistent |
| `docs/architecture.md:112-147` — Request Lifecycle | P1/P2/P3 | `[P3]` guard inside `@Transactional(SERIALIZABLE)` → `[P1]` at commit → `[P2]` async | matches § Enforcement Lifecycle | Consistent |
| `classify-and-plan/SKILL.md` Step 4 | P1–P4a/b | defers to § Decision Guide, labels its keyword table "Parser heuristic (mechanical only)" | as above | Consistent — pointer, not restatement |
| `session-a.md:62` | P1 | `verifyInvariants()` reads only fields held by the aggregate (R6) | as above | Consistent |
| `session-c.md:67-68` | P3 | both sub-cases named; guard before mutation | as above | Consistent |
| `session-d.md:133,138` | P2 | cached-field change is registered changed, "which invariant-checks it"; failure propagates and the event is not marked processed | P2 caches only; the predicate firing on the cached copy is P1 on a snapshot field | Consistent — and the mechanism now matches `session-c.md:74-75` (previous Major resolved) |
| `rule-enforcement-patterns.md:82,97` | P2/P3 | elided comments `// ... load, apply, verifyInvariants(), commit` and `// ... load, mutate, verifyInvariants(), registerChanged` | `session-c.md:74-75` and `testing.md:42` — `registerChanged` invokes it | **Inconsistent** — see Major finding below |

### R1–R8 Coverage

| Restriction | Expected location | Mentioned? | Consistent? | Notes |
|-------------|------------------|------------|-------------|-------|
| R1 — service loads only own aggregate type | `service.md`, `session-b.md`, `session-c.md` | Yes | Yes | `service.md:48-50` now states the loading half in its own sentence (previous Minor resolved); `session-c.md:64` |
| R2 — service injects only own components | `service.md`, `session-b.md`, `session-c.md` | Yes | Yes | `service.md` § Injected Dependencies closed list; `session-b.md:171-181`, `session-c.md:300-304` |
| R3 — cross-aggregate flow via DTOs only | `service.md:52`, `session-b.md`, `session-c.md:64` | Yes | Yes | — |
| R4 — saga steps declare lock intent | `sagas.md`, `architecture.md`, `session-c.md` | Yes | Yes | `architecture.md:192` and the `sagas.md:115` decision-table row now both say `SagaCommand` + `setForbiddenStates`, matching `sagas.md:100-104`; "plain command" retained only on the create row, where it is accurate (previous Major resolved) |
| R5 — `getEventSubscriptions()` in consumer only | `events.md:61`, `session-d.md:48` | Yes | Yes | — |
| R6 — `verifyInvariants()` must not read from DB | `aggregate.md:26-29`, `session-a.md:62` | Yes | Yes | — |
| R7 — DTOs are immutable | `service.md` § DTO Immutability, `session-b.md:101-102`, `session-c.md:21` | Yes | Yes | `architecture.md:222-231` carries the WRONG/RIGHT pair |
| R8 — functionalities send commands upstream only | `commands.md:143`, `sagas.md:433`, `session-b.md:99-100`, `session-c.md:120` | Yes | Yes | — |

---

## Check 3 — Improvement Opportunities

### Missing Examples

| Doc | Section | Why an example would help |
|-----|---------|---------------------------|
| `docs/concepts/rule-enforcement-patterns.md` | § P2 — Inter-Invariant | Still three sentences plus a pointer to `events.md`. It states the upstream/downstream direction but shows no `getEventSubscriptions()` fragment, so P2's classification consequence — which aggregate gets the subscription — must be inferred from a different file than the one an agent classifies from |
| `docs/concepts/aggregate.md` | § Repositories | The `{Aggregate}CustomRepositorySagas` snippet is deliberately empty-bodied and `service.md` § Custom Repository owns the JPQL. One two-line delegation example (custom-repo signature → `@Query` method) would remove a cross-file join |

The previous run's two other rows are now discharged: `events.md` § ByEvent sagaState guard carries a
worked Functionalities method **and** the service-method signature it delegates to, and
`service.md` § Method Patterns is reachable from it by an explicit pointer.

### Ambiguous Guidance

| File | Step | Ambiguous phrase | Missing decision criterion |
|------|------|------------------|---------------------------|
| — | — | — | Nothing to report. A sweep for "as appropriate", "if applicable", "as needed", "as necessary", "where appropriate" over the input set returns only `review-artifacts/SKILL.md` § 5.b, which is this check's own phrase list. Every remaining conditional read states its criterion inline |

---

## Check 4 — Neutral Domain

**Run in progress:** `quizzes-full-2` (all sessions complete)
**Base commit:** `0f22736c45d001f314442385ef4007d96ec8afee` — derived from `git merge-base HEAD master`;
the oldest-`harness:`-commit fallback was not needed.
**Added lines scanned:** 1068 across 20 files (`docs`, `.claude/skills`, `.claude/agents`, excluding
`docs/reviews`)

| File | Added line | Noun | Verdict |
|------|-----------|------|---------|
| — | — | — | No hits. 0 Violation / 0 Moved / 0 False positive |

Forbidden nouns from the eight `### {N}. {Aggregate}` headers of `applications/quizzes-full-2/plan.md`:
Course, Execution, Question, Quiz, QuizAnswer, Topic, Tournament, User. Matching used the inflected
form `\b(Noun|…)(s|es|'s)?\b`, run from `python3` rather than `rg`. The 8 spurious hits the previous
run reported are gone: the pathspec now excludes `docs/reviews`, so past reports no longer match
themselves. `.claude/agents` is inside the pathspec for the first time and contributes no hits.

Clean run.

---

## Action Items

| Priority | Category | File | Finding | Suggested Fix |
|----------|---------|------|---------|---------------|
| Major | explicit `verifyInvariants()` — residual | `docs/concepts/rule-enforcement-patterns.md:82`, `:97` | The P2→P3 rewrite snippet still elides the ByEvent body as `// ... load, apply, verifyInvariants(), commit` and the P3 body as `// ... load, mutate, verifyInvariants(), registerChanged`. `session-c.md:74-75` says "do not call `verifyInvariants()` yourself — `registerChanged` invokes it", `testing.md:42` records the same coupling, and `events.md` / `session-d.md` were corrected on 2026-08-10. `simulator/.../SagaUnitOfWorkService.java:186` confirms `registerChanged` calls it, so one doc still prescribes the redundant call the rest of the harness has dropped | Rewrite both elided comments as `// ... load, copy, mutate the copy, registerChanged` — the copy-on-write shape `service.md` § Method Patterns owns — and drop `verifyInvariants()` from both |
| Major | review scope still excludes a runtime-delegated file | `.claude/skills/review-artifacts/SKILL.md` Step 1.b, Step 6.a, Hard Rule 5 | `AGENTS.md` is read at runtime by every Phase 2 entry point (`implement-aggregate/SKILL.md` Step 3b, `implement-aggregate-full/SKILL.md` Step 2 and `.claude/agents/aggregate-slice.md` § Friction all say "read `AGENTS.md` § 'Harness evolution' **in full**"), and it defines the Type 1 / Type 2 / `2-fw` gate the whole self-healing loop turns on. It is in no `find` of Step 1.b, is never consistency-checked, and is outside Check 4's pathspec — the same gap `.claude/agents/` had before today's fix, one level up | Add `AGENTS.md` (and any other repo-root `*.md` a skill delegates to at runtime) to Step 1.b's enumeration, add it to Step 6.a's `PATHSPEC`, and widen Hard Rule 5 to name it |
| Minor | `rg` still feeds a verdict in two skills | `.claude/skills/boot-strap/SKILL.md` Step 6.1; `.claude/skills/harness-retrospective/SKILL.md` Step 2 | `conventions.md` § "Commands whose output feeds a verdict" (added today) requires `python3` wherever a search result decides an outcome, because a `PreToolUse` hook can rewrite `rg` to `grep`. `boot-strap` Step 6.1 still asserts scaffold correctness from `rg -n '\{\{' applications/{app-name}` returning no matches — the exact "clean verdict from a possibly-failed command" shape the rule forbids. `harness-retrospective` Step 2 uses `grep -c -- "- \[ \]"` to decide whether the run is complete | Replace both with a short `python3 -` script, and have each report the number of files/lines it actually scanned so a zero is interpretable |
| Minor | harness-delta listing misses `.claude/agents` | `.claude/skills/harness-retrospective/SKILL.md` Step 3.a.i | `git log --oneline "$(git merge-base HEAD master)"..HEAD -- docs .claude/skills` is called "the run's complete harness delta", but a `harness:` commit touching only `.claude/agents/aggregate-slice.md` would not appear, so the cross-check "every commit in the listing is referenced by some row" silently skips it. Step 8 of the same skill delegates Check 4 to `review-artifacts`, which now *does* cover that directory | Add `.claude/agents` to the pathspec in Step 3.a.i so the delta matches the scope Step 8 measures |

**Critical** = causes an agent to produce incorrect output (wrong code, missing file, wrong beans,
  wrong exception type, broken skill delegation).
**Major** = leaves an agent to infer or guess something the harness should have stated.
**Minor** = cosmetic inconsistency, missing example, ambiguous wording with low impact.
