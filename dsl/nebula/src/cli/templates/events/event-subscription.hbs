package {{packageName}};

{{#each imports}}
{{this}}
{{/each}}

public class {{subscription.capitalizedSubscriptionName}} extends EventSubscription {
    public {{subscription.capitalizedSubscriptionName}}({{subscription.projectionEntityName}} {{subscription.projectionPart}}Projection) {
        super({{subscription.projectionPart}}Projection.get{{subscription.capitalizedAggregateIdField}}(),
                {{subscription.projectionPart}}Projection.get{{subscription.capitalizedVersionField}}(),
                {{subscription.eventTypeName}}.class.getSimpleName());
    }

    public boolean subscribesEvent(Event event) {
         return super.subscribesEvent(event);
    }

}