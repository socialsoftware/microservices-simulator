# Harness Log — trainticket

Append-only. Schema and rules: `.claude/skills/_shared/conventions.md` § "Harness log".

| # | Session | Type | Artifact | Problem | Outcome | Ref |
|---|---------|------|----------|---------|---------|-----|
| 1 | 0 | 1 | `.claude/skills/boot-strap/SKILL.md` | Step 5 needed an inheritable `.mvn/maven.config`, but its fallback chain stops at this checkout's `simulator/.mvn/maven.config`, which is untracked and therefore absent in a linked git worktree; its `>> .git/info/exclude` also fails there because `.git` is a file. | fixed | 5e2d00c97 |
| 2 | 0 | 2 | `AGENTS.md` | Step 6's verification build failed with `release version 21 not supported`; the harness never states the required JDK anywhere, and the shell default is 17. Human chose documenting the requirement over pinning it per-directory. | fixed | 395d4ace2 |
| 3 | 1 | 1 | `.claude/skills/classify-and-plan/SKILL.md` | Step 2.b's §3.2 rule-block regex matched 0 of this spec's 16 blocks and 0 of the shape `docs/templates/domain-model-template.md` § 3.2 itself prescribes: it expects `\| Entities \|` one line after the heading, but the template puts the `\| Field \| Value \|` header and separator rows in between, and rejects the parenthetical qualifier (`STATIONS_EXIST (Route)`) 10 of the 16 headings carry. | fixed | 2709cfbd5 |
| 4 | 1 | 1 | `.claude/skills/classify-and-plan/SKILL.md` | Step 7 declared every file-list path relative to `{src}microservices/{aggregate}/` and asserted `ServiceMapping.java` was the only exception, but `commands/{aggregate}/...` and `events/...` are rooted at the app source root and `sagas/...` at the test root, per `docs/workflow.md` § "Package layout" and `docs/concepts/commands.md` § "File Location". | fixed | 7d0babce6 |
| 5 | 1 | 2 | `.claude/skills/implement-aggregate/session-a.md` | `DocumentType` is a §1 attribute type on three aggregates (User, Contacts, and Order's frozen `contactsDocumentType`), but § "Domain enums" gives only the per-aggregate path and no rule for a shared enum; human chose one shared enum at the app source root over three per-package copies. | fixed | 7d4fad662 |
| 6 | 2.1.a | 1 | `.claude/skills/implement-aggregate/session-a.md` | § `Saga{Aggregate}.java` prescribes the field, the `@Convert` and both constructors but never says the class is `@Entity`; without it Hibernate throws `Unable to locate persister: ...SagaStation` on the first persist, while every T1 case still passes because T1 never persists. | fixed | 7a1b1a477 |
| 7 | 2.1.b | 2 | `docs/concepts/commands.md`, `docs/concepts/sagas.md` | `GetStations` is an unfiltered collection read, but the `rootAggregateId = null` allowance is scoped to create commands and every list-read template takes a `{field}Id`; nothing covers a read with no id to name. Human chose generalising the null rule over adding a two-arg `Command` constructor to `simulator/`. | fixed | a492262d2 |
| 8 | 2.1.b | 1 | `docs/concepts/service.md` | § "Custom Repository - Latest-Active-Version Query" routes the bulk-read JPQL to the JPA repo and the concrete `Sagas` implementation only, but `AGENTS.md` § Architecture principle and § "Injected Dependencies" make the service inject the abstract `{Aggregate}CustomRepository`, through which the method does not exist and `GetStations` does not compile. | fixed | 09cc22b9f |
| 9 | 2.1.b | 2 | `.claude/skills/implement-aggregate/session-b.md` | Closes row 7. § T2 requires a not-found case per read method and its Path A / Path B rule of thumb branches on `aggregateLoadAndRegisterRead` vs an `Optional`-returning repository; `GetStations` returns a `List` and matches neither, an empty result being a valid answer rather than an error. Resolved under the same human decision as row 7 - an unfiltered collection read is a first-class shape - by prescribing an empty-result case. | fixed | 09cc22b9f |
| 10 | 2.1.c | 1 | `.claude/skills/implement-aggregate/SKILL.md`, `.claude/skills/implement-aggregate/session-c.md` | `docs/concepts/testing.md` § T4 makes `{Op}CompensationTest` a required scenario for every write functionality holding a lock across a later step, but session `c` - the only session mapped to T4-write artifacts - listed only `{Op}Test.groovy`, so no session produced it for UpdateStation or DeleteStation. | fixed | 318318b43 |
| 11 | 2.1.c | 1 | `docs/concepts/testing.md` | § T4 mandates `sagaStateOf(id) == NOT_IN_SAGA` on every write happy path, but DeleteStation soft-deletes its own aggregate and `aggregateLoadAndRegisterRead` resolves only non-`DELETED` versions, so the assertion throws `SimulatorException` instead of comparing - the same doc states that filtering rule under § T3. | fixed | 97fa24f8a |
| 12 | 2.2.a | 2 | `.claude/skills/implement-aggregate/session-a.md`, `docs/concepts/aggregate.md` | TRAIN_TYPE_NAME_FINAL is specified as a Java `final` field, but the harness only says such rules need no test - nothing states how a `final` field coexists with the JPA hydration and the copy-constructor the aggregate template mandates (no setter, assigned in all three constructors). Settled mechanically with a throwaway persistence probe (Hibernate 6.6 field access round-trips it), so no code was blocked; the pattern is still undocumented and recurs for Trip, User and Order. | deferred | |
| 13 | 2.2.b | 1 | `.claude/skills/implement-aggregate/session-b.md` | § "Update BeanConfigurationSagas.groovy" passes all five service dependencies to the constructor, but `docs/concepts/service.md` § "Injected Dependencies" declares the factory and `AggregateIdGeneratorService` as `@Autowired` fields and takes only three constructor parameters, so the prescribed bean method does not compile against the prescribed service. | fixed | 3d1af0ad9 |
| 14 | 2.2.c | 1 | `docs/concepts/testing.md`, `.claude/skills/_shared/conventions.md` | § Compensation Test requires confirming the lock-acquiring step's `START EXECUTION STEP` console line before trusting a compensation test, but § "Run the test suite" forbids reading maven stdout and names no mechanism that survives the output-filtering hook - a redirect to a file yielded a condensed transcript with zero matches, wrongly readable as "the step never ran". Verified genuinely for UpdateTrainType and DeleteTrainType via a subprocess capture. | fixed | 59122e1c2 |
| 15 | 2.3.a | 2 | `.claude/skills/implement-aggregate/session-a.md` | § "Domain enums" specifies the enum file (plain Java, no JPA annotations) but says nothing about the JPA mapping of the aggregate *field* typed by it; User is the first aggregate with enum-typed fields (`gender`, `documentType`) and both mappings compile and persist, so the gap is invisible to the build. Proceeded with `@Enumerated(EnumType.STRING)` rather than JPA's `ORDINAL` default, so stored rows survive a reordering of the enum constants; recurs for Contacts, Trip, PriceConfig and Order. | deferred | |
| 16 | 2.4.a | 1 | `.claude/skills/implement-aggregate/session-a.md` | § "Owned entity classes" describes the prescribed collection mapping `@OneToMany(cascade = ALL, orphanRemoval = true)` as making the aggregate "the inverse side with no FK column"; a `@OneToMany` without `mappedBy` is JPA's owning side mapped through a join table, and the entity it names (`RouteStation`) has no back-reference for a `mappedBy` to point at, so the gloss invites a mapping that does not resolve. | fixed | 1ec57d719 |
| 17 | 2.4.a | 2 | `.claude/skills/implement-aggregate/session-a.md` | § "Owned entity classes" mandates a deep copy in the copy constructor for the nested entity-to-entity `@OneToOne` case only; it says nothing for the collection case, where copy-on-write plus `orphanRemoval = true` makes sharing entity instances across aggregate versions a silent data-loss bug. Route's `routeStations` is the first collection the application has. Proceeded by applying the stated `@OneToOne` rule to the adjacent cardinality (`new RouteStation(existing)` per element); recurs for any later aggregate owning a collection. | deferred | |
| 18 | 2.4.b | 1 | `.claude/skills/implement-aggregate/session-b.md` | § "Update `{AppClass}SpockTest.groovy`" requires the `create{Aggregate}` fixture's arguments to be "defaulted to the domain constant" while also requiring its call sites to survive session `c`'s reroute onto the real create saga; for Route the two cannot both hold, since `CreateRoute`'s STATIONS_EXIST data-assembly step fetches each `stationAggregateId` and a constant placeholder id resolves to nothing. `createRoute` mints its stations through `createStation` instead. | fixed | 7957ce979 |
| 19 | 2.4.c | 1 | `.claude/skills/implement-aggregate/session-c.md` | § "One `{Op}Test.groovy` per write functionality (T4)" requires every violation test to assert `{AppClass}Exception` plus `ex.message == {RULE_NAME}`, but its own P4a-prerequisite bullet covers a rule the harness forbids guarding in the service (STATIONS_EXIST): the upstream fetch fails inside `aggregateLoadAndRegisterRead`, which raises `SimulatorException` and has no rule constant, so the mandated assertion cannot be written. | fixed | fe917c1c3 |
| 20 | 2.5.b | 2 | `docs/concepts/commands.md` | § "What a Command Is" names two step shapes that have no root aggregate to pass (create, unfiltered collection read) but not the **filtered** collection read, whose only id belongs to a foreign aggregate - GetContactsByAccount filters Contacts by `userAggregateId`, which is not a Contacts id. Proceeded by passing the foreign filter id, matching GetRoutesByStation from 2.4.b; the doc's own rule that the field is dereferenced only for a step declaring a semantic lock or forbidden states makes the choice inert either way, so nothing in the build can distinguish it. | deferred | |
| 21 | 2.7.a | 2 | `docs/concepts/testing.md` | § "Choosing Input Values - EP & BVA" mandates a boundary-straddling pair for every ordered-domain P1 predicate and gives the tick for integers (`1`/`0`) and for `LocalDateTime` (`.minusNanos(1)`), but PRICE_RATES_POSITIVE compares `BigDecimal` rates against zero and an arbitrary-precision decimal has no smallest positive value, so no on-point exists in the domain itself. Proceeded by taking the on-point from the persisted column's scale (`0.0001` at `scale = 4`), the smallest positive value the mapping can round-trip; recurs for any later aggregate with a `BigDecimal` positivity rule. | deferred | |
| 22 | 2.7.b | 1 | `docs/concepts/commands.md` | Closes row 20. § "What a Command Is" declared "Two step shapes have none" and enumerated create and unfiltered collection read, but GetPriceConfigByRouteAndTrainType is a third: a composite-key read whose target id is unknown until the service resolves the key, so there is no id to pass. The paragraph's own governing property (the field is dereferenced only for a step declaring a semantic lock or forbidden states) already covers it, so the closed enumeration contradicted the rule stated beneath it. Enumeration broadened; the filtered collection read of row 20 is now called out explicitly as passing its foreign filter id. | fixed | 458f696f3 |
| 23 | 2.8.a | 1 | `.claude/skills/implement-aggregate/session-a.md` | § "`{Aggregate}SagaState.java`" said a simple two-step write saga (read then write-as-final-step) needs only `READ_{AGGREGATE}`, while `docs/concepts/sagas.md` § "Lock-Acquisition Step Pattern (Two-Step Write Sagas)" locks exactly that shape with `IN_UPDATE_{AGGREGATE}` and § "Step Ordering" item 3 requires the lock step regardless of how many steps follow; the two also left no naming rule for Order's five domain-specific mutating operations (PayOrder, CollectTicket, UseTicket, CancelOrder, DeleteOrder), none of which is an "update" or a "delete". | fixed | 265d7ff91 |
