# Artifacts Review — 2026-08-23

**Date:** 2026-08-23
**Skill files reviewed:** 13 (`.claude/skills/**/*.md`)
**Doc files reviewed:** 11 (`docs/**/*.md`, excluding generated `docs/reviews/`)
**Verdict:** Issues requiring action

> **Clean** = all alignment checks pass; only improvement opportunities found.
> **Minor issues** = inconsistencies present but none cause incorrect agent output.
> **Issues requiring action** = any inconsistency that would cause an agent to produce
>   incorrect output.

---

## Executive Summary

The most serious finding is that `implement-aggregate-full/SKILL.md` § "Shared files per session
type" omits, for session `d`, the two shared files `session-d.md` mandates appending to
(`{Aggregate}Functionalities.java` for the `ByEvent` methods and `{Aggregate}Service.java` for their
service helpers), plus `{Aggregate}.java` for `getEventSubscriptions()`. A `d` slice is scoped by
its brief (`.claude/agents/aggregate-slice.md` § Scope: "append to the shared files listed in your
brief"), so a sliced session `d` can produce subscription classes with no `ByEvent` method behind
them. Two secondary gaps sit in this skill itself: `.claude/agents/aggregate-slice.md` is a
runtime-delegated harness file that neither the Step 1.b enumeration nor the Check 4 diff pathspec
covers, and Check 4's pathspec includes the generated `docs/reviews/` tree that Step 1.b excludes.
Pattern alignment (P1–P4, R1–R8) and path validity are otherwise clean, and the neutral-domain
check found zero violations across 513 added harness lines.

---

## Check 1 — Path Validity

Every literal `docs/...`, `.claude/...` and `simulator/...` path in every doc and skill was resolved
against disk, and every relative markdown link target was normalised and resolved. Two paths do not
exist on disk; both are correct as written.

| Source file | Referenced path | Status | Notes |
|-------------|----------------|--------|-------|
| `.claude/skills/_shared/session-completion.md:175` | `simulator/path/to/ChangedFile.java` | Template | Placeholder inside the retro template's `### simulator/...` block, not a reference |
| `.claude/skills/boot-strap/SKILL.md:113,146` | `simulator/.mvn/maven.config` | Absent by design | The same skill states the file is untracked machine config; absence in a clean worktree is the case Step 5 handles |
| all other literal paths (all files) | — | Yes | No broken reference found |
| all relative markdown links (all files) | — | Yes | Includes `docs/templates/aggregate-grouping-template.md:61` → `../../docs/concepts/events.md`, which resolves correctly from `docs/templates/` |
| every `applications/...` path (all files) | — | Template | Scanned mechanically: every occurrence is written `applications/{app-name}/...`. No literal application name is hard-coded anywhere in `docs/` or `.claude/` |

Nothing to report as a finding.

---

## Check 2 — Pattern Alignment (P1–P4 and R1–R8)

### P1–P4 Consistency

Authoritative definitions from `docs/concepts/rule-enforcement-patterns.md` § Quick Reference:
P1 = single-aggregate state rule in `verifyInvariants()` (strong); P2 = cache state from another
aggregate via `getEventSubscriptions()` + handler chain (eventual, never blocks); P3 = synchronous
service guard in `*Service.java`, own-table uniqueness **or** saga-assembled DTO validation (strong);
P4a = precondition implicit in a saga fetch query, no code; P4b = invariant holds because the saga
passes the same value to two aggregates, no code.

| Location | Pattern | Claim | Authoritative definition | Verdict |
|----------|---------|-------|--------------------------|---------|
| `docs/architecture.md` § Choosing the Right Enforcement Pattern | P1 | "Always true within one aggregate; derivable from its own fields → P1 — `verifyInvariants()`" | as above | Consistent |
| `docs/architecture.md` § Choosing the Right Enforcement Pattern | P3 | "Synchronous service-level check (own-table uniqueness OR saga-assembled DTO field validation)" | as above | Consistent (both sub-cases named) |
| `docs/architecture.md` § Choosing the Right Enforcement Pattern | P2 | "Cross-aggregate; eventual consistency is acceptable" | as above | Consistent |
| `docs/architecture.md` § Choosing the Right Enforcement Pattern | P4a/P4b | "Precondition implicit in saga fetch / same value to two aggregates" | as above | Consistent |
| `docs/architecture.md` § Request Lifecycle | P1/P2/P3 | `[P3]` service guard inside `@Transactional(SERIALIZABLE)` → `[P1]` at UoW commit → `[P2]` async ~1 s | matches § Enforcement Lifecycle of the authoritative doc exactly | Consistent |
| `.claude/skills/classify-and-plan/SKILL.md` § Step 4 | P1–P4a/b | Defers: "Apply the flowchart in `docs/concepts/rule-enforcement-patterns.md` § Decision Guide … do not re-derive the classification logic here"; the keyword table is labelled "Parser heuristic (mechanical only … not part of the doc's decision criteria)" | — | Consistent; pointer, not a restatement |
| `.claude/skills/implement-aggregate/session-a.md` § `{Aggregate}.java` | P1 | "`verifyInvariants()`: enforces all **P1 rules** for this aggregate listed in plan.md" | as above | Consistent |
| `.claude/skills/implement-aggregate/session-c.md` § `{Aggregate}Service.java` | P3 | "P3 own-table uniqueness guards" and "P3 DTO field checks … receive the saga-assembled DTO as a parameter" | as above | Consistent (both sub-cases named) |
| `.claude/skills/implement-aggregate/session-d.md` § `{Aggregate}EventProcessing.java` | P2 | "**P2 rule enforcement:** The invariant check happens on the ByEvent path, when the service method's `registerChanged` call invokes `verifyInvariants()`" | P2 caches state; never blocks | Consistent |

No P1–P4 drift found.

### R1–R8 Coverage

Authoritative one-liners from `docs/architecture.md` § Architectural Restrictions.

| Restriction | Expected location | Mentioned? | Consistent? | Notes |
|-------------|------------------|------------|-------------|-------|
| R1 — service loads only own aggregate type | `service.md`, session-b.md, session-c.md | Partial | Partial | `service.md:35` cites R1 and R2 together but states only the *injection* half ("Never inject a foreign service class or a foreign repository"); R1's own content (never call `aggregateLoadAndRegisterRead` for a foreign type) is stated in `architecture.md` alone. session-c.md:23 states it correctly ("the service loads only its own aggregate type"). session-b.md cites "R1/R2" at line 189 in the injection sense only |
| R2 — service injects only own components | `service.md`, session-b.md, session-c.md | Yes | Yes | `service.md` § Injected Dependencies names the closed list; session-b.md:186-196 repeats the closed list and the constructor/field split |
| R3 — cross-aggregate flow via DTOs only | `service.md`, session-b.md, session-c.md | Yes | Yes | `service.md:37`; session-b.md:24; session-c.md:23 |
| R4 — saga steps declare lock intent | `sagas.md`, session-c.md | Yes | Yes | `sagas.md` § R4 Decision Table carries all three cases (primary / foreign / newly created); `architecture.md` § R4 names the same three and defers to it; session-c.md:39,115 points there |
| R5 — `getEventSubscriptions()` in consumer only | `events.md`, session-d.md | Yes | Yes | `events.md:60`; `aggregate.md:31` also states it; session-d.md:23 |
| R6 — `verifyInvariants()` must not read from DB | `aggregate.md`, session-a.md | Yes | Yes | `aggregate.md:26`; session-a.md:23 |
| R7 — DTOs are immutable value objects | `service.md`, session-b.md, session-c.md | Yes | Yes | `service.md` § DTO Immutability (R7); session-b.md:26; session-c.md:21 |
| R8 — functionalities only send commands upstream | `commands.md`, session-b.md, session-c.md | Yes | Yes | `commands.md:132`; `sagas.md:408`; session-b.md:36; session-c.md:115 |

---

## Check 3 — Improvement Opportunities

### Missing Examples

| Doc | Section | Why an example would help |
|-----|---------|---------------------------|
| `docs/concepts/sagas.md` | § Step Ordering, item 1 (validate-dates step) | The criterion is written against literal field names (`startTime`/`endTime`) and a literal downstream example ("e.g., a Shipment"), while `session-c.md:117` restates it abstractly as "time-based invariants". An aggregate whose temporal fields are named anything else can be read as out of scope. A neutral worked example keyed on "any pair of ordered temporal fields" would close the gap |
| `docs/concepts/events.md` | § EventSubscription, `subscribedVersion` paragraph | The `0L` fallback ("If the subscriber entity does not track the publisher's version … use `0L`") describes a case `classify-and-plan` § Step 3.d now forbids: a snapshot that subscribes always gets a version field, and one that does not subscribe never constructs a subscription. The branch is unreachable under current planning rules and should either be deleted or annotated with the case that still reaches it |
| `docs/concepts/aggregate.md` | § Repositories | The three-artifact table and `service.md` § Custom Repository — Latest-Active-Version Query both describe the interface/impl split, but neither shows an end-to-end example of one query travelling across all three files. `service.md`'s three-row table names the roles; a single worked method spanning them would remove the last inference |

### Ambiguous Guidance

| File | Step | Ambiguous phrase | Missing decision criterion |
|------|------|------------------|---------------------------|
| `.claude/skills/implement-aggregate-full/SKILL.md` | § Slice brief, `FILES YOU CREATE` | "the per-item files for this item, from the plan.md `2.{N}.{type}` row" | Session `c`'s row carries aggregate-level files that are not per-item: `{Aggregate}Controller.java` (unconditional, one per aggregate) and `{AppClass}ErrorMessage.java`. Nothing states which slice owns them. See Action Items |
| `.claude/skills/implement-aggregate-full/SKILL.md` | § Shared files per session type | table is presented as complete | Sessions `c` and `d` append to files the table omits. See Action Items |
| `docs/workflow.md` | § Test naming | table lists five test types | `{Operation}CompensationTest.groovy` is missing, although `classify-and-plan` § Step 7 lists it in the 2.N.c row and `testing.md` § Compensation Test makes it required for lock-holding write functionalities |

---

## Check 4 — Neutral Domain

**Run in progress:** `trainticket` (resolved per `_shared/conventions.md` § "Resolve app context";
both `plan.md` files are fully ticked, so tie-break 2 applied — `applications/trainticket` carries
the more recent commit)
**Base commit:** `0f22736c45d001f314442385ef4007d96ec8afee`, derived from
`git merge-base HEAD master`. The fallback was not needed: `HEAD` is
`e0cfeeadd61cdffc0cfb2d8509956f3cbdb9f9b1` on branch `trainticket`, which differs from the
merge-base, so the first rule yielded a usable base. 49 `harness:` commits touch `docs/` and
`.claude/skills/` between the base and `HEAD`.
**Added lines scanned:** 513 (`git diff <BASE> -- docs .claude/skills -M`, additions only,
excluding the generated `docs/reviews/` tree per Step 1.b)
**Forbidden nouns:** `Contacts`, `Order`, `PriceConfig`, `Route`, `Station`, `TrainType`, `Trip`,
`User` — one per `### {N}. {Aggregate}` header in `applications/trainticket/plan.md`

| File | Added line | Noun | Verdict |
|------|-----------|------|---------|
| `docs/reviews/review-2026-08-23.md` | `**Forbidden nouns:** \`Contacts\`, \`Order\`, \`PriceConfig\`, \`Route\`, \`Station\`, \`TrainType\`, \`Trip\`, \`User\` (from …)` | all 8 | False positive — the line is the previous run's own report of this same check, inside `docs/reviews/`, which Step 1.b excludes from the artifact set. It is scanned only because Step 6.b's pathspec is broader than Step 1.b's enumeration (see Action Items) |

**Counts: 0 Violation / 0 Moved / 8 False positive.**

Excluding the generated report tree, zero occurrences of any forbidden noun appear in the 513 lines
this run added to `docs/` and `.claude/skills/`. This is a clean run for the neutral-domain rule.

Scope caveat: `.claude/agents/aggregate-slice.md` changed during this run (3 added lines) and is
**not** covered by the Step 6.b pathspec, so those lines were never scanned. Re-running the same
match over `docs .claude` (which does include it) also yields zero hits, so the omission produced no
missed violation this time — but the check does not cover the file by construction.

---

## Action Items

| Priority | Category | File | Finding | Suggested Fix |
|----------|---------|------|---------|---------------|
| Critical | slice scope | `.claude/skills/implement-aggregate-full/SKILL.md` § "Shared files per session type" | The `d` row lists only `{Aggregate}EventHandling.java`, `{Aggregate}EventHandler.java`, `{Aggregate}EventProcessing.java`, `{Aggregate}InterInvariantTest.groovy`. `session-d.md` § "'ByEvent' methods in Functionalities — mandatory pattern" states "add a separate `{operation}ByEvent` method to `{Aggregate}Functionalities`" and "Write a new service helper … only when no existing service method does", and § "`getEventSubscriptions()` — required ACTIVE guard" requires editing `{Aggregate}.java`. None of those three files is in the row. `.claude/agents/aggregate-slice.md` § Scope binds a slice to "append to the shared files **listed in your brief**", and the brief is filled from this table — so a sliced `d` session can ship subscription classes with no `ByEvent` method behind them | Add `{Aggregate}Functionalities.java` (one `{operation}ByEvent` method per event), `{Aggregate}Service.java` (the ByEvent service helper, when no existing mutate method fits) and `{Aggregate}.java` (one `getEventSubscriptions()` helper per inter-invariant) to the `d` row |
| Major | slice scope | `.claude/skills/implement-aggregate-full/SKILL.md` § "Shared files per session type" | `{AppClass}ErrorMessage.java` is absent from the `c` and `d` rows, yet `session-c.md` § "Error message constants" ("add constants for: P3 guard violations introduced in this session") and `session-d.md` § "Error message constants" both require appending to it. `{AppClass}SpockTest.groovy` is absent from the `b` row, yet `session-b.md` § "Update `{AppClass}SpockTest.groovy`" requires both the `@Autowired` field and the `create{Aggregate}(...)` helper there | Add `{AppClass}ErrorMessage.java` to the `c` and `d` rows and `{AppClass}SpockTest.groovy` to the `b` row. The `c` row's `{AppClass}SpockTest.groovy` is already covered by the "replaced, not appended to" note below the table |
| Major | slice ownership | `.claude/skills/implement-aggregate-full/SKILL.md` § "Slice brief" | `FILES YOU CREATE: <the per-item files for this item, from the plan.md 2.{N}.{type} row>` has no rule for the aggregate-level files that row also carries. `classify-and-plan` § Step 7 marks `{Aggregate}Controller.java` as "**unconditional** … List it for every aggregate; it is not gated on the aggregate having any HTTP-facing functionality" — it is one file per aggregate, not per write op, so no slice owns it | State that aggregate-level (non-per-item) files in a sliced session belong to slice `k=1`, alongside the existing rule that gives `c1` the `create{Aggregate}()` helper body |
| Major | review scope | `.claude/skills/review-artifacts/SKILL.md` § Step 1.b, Step 2, Step 6.b | `.claude/agents/aggregate-slice.md` is a harness file — `docs/workflow.md` § "Two entry points" calls it "Its contract for a slice", and `implement-aggregate-full` delegates to it at runtime — but Step 1.b enumerates only `find .claude/skills`, so it is never read, and Step 6.b diffs only `docs .claude/skills`, so its added lines are never scanned for domain nouns. It changed during this run. `AGENTS.md` § "Harness evolution" defines the harness delta as `git log --oneline docs/ .claude/`, which does include it | Widen Step 1.b's second `find` and Step 6.b's diff pathspec from `.claude/skills` to `.claude`, matching `AGENTS.md`'s own definition of the harness delta |
| Minor | review scope | `.claude/skills/review-artifacts/SKILL.md` § Step 6.b | The Check 4 diff pathspec includes `docs/reviews/`, which Step 1.b explicitly excludes from the artifact set. Every run after the first therefore re-scans the previous run's own "Forbidden nouns" line and reports one guaranteed false positive per aggregate — 8 this run | Exclude the generated tree in the diff: `git diff "$BASE" -- docs .claude ':(exclude)docs/reviews' -M` |
| Minor | doc completeness | `docs/workflow.md` § "Test naming" | The table omits `{Operation}CompensationTest.groovy`, which `classify-and-plan` § Step 7 lists in the 2.N.c row and `docs/concepts/testing.md` § Compensation Test makes required for every write functionality holding a lock across a later step | Add a `T4 Write Compensation \| {Operation}CompensationTest.groovy \| 2.N.c` row |
| Minor | stale guidance | `docs/concepts/events.md` § EventSubscription | The `subscribedVersion` `0L` fallback covers a subscriber that "does not track the publisher's version", but `classify-and-plan` § Step 3.d now requires a version field on every subscribing snapshot and forbids one on a non-subscribing row, making the branch unreachable | Delete the `0L` sentence, or scope it explicitly to the case that still reaches it |
| Minor | restriction coverage | `docs/concepts/service.md:35` | "Never inject a foreign service class or a foreign repository — see [R1, R2 in architecture.md]" cites R1 while stating only R2's content. R1's own rule — `aggregateLoadAndRegisterRead` is called only for the service's own aggregate type — appears nowhere in `service.md`, the doc Check 2 expects to carry it | Add one sentence stating R1 in its own terms before the R2 sentence |
