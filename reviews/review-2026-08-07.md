# Artifacts Review — 2026-08-07

**Date:** 2026-08-07
**Skill files reviewed:** 13
**Scaffold templates reviewed:** 9
**Doc files reviewed:** 11 (`docs/reviews/*.md` — 14 files — excluded as generated output per the skill's own "Generated outputs excluded from input set" rule)
**Verdict:** Clean

> **Clean** = all alignment checks pass; only improvement opportunities found.
> **Minor issues** = inconsistencies present but none cause incorrect agent output.
> **Issues requiring action** = any inconsistency that would cause an agent to produce
>   incorrect output.

---

## Executive Summary

No broken references, no P1–P4 or R1–R8 drift, and zero neutral-domain violations were found across the harness. This run (`quizzes-full-2`) has accumulated 60 `harness:` commits since master, and the artifacts remain internally consistent through all of them — a run in progress is repairing itself as designed, not accumulating contradictions. No action items. Proceed with the next aggregate session.

---

## Check 1 — Path Validity

Every literal `docs/...` and `.claude/skills/...` path referenced from an actual harness file (concept docs, skill files, session sub-files — excluding `docs/reviews/*.md`, which are generated reports outside the review's own input set) resolves to a file in the Step 1.b enumeration.

| Source file | Referenced path | Status | Notes |
|-------------|----------------|--------|-------|
| `docs/architecture.md`, `docs/concepts/*.md`, `.claude/skills/**/*.md` | `docs/architecture.md`, `docs/concepts/{aggregate,commands,events,rule-enforcement-patterns,sagas,service,testing}.md`, `docs/workflow.md` | Yes | All resolve |
| `.claude/skills/**/*.md` | `.claude/skills/_shared/{conventions,session-completion}.md`, `.claude/skills/boot-strap/{SKILL.md,templates/README.md}`, `.claude/skills/classify-and-plan/SKILL.md`, `.claude/skills/implement-aggregate{-full,}/SKILL.md`, `.claude/skills/implement-aggregate/session-{a,b,c,d}.md`, `.claude/skills/review-artifacts/SKILL.md` | Yes | All resolve |
| `docs/workflow.md`, `implement-aggregate-full/SKILL.md` | `.claude/agents/aggregate-slice.md` | Yes | Exists on disk; outside `.claude/skills/` by design (agent definition, not a skill) |

**Stale references found, all confined to `docs/reviews/*.md` (excluded from scope):** `docs/concepts/tcc.md`, `docs/examples/example-{domain-model,aggregate-grouping}.md`, `.claude/skills/{adversarial-review-aggregate,retro,review-aggregate-vs-quizzes,review-aggregate,review-tests}/SKILL.md` are named only inside historic review reports (`review-2026-05-01.md`, `review-2026-05-23.md`, `review-2026-07-23.md`, `review-2026-08-01.md`, `review-2026-08-01-2.md`, `agnosticism-review-2026-07-19.md`), never inside a live doc or skill. This matches the memory note that `review-aggregate-vs-quizzes` is retired and historic-only. Not a finding — these files were never in Check 1's scope.

**Literal application names:** the only `applications/{literal-name}/` occurrences outside `docs/reviews/` are `applications/my-app/` in `classify-and-plan/SKILL.md:21`, which is the skill's own illustrative example name (`/boot-strap my-app`), used the same way `docs/workflow.md` and `boot-strap/SKILL.md` use it — not a leaked run-specific name. No Major finding.

### Scaffold template contents

| Template | Symbol claimed | Claimed by | Present? | Notes |
|----------|---------------|------------|----------|-------|
| `AppSpockTest.groovy.template` | `impairmentService`, `unitOfWorkService`, `entityManager` fields | `testing.md` § Compensation Test, § T2 (implicitly) | Yes | Lines 20-25 |
| `AppSpockTest.groovy.template` | `loadBehaviorScripts()` | `testing.md` § Compensation Test | Yes | Lines 29-33 |
| `AppSpockTest.groovy.template` | `sagaStateOf(Integer)` | `testing.md` § T4 | Yes | Lines 35-39 |
| `AppSpockTest.groovy.template` | `loadForCheck(Integer, Class)` | `testing.md` § T3 | Yes | Lines 41-44 |
| `AppSpockTest.groovy.template` | `flushAndClear()` | `testing.md` § T2 | Yes | Lines 50-53 |
| `AppSpockTest.groovy.template` | marker comments (`// Domain constants...`, `// Domain @Autowired fields...`, `// Domain create* helpers...`) | `session-a.md`, `session-b.md`, `session-c.md` | Yes | Session-a.md's own shorthand (`` `// Domain constants are added here` ``) truncates the template's full comment text (`...as aggregates are implemented in Phase 2.`) but is unambiguous in context — Minor, not worth a fix |
| `BeanConfigurationSagas.groovy.template` | infrastructure beans (`AggregateIdGeneratorService`, `EventApplicationService`, `SagaCommandHandler`, etc.) plus `// Domain beans are added below` marker | `boot-strap/SKILL.md`, `session-a.md`/`b`/`d` (`@Bean` insertion instructions) | Yes | Marker present at line 127 |
| `AppException.java.template`, `AppErrorMessage.java.template` | exception constructors, `PLACEHOLDER` constant | `boot-strap/SKILL.md` (implicitly, via "9 templates are complete") | Yes | No specific symbol claims made beyond "complete and correct as-is" |

No Critical or Major findings. One Minor note (marker-comment shorthand) recorded above — no fix needed, agents resolve it unambiguously from context.

---

## Check 2 — Pattern Alignment (P1–P4 and R1–R8)

### P1–P4 Consistency

| Location | Pattern | Claim | Authoritative definition | Verdict |
|----------|---------|-------|--------------------------|---------|
| `docs/architecture.md` — pattern selection table | P1/P2/P3/P4 | P1 = derivable from own fields; P3 = sync service-level (own-table OR saga-assembled DTO); P2 = cross-aggregate eventual; P4a/b = by construction | Matches `rule-enforcement-patterns.md` Quick Reference exactly | Consistent |
| `docs/architecture.md` — Request Lifecycle | P1/P2/P3 | Same P1/P2/P3 roles, placed correctly in the lifecycle diagram (P3 before commit, P1 at commit, P2 async after) | Matches `rule-enforcement-patterns.md` § Enforcement Lifecycle ordering | Consistent |
| `classify-and-plan/SKILL.md` — Step 4 | P1–P4a/b | Delegates to `rule-enforcement-patterns.md` § Decision Guide explicitly, does not restate | N/A (pointer) | Consistent |
| `session-a.md` | P1 | `verifyInvariants()` recipe matches P1 recipe (private boolean predicates, combined throw, domain-sentinel comparison via `{App}DomainConstants`) | Matches `rule-enforcement-patterns.md` § P1 exactly | Consistent |
| `session-c.md` | P3 | P3 guard placement (top of method, before copy), own-table + DTO-check sub-cases | Matches `rule-enforcement-patterns.md` § P3 and `service.md` § P3 Guard Placement | Consistent |
| `session-d.md` | P2 | Subscription class, `getEventSubscriptions()` ACTIVE guard, polling, EventProcessing delegation | Matches `rule-enforcement-patterns.md` § P2 recipe | Consistent |

### R1–R8 Coverage

| Restriction | Expected location | Mentioned? | Consistent? | Notes |
|-------------|------------------|------------|--------------|-------|
| R1 — service loads only own aggregate | `service.md`, `session-c.md` | Yes | Yes | `service.md` § Injected Dependencies; `session-c.md` line 64 |
| R2 — service injects only own components | `service.md`, `session-c.md` | Yes | Yes | Same locations, closed-list wording matches |
| R3 — cross-aggregate flow via DTOs only | `service.md`, `session-c.md`, `session-b.md` | Yes | Yes | `service.md` § Injected Dependencies + § DTO Immutability; `session-b.md:100-102` (R8/R7 citation, DTO-as-is) |
| R4 — saga steps declare lock intent | `sagas.md`, `session-c.md` | Yes | Yes | `sagas.md` § R4 Decision Table; `session-c.md:35,120` |
| R5 — `getEventSubscriptions()` in consumer only | `events.md`, `session-d.md` | Yes | Yes | `events.md` § EventSubscription; `session-d.md:44-48` |
| R6 — `verifyInvariants()` must not read from DB | `aggregate.md`, `session-a.md` | Yes | Yes | `aggregate.md:26-29`; `session-a.md:62` |
| R7 — DTOs are immutable | `service.md`, `session-b.md`, `session-c.md` | Yes | Yes | `service.md` § DTO Immutability (R7); `session-b.md:101-102` |
| R8 — functionalities send commands upstream only | `commands.md`, `session-b.md`, `session-c.md` | Yes | Yes | `commands.md:135`; `session-b.md:99-100`; `session-c.md:120` |

All eight restrictions are cited by name at every location the checklist expects, with definitions that agree with `architecture.md`'s authoritative text.

---

## Check 3 — Improvement Opportunities

### Missing Examples

Nothing to report. Every pattern description in the concept docs (P1–P4, R1–R8, the ByEvent guard, the compensation mechanism, the collection-valued data-assembly step) ships a worked code example.

### Ambiguous Guidance

Scanned all skill files for hedge phrases ("depends on", "may be", "if applicable", "as appropriate", "as needed"). All matches are either descriptive uses of "may be" (e.g. "Entities contained... may be comma-separated") or are immediately followed by a concrete decision rule (e.g. `classify-and-plan/SKILL.md:181` "depends on one thing only:" followed by the exact criterion). Nothing to report.

---

## Check 4 — Neutral Domain

**Run in progress:** quizzes-full-2
**Base commit:** `0f22736c45d001f314442385ef4007d96ec8afee` (`git merge-base HEAD master`) — the primary rule; it did not degenerate to HEAD, so the `harness:`-commit fallback was not needed.
**Added lines scanned:** 695 (harness delta over `docs/` and `.claude/skills/`, excluding `docs/reviews/` — the prior 2026-08-06 review established this exclusion is necessary, since `docs/reviews/*.md` legitimately quotes the run's own forbidden-noun list in past Check 4 output and would otherwise generate false positives on every subsequent review)

Forbidden nouns for this run (from `plan.md` `### {N}. {Aggregate}` headers): Course, Execution, Question, Quiz, QuizAnswer, Topic, Tournament, User.

| File | Added line | Noun | Verdict |
|------|-----------|------|---------|

No hits against the 695 scanned lines. Zero matches — not even a `Moved` or `False positive` case this time (all three of those types were exhausted by earlier reviews and the harness has stayed clean since).

**Violations: 0. Moved: 0. False positives: 0.** Clean run.

---

## Action Items

Nothing to report. No Critical, Major, or Minor findings require action.

**Critical** = causes an agent to produce incorrect output (wrong code, missing file, wrong beans,
  wrong exception type, broken skill delegation).
**Major** = leaves an agent to infer or guess something the harness should have stated.
**Minor** = cosmetic inconsistency, missing example, ambiguous wording with low impact.
